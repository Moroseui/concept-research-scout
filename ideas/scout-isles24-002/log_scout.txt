The scout stage is complete: `ideas/scout-isles24-002/scout_candidates.json` is written and validated (parses cleanly, 5 candidates, all scores in the canonical `{"value": N, "why": ...}` shape, no `keystone_evidence: null` anywhere).

**Dataset verification (charter hard rule).** I re-verified ISLES'24 against four primary sources today: the Zenodo release (149 training cases, CC BY-NC-SA 4.0, NCCT/CTA/4D CTP/DWI/ADC plus Tmax/CBF/CBV/MTT maps), the official GitHub repo (co-registered derivatives, four ranking metrics), and both arXiv papers. Two findings worth flagging: the release's hosting pages now explicitly enumerate **per-case large-vessel-occlusion binary masks** — the fact last cycle could not verify — and the arXiv abstract's 150/98 split are planned counts; the realized 149/96 govern.

**Ten questions generated, five developed** — all lead with an X a physician would want to hear, none repeats cycle-001 territory (collaterals, veins, topology, truncation, deconvolution):

1. **c01 (Mode A)** — *The water already in the tissue*: does the model use **net water uptake**, the validated NCCT edema clock, tested by a graded "give the water back" counterfactual? Keystone INSPECTED_TRUE.
2. **c02 (Mode B)** — *The healthy hemisphere is the ruler*: does the model normalize perfusion to the **contralateral mirror** (the field's own rCBF convention)? The intervention region and readout region are spatially disjoint — the cleanest identifiability structure this cycle, and a null is itself a positive finding (absolute-value fragility).
3. **c03 (Mode B)** — *Two tissues, two death thresholds*: does the model apply the published **gray-vs-white ischemic thresholds** (CBF<30% vs <20%)? Keystone honestly NOT_INSPECTED (tissue-class mapping on acute NCCT).
4. **c04 (Mode C, priority 4.05)** — *The barrier is already leaking*: does a raw-CTP model use **Patlak blood-brain-barrier leakage** in the acquisition tail? Gated on an uninspected duration keystone, stated openly.
5. **c05 (Mode C, priority 4.15)** — *The clot that lets contrast through*: does the model read **thrombus perviousness** inside the newly verified occlusion masks, with the perfusion shadow held fixed? This dodges idea-016's timing kill via the bolus-normalized dynamic variant from raw CTP.

Zero revivals — nothing in the portfolio brief has a newly verified unblock fact, and I said so rather than padding. The single-dataset quota conflict with the charter is disclosed again in `quota_note`, along with the template spread (two counterfactual-synthesis cards flagged for the homogenization watch, one justified `other:remote-perturbation`). Every candidate carries a one-line use-vs-association distinction, a `dies_like_prior` mapping, and primary-source citations for its X measurement; the few things I could not pin today (e.g., Minnerup 2016's DOI, file-level mask granularity inside the 99 GB archive) are listed in `unverified_claims` rather than asserted.
