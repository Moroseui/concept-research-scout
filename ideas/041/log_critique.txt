Critique complete and committed (`5829210`). **Decision: ADVANCE TO REVISION — no fatal objection.** The full analysis is in `ideas/041/critique.md`.

**What the adversarial pass found:**

- **The keystone holds and the novelty delta is real.** The feature paper (PMID 40824507) verifiably computed Higuchi fractal dimension on the exact 149-case public cohort, and a fresh search found no existing model-use test of the feature — only association and ordinary architecture-ablation work.
- **All three cited neighbors have fabricated author names** (verified against publisher pages today): "Lim et al." is actually Ichikawa et al., "Robben et al." is Winder et al., and "van Os et al." is Klug et al. Every identifier and every content description checks out, so this is citation hygiene, not a novelty failure — but it's systematic and must be repaired.
- **The claim object is ambiguous.** The deliverable says "**the** final-infarct model," but the experiment trains a small U-Net from scratch on 20 cases. The question's indefinite article already covers the honest reading, so this is repairable without changing the question's identity.
- **The sharpest scientific risk:** the same paper reports FD–CBF correlation rho > 0.9 in the phantom, so within a CBF decile there may be little non-noise FD variance left to erase. I required a preregistered within-CBF-stratum decodability gate — without it, this dies like the charter's eleven prior IDENTIFIABILITY_FAILURE kills; with it, that failure mode gets detected instead of narrated.
- Smaller defects: the erasure is stated in two incompatible spaces (input-curve editing vs. encoder erasure), `prior_legwork` is inflated (the feature paper's code is request-only, so 5 → 3, priority ~3.6), the 25 GB staging assumption from the ~99 GB Zenodo archive is unverified, and the plain pitch drops the card's own hedge ("it does" vs. the card's demoted "X-associated representation") — a named pitch-fidelity defect.

**Constructive outcome:** the nearest defensible version is this same candidate, revised — with the decodability gate plus an input-space curve-resynthesis necessity arm (with a validation that the resynthesis actually moves FD, since spectrum-preserving surrogates can leave FD intact). The association-only smaller version ("does FD add predictive value?") exists as low-hanging fruit but is not worth doing standalone; it's the exact association-vs-use conflation the charter polices, though it falls out nearly free as a Stage-0 byproduct.
