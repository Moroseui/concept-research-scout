Phase-B probe code for idea 047 is implemented. `probes/047/run.py` is now the contract-v3 Phase-B implementation (the Phase-A code it replaces remains of record in git history), with `README.md`, `requirements.txt`, and `verification.json` updated to match. The commit is left to the pipeline driver, matching both Phase-A rounds.

**Authority verified before writing anything:** the approval marker binds contract blob `dc586665d0be...`, which is exactly what the working-tree contract hashes to, and the contract is the amended v3 (sentinel absent, variable list bound). The coding gate — reviewed card, feasibility memo, contract, human approval — is satisfied.

**What the new run.py does, in contract order:** identity gates (five SHA-256 pins, member-manifest git blob, the Phase-A bundle's own recorded governing blob, head-membership cross-check, and exact `math.fsum` recomputation of all seven frozen Phase-A constants); split freeze written and hashed before any archive byte is touched; one selective `7z` extraction of exactly the 198 phenotype members after archive byte-count and md5 verification, each staged file size- and CRC32-checked against the frozen manifest; the pre-registered schema/missingness census with the `PHENOTYPE_SCHEMA_MISMATCH` stop (exit 4, reduced interface); then the single 10-versus-89 estimation table with the closed statistic menu, both frozen uncertainty displays (leave-one-head-out ranges plus 10,000 seed-20260902 relabelings under the verbatim exchangeability label, one shared draw set), the D4 support columns on every row, and the frozen small-cell suppression with every suppression logged. Smoke builds a 12-case synthetic world that exercises the `sub-strokecase` spelling, real CRC verification, empty/parse-failure/decimal-normalized/out-of-set/two-row-file/fully-missing paths, and real suppression, and asserts each path actually fired.

**Verification was desk-bound:** this sandbox blocks Python execution without interactive approval (same as both Phase-A rounds), so `py_compile` and the smoke run are recorded as `not_run` in `verification.json` for the reviewer's environment. What I did verify mechanically: all input pins by `sha256sum`; all 99 census ids join to both phenotype families in the pinned manifest (149 members each, zero missing, zero `strokecase` spellings); the consumed freeze proposal's spellings equal the seven bound fields exactly with `center.documented=false`, so the bindings gate passes against the real artifact; every authority-gate literal appears in the approved contract; and a full read-back review of the final file.

Three deliberate judgment calls the cross-family reviewer should look at: (1) integer parsing accepts an exactly-integral decimal like `"7.0"`, tallied per variable as `decimal_normalized_values` — the 2026-08-12 normalize-deterministically lesson applied to values, visible never silent; (2) the frozen suppression rule is cell-level only, so published margins and mandated contrasts can arithmetically bound a suppressed cell — implemented to the letter, with that residual disclosed in the suppression log and summary rather than patched with unfrozen extra suppression; (3) the real staging event needs the system `7z` binary (the only non-stdlib dependency, documented in the README, refused loudly if absent).
