Phase-A probe for idea 047 is implemented and verified as far as this sandbox permits. The deliverables are in `probes/047/`: `run.py` (stdlib-only, one command, ~1,260 lines), an updated `README.md` (merged with the pre-existing plan README rather than discarding it — the old plan text remains at commit `c81d448`), `requirements.txt`, and `verification.json`.

**What the probe does, in contract order.** Authority gate (approval marker must bind the current contract blob — verified fresh: the working-tree contract hashes to the approved `b4887c05a21b…` — and the contract must contain all 17 frozen implementation literals *including* the pre-amendment sentinel, so this Phase-A code refuses to run under any future amended contract); split freeze before any input opens; SHA-256 identity gates on the three pinned tables plus byte-verification of `probes/023/run.py` against blob `0e9a40b4…` with the `coordinate_arrays()` lines 486–523 recorded verbatim in `resolved_config.json` (evidence, never executed); the support provenance-and-join gate (failure = exit 4, `SUPPORT_PROVENANCE_FAILURE`, a decision-grade stop that writes the gate record and no support-clause output); exact census cross-checks (fsum recomputation must equal `census_summary.json` and the contract constants bit-for-bit); the frozen support clause (absolute-contribution share beside support share with integer-exact voxel sums, tie-broken rank discrepancies asserting zero-sum, descriptive Spearman rho, the 79.29% share only under its reversal-accounting label); and the dictionary staging (md5-gated against `c8d806a0…`, held-copy or pinned-URL, the only network act), verbatim cell inventory, and the machine-proposed variable freeze with the pre-registered `CLINICAL_UNSUPPORTED` branch. A path guard makes `per_patient.csv` and all phenotype filenames untouchable.

**Verification — what ran and what couldn't.** This sandbox blocks all Python execution (a known, memory-documented restriction; `dangerouslyDisableSandbox` doesn't lift it). So I verified everything reachable with read-only tools, and it all passed: the three input pins and the take-13 blob reproduce; all 17 authority literals grep-verify in the contract; the gate facts verify independently (99 unique analyzed ids, empty symmetric difference against the contribution ids, all B_i pure positive integers spanning exactly the drafting fingerprint 1401–617540, the two bookkeeping rows byte-verbatim, signed_rank a 1..99 permutation); plus a four-pass desk review of the file as written to disk. What could **not** run here — and is recorded as `not_run` in `verification.json` — is `py_compile`, the `--smoke` run, and the real Phase-A execution. Those are your first actions, in order:

```bash
python3 -m py_compile probes/047/run.py
python3 probes/047/run.py --smoke --output-dir /tmp/probe-047-smoke   # <60 s, SMOKE_ONLY
python3 probes/047/run.py --output-dir /path/to/probe-047-phase-a     # real Phase A
```

The real run needs either network for the 12 kB dictionary fetch or `--dictionary-file` pointing at a held copy; the md5 gate arbitrates either way. Nothing is committed — the working tree holds the four probe files (plus the untracked stage prompt) for your review and the normal packaging path.
