Revision round 2 is complete: all three blocking findings from `probe_review.md` are fixed in `probes/047/run.py`, with `README.md` and `verification.json` updated to match. Nothing outside the three findings was touched — the contract, gates, arithmetic, and exit-code taxonomy are unchanged, and the approval remains fresh (working-tree contract still hashes to the approved blob `b4887c05a21b`).

**B1 — no analysis-time network.** The `urllib` fetch branch is deleted outright; a grep confirms no network-capable import remains anywhere in the file. `--dictionary-file` is now a required pre-staged input: a real run refuses with exit 2 if the flag is absent and exit 3 if the file is missing, and the held copy is verified against the same unchanged pins (record 16813698, 12,149 bytes, md5 `c8d806a0…`). `DICT_URL` survives only as a comment documenting where the held copy must come from.

**B2 — case-identified split freeze.** The run now loads and pin-verifies only the phenotype-blind `exclusions.csv` first, and `freeze_split` writes and hashes a manifest of the actual analyzed case IDs (plus the two excluded bookkeeping cases and the reserved-49 policy) before any byte of the outcome-derived contribution table or census summary is touched — including before their determinism hashing. After the contribution table opens, the support gate requires exact ID-set equality against the frozen manifest and records `split_manifest_sha256` and the equality verdict in `provenance_gate.json`; a mismatch feeds the pre-registered `SUPPORT_PROVENANCE_FAILURE` stop, so a count-preserving case substitution is now detectable from the bundle. Routing the check through the gate keeps the contract's registered stop semantics intact.

**B3 — complete determinism manifests on every registered terminal path.** A single `hash_targets` set (three frozen tables, the take-13 code evidence, and the pre-staged dictionary) feeds both manifests, so the dictionary is now hashed at start. A new `finalize_determinism_end` re-hashes everything, requires exact agreement with the start manifest, and writes and prints the end manifest — invoked on both the valid-completion path and the `SUPPORT_PROVENANCE_FAILURE` stop before returning. Loud pre-completion failures still produce no status and can't masquerade as decision-grade bundles.

One honest caveat, recorded in `verification.json`: this build sandbox blocks Python execution (same as round 1), so `py_compile` and the `--smoke` run could not be executed here. I desk-verified every edited region in its final on-disk form, confirmed all call-site signatures by grep, re-verified the three input pins by `sha256sum` and the approval blob by `git hash-object`, and listed the exact smoke/compile commands under `not_run` for the reviewer's environment, which ran round 1's smoke successfully. I've left the commit to the pipeline driver, matching the round-1 pattern.
