The revision is complete and confined to the reviewer's blocking finding. Here's a summary:

## What was fixed (probe_review.md, hard standard 5)

**The split is now predeclared and frozen before any outcome-derived table access.** `run.py` gains `FROZEN_ANALYZED_IDS_BY_RANK` — all 99 case IDs in signed-rank order, bound as authoring-time literals transcribed from the pinned contribution table (I re-verified the pin `aba52512…` first, and a mechanical diff of the tuple against the table's rank order came back identical, 99/99). `run()` now executes: authority → **split freeze from that declaration** (writing and hashing `split_manifest.csv`/`.json`) → only then hashing and opening the contribution and support tables → a new `verify_split_against_tables()` that requires exact rank→id and `in_head` agreement, failing with the invalidating input-identity exit code on any mismatch. The manifest states the stronger fact it now enforces (`created_before_any_outcome_derived_table_opened: true`), as the reviewer directed. The smoke leg got the same treatment: `SMOKE_IDS_BY_RANK` is declared from the fixture construction, and the freeze now happens before the synthetic tables even exist on disk, exercising the identical verification path.

## One disclosed correction inside the rewritten function

Binding the real IDs surfaced a latent defect in the round-1 `freeze_split`: its assert barred **both** `EXPECTED_BOOKKEEPING` IDs from the strata, but `sub-stroke0142` is one of the 99 analyzed cases (signed_rank 57) — its bookkeeping row records a duplicate archive *lesion member* (`excluded_archive_lesion`), not a case exclusion. The old assert would have aborted a valid real run with exit 12. The assert now bars only `record_type == excluded_case` (`sub-stroke0043`), matching the Phase-A provenance-gate semantics. This is not a scope expansion: it's inside the function the finding required rewriting, it's necessary for the repaired ordering to run on the real cohort, and it loosens nothing — the excluded case is still barred and the analyzed set is now pinned id-by-id. It's flagged prominently in `verification.json` for the reviewer.

## Verification

Checks run with available tools: pin re-verification of both tables, the tuple-vs-table order diff (identical), head-flag cross-check (the ten `in_head=True` rows are exactly ranks 1–10), a duplicate-literal scan (only the expected `sub-stroke0142` overlap), exclusions.csv record-type confirmation, and file hygiene (zero tabs, balanced triple-quotes, all edited hunks read back). `py_compile` and the smoke run are recorded as `not_run` in `verification.json` — this sandbox blocks Python execution, same as every prior round; the smoke remains designed for the reviewer's environment. `README.md` step ordering was updated to match, and no contract, gate, cap, statistic, or interface changed.
