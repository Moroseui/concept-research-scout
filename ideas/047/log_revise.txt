Revision complete. `ideas/047/idea_card.json` is rewritten to the round-3 finite-population design under the operator's ruling, and `ideas/047/revision.md` documents every material change, ending with `{"claim_retention": "narrowed"}`.

**What the ruling forced, and how the card now reads.** The operator's inspection settled the debate's open code question against the proposer: the cached q1+q4 counts are per-band quartile-cell counts, not total eligible support. The S_i proxy is therefore removed outright (not kept as a sensitivity arm), and the frozen burden variable is now B_i — the exact take-13 eligible-region voxel count, recomputed label-blind from the held maps — with a new hard reproducibility gate: the re-run region machinery must reproduce all 297 cached q1/q4 values exactly before any B_i is used, else the support clause stops for escalation.

**Everything the gauntlet invalidated is gone:** the fitted-residual "keystone" subgroup and endpoint (D2), all permutation machinery in the burden clause (round 2), the cancellation-confounded use of the 79.29% figure (round 3 — it survives only as separately labeled reversal accounting), the "clinically silent / clinically marked" dichotomy in title, question, pitch, and rationale (D4), the conditional "treatment fields if present" discretion (D5/D6, replaced by a bounded max-three predeclaration at the dictionary step), the admission-vs-24-hour NIHSS conflation (D1, now a documented forward-correction rule), and the "~50 kB, five minutes" cost fiction (D3, repriced as one archive staging event plus a CPU map pass; feasibility and prior_legwork each dropped 5→4).

**Two new verified facts strengthen the revision beyond what the debate had.** First, the newly load-bearing map branch is member-verified: joins of the md5-verified archive manifest against the 99 census ids give 99/99 coverage for tmax, cbf, cbv, mtt, and the rawdata NCCT (the one known source-defective member belongs to already-excluded sub-stroke0043); keystone prerequisite (3) was added and inspected, keeping INSPECTED_TRUE legitimate. Second, the honest numerator already exists in the ratified census: the signed-rank top ten and the absolute-contribution top ten are the same set (largest negative magnitude 0.0029142 < tenth positive 0.0029761), so `census_summary.json`'s absolute-mass head share **0.50635** is the valid contribution share for the frozen head. Round 3's predicted deflation is thus already on record — 50.64% in the honest currency versus the 79.29% accounting figure — and only the support share awaits computation.

**Negative outcomes preserved:** the support-share comparison is decisive either way at its 99-case scope (exact arithmetic, no sensitivity limit), and the clinical null is bounded rather than uninterpretable — the estimation table reports how much separation a 10-versus-89 comparison could have excluded.

Claim retention is **narrowed**: same cohort, same frozen head, same two clauses; what changed is packaging (binary verdicts → exact shares and estimation displays) and the burden term's operationalization — consistent with the rounds-2/3 identity checks and the 045/046 precedent that a card is not re-registered for becoming more modest.
