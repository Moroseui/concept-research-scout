Both artifacts are written and lint-clean (the only unquoted scalars are `track` and `human_approved`; every prose value is a single-line quoted string; the amendment sentinel literal appears exactly once, in its field). The stage is complete — no code was written, per the coding gate.

## What this stage produced

**`ideas/047/probe_contract.yaml`** (contract v1, draft, `human_approved: false`) and **`probes/047/README.md`**.

**The riskiest assumption, and the probe that tests it.** The contract puts the card's own reproducibility gate first: the frozen take-13 region machinery, re-run lesion-free on restaged maps, must reproduce all 297 cached `q1_voxels`/`q4_voxels` values in `probes/023/results/results_v2/per_patient.csv` by exact integer equality before any science is emitted. A mismatch stops the run as the pre-registered `REPRODUCIBILITY_GATE_FAILURE` (decision-grade stop, not a negative, not invalidating), with a per-row mismatch report plus an environment comparison — the 023 exit-5 lesson that version drift must not masquerade as a payload fact.

**New verified facts from code inspection** (this stage read the frozen implementation directly, `probes/023/run.py`, blob `0e9a40b4…`):
- The entire recomputation chain is provably lesion-free: `q1_voxels`/`q4_voxels` come from per-band `log(CBV)` quartile cuts (`np.quantile(z, [0.25, 0.75])`, counts of `z<=q1`/`z>=q3` at run.py:617-623) and never touch a lesion mask — so the gate runs fully outcome- and phenotype-blind, confirming what the card assumed.
- `B_i` is exactly the code's `eroded_region_voxels` (run.py:513), and `flow_band_labels` asserts the three bands partition that region — so `B_i` equals the sum of band member counts by construction, which the contract pins as an invariant.
- The frozen loader requires the NCCT, so the extraction set is 693 members (99 × {4 maps, NCCT, 2 phenotype CSVs}), enumerated verbatim in the probe README per the 023 amendment's extraction-set rule.

**Right-sizing structure** (per the 2026-09-01 ruling): one contract, two pre-registered phases instead of serial contracts. Phase A is phenotype-blind — dictionary-only variable-freeze proposal hashed *before* any map byte is read, one staging event, the gate, then the frozen support clause (the round-3 share pair, rank-discrepancy display, descriptive Spearman, the 79.29% figure only as labeled reversal accounting, with the census cross-check values 0.5063…/0.7929…/54-6-39 pinned). The genuinely new authority scope — the first-ever read of the clinical phenotype rows — sits behind the 023-precedent mechanical amendment (which binds the variable list and swaps `required_outputs` to the Phase-B interface, avoiding the F3 phase-conflation defect) plus a fresh human approval. Phase B emits the single aggregate 10-vs-89 estimation table under D3/D4 with small-cell suppression. Zero GPU minutes, one seed (Phase B exploratory relabelings only), one variant, no permutation machinery anywhere in the support clause.

**Two open questions are flagged for the human in the contract:** (1) whether the amendment-plus-fresh-approval gate satisfies the right-sizing carve-out for new phenotype exposure, or Phase B should be a separate contract; (2) whether the frozen exploratory null-reference range is the accepted clinical uncertainty currency (critique D4 allowed either that or CIs).

**One governance note for the reviewer/operator:** unlike ideas 045/046, idea 047 has no `feasibility.md` (the feasibility stage is not yet a default pipeline stage). The contract embeds the card's honest cost envelope and cites it, but the coding gate still requires a feasibility memo before probe *code* — worth deciding at review whether the revised card plus this contract satisfies that requirement or a memo should be ordered before approval.
