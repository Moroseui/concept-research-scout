The probe-plan round is complete. Since this idea's Phase A already executed and its interpretation is ratified, the correct output of this stage was not a fresh probe but the pre-registered amendment: I authored **contract v3** at `ideas/047/probe_contract.yaml` (new blob `dc586665d0be…`, `human_approved: false`) and updated `probes/047/README.md`. The mechanical `amend-contract` subcommand couldn't do this — it is hardcoded to 023's numeric placeholders — so the amendment is agent-authored per v2's own `amendment_protocol`, leaving the diff for your review.

What v3 does, exactly as v2 pre-registered:

- **Binds the frozen clinical variable list** from Phase A's machine-derived proposal (derived dictionary-only, before any clinical byte existed): MRS 3 months (ordinal, primary outcome), NIHSS 24h (continuous, the lineage-preserving idea-046 field — critique D1 resolves cleanly since the release documents it), NIHSS at admission (continuous, baseline context), Age, Sex, plus the two contextual-cap fields mTici postinterventional and Onset to door. Each carries exact release spelling, parse/missingness rule, and closed-menu statistics. Center is absent from the dictionary, so the mandatory-if-documented rule is discharged by recorded absence.
- **Records the consumed Phase-A artifacts**: bundle commit `6037f24`, import manifest sha, and the individual hashes of `proposed_variable_freeze.json` and `per_case_support.csv`, plus the ratified share values as frozen constants that Phase B cross-checks at startup and republishes only in the D4 joint display.
- **Swaps `required_outputs` to the Phase-B interface** (F3 lesson: the Phase-A bundle keeps validating under its own historical blob `b4887c05…`).
- **Gates the genuinely untested assumption**: the case-level column spellings were never verifiable under D3, so Phase B runs a schema/missingness census (the probe feasibility section 8 pre-registered) before any contrast, with a decision-grade stop `PHENOTYPE_SCHEMA_MISMATCH` if the rows can't support the minimum variable set. Field resolution is exact-normalized-match only — no fuzzy matching.

Four items are flagged in `open_questions_for_human` for your approval decision: (1) both exploratory uncertainty displays are retained verbatim per the interpretation's directive — strike the relabeling range before approval if you want deterministic-only; (2) the scale-type bindings (mRS/mTici ordinal, NIHSS/Age/Onset continuous, Sex binary) with recorded dictionary-grounded reasons; (3) the frozen mTICI level order 0<1<2a<2b<2c<3, with out-of-set values degrading to counted missingness; (4) the one addition beyond the literal binds list — the schema census output and its stop — which needs your explicit ratification.

The blob change stales the Phase-A approval by construction. Next acts are yours: review the contract diff, then `approve-probe` on the v3 blob; Phase-B probe-build follows under the fresh approval. The `ideas/047/registry.yaml` two-node DAG remains queued per the registry-per-probe rule. No code was written and no phenotype byte was staged or read.
