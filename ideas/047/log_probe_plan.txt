Both deliverables are written. Summary of this probe-plan round:

## What was produced

**`ideas/047/probe_contract.yaml` — contract v2** (replacing the v1 draft, which is preserved at commit `63459ec`), plus a matching rewrite of `probes/047/README.md`. Nothing is committed; both files await the driver/review flow, and `human_approved` remains `false`.

## Why v2, and what I verified before drafting

The feasibility memo's REVISE turned on one decisive claim, which I independently re-verified against the repository rather than trusting the memo:

- All four SHA-256 pins reproduce (`exclusions.csv`, `per_patient.csv`, `per_case_contributions.csv`, `census_summary.json`).
- `exclusions.csv` filtered to `record_type == analyzed_case` yields exactly 99 rows whose case ids are **set-identical** to the 99 census contribution ids (`comm -3` empty), each with a positive integer `eroded_region_voxels` (range 1401–617540); the only other rows are the two documented bookkeeping rows (sub-stroke0142, sub-stroke0043).
- `probes/023/run.py` in the tree byte-matches the pinned blob `0e9a40b4…`, and its `coordinate_arrays()` (lines 486–523) writes `eroded_region_voxels` as exactly the card's `B_i`: Tmax>6.0 deficit → six-neighbor erosion → border/midline exclusion → p98 CBV vessel exclusion → finiteness/positivity.

So the exact support quantity the study needs already exists as a ratified take-13 output, and recomputing it from maps would only add failure surfaces.

## Main changes v1 → v2

- **Support path:** Phase A becomes a hash-and-join audit of three pinned tables plus byte-verification of the frozen code (never executed). The 693-member extraction, ~3 GB map staging, region recomputation, and 297-row reproducibility gate are retired; a `SUPPORT_PROVENANCE_FAILURE` stop replaces the gate. `per_patient.csv` is dropped as an input entirely.
- **Blindness strengthened:** Phase B alone stages the 198 phenotype members — no phenotype byte exists on disk until the amendment's fresh approval.
- **Dictionary pins fixed at drafting** (md5 `c8d806a0…`, 12,149 bytes), never runtime-resolved; targeting rule updated with the memo's verified fields — 24-hour NIHSS as the lineage-preserving field, admission NIHSS as context, center mandatory-if-documented (memo items 3 and 5).
- **Uncertainty currency resolved (memo item 4):** deterministic leave-one-head-case-out ranges plus a relabeling range explicitly labeled "hypothetical exchangeability reference, not a confidence interval."
- **Novelty guard:** the EJR 2026 neighbor (DOI 10.1016/j.ejrad.2026.113200) is named in claim discipline.

## Flagged for the human (in the contract's open questions)

The operator's unblock ruling froze "the exact-map recomputation branch." V2 delivers the identical quantity via the ratified cached output instead of literal re-execution — approving the blob ratifies that substitution (and supersedes the card's recomputation wording); rejecting it reverts to the v1 recomputation design. The uncertainty-display choice and the one-contract-two-phase structure are the other two open rulings.
