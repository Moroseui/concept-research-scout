Keystone screen for idea 036 is complete: **PASS**, written to `ideas/036/keystone_screen.md` and committed (`5fb5818`, matching the pipeline's stage-commit pattern).

What I verified, first-hand where possible:

- **Matching channels (verified true).** Cloned the official challenge repo (`ezequieldlrosa/isles24`, commit `94b34863`). Its README and shipped sample cases show all four perfusion maps released as derivatives resampled into NCCT space (`space-ncct_{tmax,mtt,cbf,cbv}.nii.gz`), alongside raw NCCT/CTA/CTP and the follow-up lesion mask — so the five-channel matching and the atlas registration share one coordinate frame. License CC BY-NC. Cohort per arXiv 2408.10966: 150 public training cases, 98 hidden test (the card says 149 — one off, flagged for critique).
- **Atlas (verified true).** Cloned `Chin-Fu-Liu/Arterial_Atlas` (commit `fbeb3fe7`): MNI-space NIfTI label maps with 30 level-1 territories and a 4-territory level-2 rollup, CC BY-SA 4.0. One understatement in the card: the repo has label maps only, no registration template, and the atlas is MRI-derived — so atlas-to-NCCT is a cross-modal, template-mediated registration, not a direct one.
- **K1, the frozen model (verified true at document level).** Re-confirmed the idea-035 finding by cloning `KurtLabUW/ISLES2024`: public weights link, nnU-Net ResEnc-L inference command, and a `save_probabilities` flag in the vendored nnU-Net — continuous per-voxel output is available by construction. The material caveat: the challenge-winning score is Dice 0.285 ± 0.213, so the keystone's "non-trivial held-out performance" bar must be prespecified numerically before any training, or it gets set post hoc.
- **K2, few-mm registration (unverifiable at screen prices).** No document can settle it; it is exactly the stage-0 gate the card already prespecifies with a perturbation analysis. The defacing/skull status of released NCCT is stated nowhere I could fetch — recorded as a residual.

The mandatory wrong-keystone check found no substitution: the card's stated keystone and its actual load-bearing assumptions coincide, which is itself unusual for this program. The idea passes onward to critique with the performance-bar and cross-modal-registration caveats on the record.
