Cycle isles24-004 scouting is complete: `ideas/scout-isles24-004/scout_candidates.json` is written and validates as JSON (5 candidates, all rubric shape rules followed).

**What I verified before generating (charter hard rule), from primary sources this cycle:** per-case composition (NCCT, CTA, 4D CTP, four perfusion maps, follow-up DWI/ADC, infarct/LVO/Circle-of-Willis masks, clinical tables), 149 public training cases under CC BY-NC-SA on Zenodo, the four official metrics, and — verbatim from the challenge paper — that ground truth was drafted by the public ISLES'22 ensemble (DeepISLES) and corrected only "when needed," that scans are defaced with TotalSegmentator face masks, and that released CTP is motion-corrected and resampled at 1 frame/second. Sources: [Zenodo 16731717](https://zenodo.org/records/16731717), [arXiv 2408.10966 (HTML)](https://arxiv.org/html/2408.10966v1), [arXiv 2408.11142](https://arxiv.org/abs/2408.11142), [arXiv 2505.18424 (HTML)](https://arxiv.org/html/2505.18424v1), [isles24 repo](https://github.com/ezequieldlrosa/isles24), [DeepISLES repo](https://github.com/ezequieldlrosa/DeepISLES), [TotalSegmentator](https://github.com/wasserth/TotalSegmentator), [Arterial Atlas](https://github.com/Chin-Fu-Liu/Arterial_Atlas).

**The five developed candidates** (from ten recorded questions; all lead with a physician-legible X, per the cycle directive):

1. **c01, Mode A — "The ground truth remembers the algorithm that drafted it"** (priority 4.1, the only INSPECTED_TRUE keystone). The GT drafting algorithm is public and re-runnable on the released follow-up MRI, so the draft-vs-expert-correction field is computable per case; stage 1 measures how much of the benchmark's truth is uncorrected machine output, stage 2 tests whether trained models inherit the draft's conventions in expert-overridden voxels.
2. **c02, Mode B — territory-map prior** tested by regression discontinuity (borrowed from econometrics) at registered arterial-territory borders with placebo boundaries.
3. **c03, Mode B — systemic bolus transit as a cardiac-pump signal**, tested by shape-preserving time-axis re-indexing of the real 4D frames; explicitly a new question adjacent to paused idea-022, not a revival.
4. **c04, Mode C — conjugate gaze deviation (Prévost's sign)** — the obviously-wrong slot; lives or dies on a one-hour check of whether defacing spared the globes, which documentation genuinely does not settle.
5. **c05, Mode C — physiologic calcification as an age odometer**, a fairness-flavored shortcut probe that dissolves the objection that killed cycle 003's siphon-calcification question by making age-proxy use the claim itself.

Notable dispositions among the dropped five: the atrial-fibrillation question was *refuted by verification* (1 fps resampling is below Nyquist for cardiac frequencies), and the fiber-grain question was dropped as dying like idea-020. Zero revivals — nothing in the portfolio brief has a newly verified unblocking fact. The all-ISLES'24 conflict with the two-per-dataset quota is disclosed in `quota_note` with the charter controlling, matching cycle 003's precedent.
