The sandbox declined the shell validation, so I'll note that the file was composed to the mandated shape and rely on the merge-stage validator; the content itself is complete. Wide-scout stage is done.

## Wide-track scouting complete — `ideas/scout-isles24-004/wide_candidates.json` written

**Three developed candidates (all `track: wide`), five dropped with reasons, eight questions on record.**

**c06 — The scan remembers which hospital took it** (priority 3.5, `other:noise-residual-transplant`). Borrows PRNU camera fingerprinting from digital forensics: is the model reading the scanner-noise signature of Munich-vs-Zurich as a site prior? The use test is a within-case noise-residual transplant toward the other site's measured spectrum, with an equal-energy sham gate. Search found the exact bracket: CT-scanner forensic identification exists (Kharboutly et al., EUVIP 2014/EUSIPCO 2015) and site-confounding exists (Zech 2018), but nobody has use-tested the noise channel on a clinical model. Keystone (decodability on model-facing derivative data) is honestly NOT_INSPECTED and is itself the cheap stage-0 gate.

**c07 — The edge of the map** (priority 3.6, conditional-observational). Borrows swath-coverage analysis from remote sensing: CTP slabs are often narrower than the brain, but the ground truth comes from whole-brain MRI — so how much of the scored target did the primary sensor never see, and do models draw their lesion boundary at the sensor's edge? Stage 1 is model-free, CPU-only arithmetic on released maps and masks, including a "sensor-respecting performance ceiling" computed with the official metric code. I verified this stage that neither ISLES'24 paper states slab coverage — so the keystone is a genuine unresolved fact, checkable on three cases.

**c08 — The ground truth was drawn on a swollen brain** (priority 4.05, the strongest card). Keystone INSPECTED_TRUE with quotes I re-fetched today: follow-up MRI is acquired 2–9 days post-stroke (overlapping the documented 3–5-day edema peak) and registered to acute CT with **affine-only** transforms, which cannot represent mass-effect deformation. Borrows the geodesy datum-shift discipline: the primary endpoint is registration-free — the fraction of ground-truth "infarct" sitting on voxels that are unambiguously CSF on the acute scan, where infarct is physically impossible — then a test of whether trained models reproduce that halo. It composes with baseline c01 (the DeepISLES rerun doubles as the untrained comparator) and dodges the identifiability failure that killed idea-020 by never claiming any registration is "truth."

Dropped: clot-length treatment marginalization (homogenizes with backlog 002-c05), circadian proxies (X not image-computable), beam-hardening distrust prior (ungateable intervention, dies like idea-020), Weber-law ratio coding (duplicates idea-021), and rational-expectations site pricing (no X of its own; folded into c06's interpretation limits).

One caveat to flag: I could not run a JSON parse check because shell access was declined in this environment — the file was written strictly to the required shape (canonical `{"value": N, "why": ...}` scores, `keystone_evidence` present only on the one INSPECTED_TRUE card), but the merge validator should confirm.
