**Verdict: APPROVE.** The round-2 review is written to `ideas/046/probe_review.md`; round 1 remains preserved in git (f3fb2df).

What I verified:

**The round-1 blocking finding (B1) is closed exactly as specified.** The new `compare_v1_lineage_guards` function (probes/046/run.py:357–401) freezes the v1 audit values as in-code constants with provenance — verified transcription-exact against both the contract's `baselines[0]` and the actual v1 `definition_audit.json` (residual 6.938893903907228e-18, sign counts 54/6/39, ties 5/5). It runs in real mode only, after the guards are recomputed; any disagreement fails loudly with a message naming the lineage class before the status is assigned or any results artifact is written, so `CENSUS_COMPLETE` is now unreachable on a lineage divergence. The comparison verdict is persisted in `census_summary.json`, smoke skips it with a visible `SKIPPED_SMOKE_SYNTHETIC_DATA` record, and the tie-count space ambiguity round 1 flagged is resolved the stronger way: ties are re-counted in delta space, before division by 99, with the space named in the persisted record.

**Spurious-fire risk excluded.** Since the guard demands bit-for-bit residual equality, I diffed v2's `measure()` against the executed v1 code (git `3b989a4`): identical formulas under `math.fsum`, which is order-independent, so the recomputed residual is bit-identical to the frozen constant by construction. I also confirmed the two tie-counting mechanisms (v1's hex encodings vs v2's float-equality sets) can only diverge at signed zero, a configuration v1's own recorded counts rule out for this frozen input — noted as a non-blocking finding.

**Everything else holds.** Identity anchors re-verified (contract blob `942e5307…` matches the approval marker; input CSV hashes to the pinned `1d01551c…`; v1 bundle untouched); the smoke was re-run after the revision and passed with byte determinism; the round-1→round-2 diff contains no scope expansion; the stale v1 comment was fixed; and the remaining round-1 nits (exit-code taxonomy, unlabeled redundant audit fields, etc.) carry forward as non-blocking.

The probe is now cleared for the operator's execution sequence under the standing approval of blob `942e530737c9`. I have not committed the review file — the pipeline driver owns that commit.
