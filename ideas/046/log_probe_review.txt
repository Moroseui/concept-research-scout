Round-2 review complete — verdict **APPROVE**. The review is written to `ideas/046/probe_review.md` (round 1 preserved in git at 8637cab, per repository convention).

What I found:

**Both round-1 blockers are resolved exactly as directed, with nothing else in the diff.** The round-1 → round-2 change to `run.py` is 28 lines confined to `measure()`, `summarize_definitions()`, and their call sites:

- **B1 (residuals not recorded):** the code now computes both the stable (`math.fsum`) and ordinary (naive `sum`) residuals, and persists both numeric values in `definition_audit.json` — the ordinary one explicitly named `ordinary_summation_residual_diagnostic_only`, with the tolerance gate still consuming only the stable residual. The contract's primary metric value now appears in a required output instead of just a boolean.
- **B2 (hardcoded tie verdict):** both contract-frozen orderings are now actually constructed as sort-key lists, uniqueness is measured per ordering, and the `deterministic_secondary_case_id_rule_defined` boolean is derived from that measurement rather than hardcoded `True`. I verified the case-to-delta pairing is correct (`measure()` builds deltas over `sorted(cases)` and returns the same ordering, so the `zip` aligns), and that both lists stay in memory — no identifier or rank reaches any artifact.

**Identity anchors re-verified this round, not inherited:** the contract blob (`3996009b...`) matches both `HUMAN_APPROVED_PROBE` and the current contract bytes; the frozen input's SHA-256 matches the pin and `run.py:47`; the input is still 298 lines.

**One new non-blocking note:** the `assert deterministic_ordering` would route a hypothetical tie-rule failure to exit 12 rather than the negative pattern — but that branch is mathematically unreachable (the cohort gate guarantees unique case IDs, so the tiebroken tuples are always distinct), and the behavior is fail-loud, never fail-silent. Round-1's optional suggestions were correctly left unadopted under the no-scope-expansion instruction; they remain on the record for the future census contract.

The probe now records what it measures and measures what it certifies. Per the contract, a passing run authorizes only the drafting of a separate scientific census contract — that next contract must encode the D3 read-restriction and D4 joint-display rules from the feasibility memo.
