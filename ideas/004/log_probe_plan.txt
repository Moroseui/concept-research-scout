Conformance scan is clean: "repeatability" appears only inside the clause prohibiting the term, every "margin" occurrence is a negation or a citation of the pin-2 amendment, and the only "material-" hits are the verb "materialize" for the pair list. The four placeholder occurrences are the two hash/count fields, the budget cap that mirrors them, and the invalidating-failure clause that polices them.

Done. I drafted contract v2 for the 425-pair floor study, governed by the ratified requirements file rather than the default small-probe limits.

**What was written:**

- `ideas/004/probe_contract.yaml` — contract v2 (`contract_version: 2`), superseding the executed v1 load-probe contract (preserved in git history). It encodes all eleven requirements: the frozen 425-pair scope with exact stratum counts and the manifest specification (R1); canonical sharper-minus-softer sign directions fixed per stratum before any score is seen (R2); label-free tier 1 with per-head × per-stratum paired differences on both scales, |Δ| quantiles, patient-cluster bootstrap with fixed seeds, and no cross-head averaging (R3); descriptive tier 2 ΔAUROC with the ≥10-positive/≥10-negative sparse-label eligibility rule, a mandatory excluded-cell table, and zero threshold language, contextualized only by the ratified memo cited by blob hash with all five inherited caveats (R4); the r6 environment closure and single enumerated buffer-key exception (R5); r5 kernel normalization and shortfall audits (R6); 17 chunks × 25 pairs with same-session pairing, per-chunk manifests and environment records, and the v1 pair as a session-start anchor with a preregistered 1.0e-4 cross-session probability tolerance against the pinned v1 reference scores (R7); budgets capped in volumes and sessions, not GPU minutes (R8); the four-pair preregistered spot-check subset and sixteen invalidating failures kept strictly separate from negative outcomes (R9); the full results-bundle layout (R10); and vendor-scoped claim discipline with the one-pair exposure argument restated (R11).

- `probes/004/README.md` — rewritten to describe the v2 plan and approval flow while keeping an accurate record of the v1 artifacts still in that directory (v1 `run.py` must not be used for the floor study). No code was implemented.

**One structural decision, flagged for the operator rather than silently assumed:** R1 requires the manifest's SHA-256 to be recorded *in* the contract, but the manifest can only be produced by code, and code requires an approved contract. The contract therefore defines a two-phase, two-approval flow: approval #1 authorizes only a metadata-only manifest freeze; recording the resulting hash and unique-volume count amends the contract, which the new hash-bound `approve-probe` gate automatically treats as stale; approval #2 against the amended blob authorizes bulk execution. Any bulk activity while placeholders remain is an invalidating failure. This is written into the contract as an open question for confirmation at first approval.

**A second flagged question:** the anchor pair is one of the 425, and its scores were already exposed by v1. Following the ratified memo's own terms, the contract excludes its deltas from all confirmatory statistics (236 counted Br40f|Br60f pairs); if the operator prefers retention with a footnote, that is a pre-approval amendment.

The existing `HUMAN_APPROVED_PROBE` marker is bound to the v1 blob, so the gate correctly blocks probe-code until the operator approves this contract. `human_approved` is set to `false` accordingly.
