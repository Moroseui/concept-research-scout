The exit-7 revision is implemented in `probes/004/run.py`, strictly within the ledger's three-requirement scope, and statically verified. This sandbox still cannot execute Python (script runs are approval-gated, `dangerouslyDisableSandbox` included), so `python3 run.py --smoke` remains the human's first step — `verification.json` records exactly what was and wasn't checkable.

**What changed, mapped to the 2026-08-12 revision spec:**

1. **Kernel normalization** — new `normalize_kernel()` (run.py:379): a value parsing as a Python list literal via `ast.literal_eval` yields element 0 (`"['Br40f', '3']"` → `Br40f`); anything else, including unparsable strings, yields the stripped raw string. The A/B membership predicate in `select_pair` now compares normalized values. Geometry match columns still compare raw row-vs-row strings, untouched per the spec.
2. **Diagnosability** — `select_pair` now always builds a top-10 kernel-value tally (raw, normalized, count, up to 3 example VolumeNames) and per-filter drop counts (missing contrast member / duplicate members / geometry mismatch, per column). Both modes write `selection_audit.json` unconditionally *before* the count gate, so an exit-7 stop always leaves the audit on disk; a shortfall against the frozen 237 additionally dumps the same diagnostics into `run_log.txt` via `report_selection_shortfall` before `fail(7)`.
3. **Manifest** — `input_manifest.csv` now records `kernel_normalized` and `kernel_raw` from each selected volume's own metadata row instead of hardcoded constants.

**Smoke coverage of the fix:** the synthetic clean pair now stores its kernels in the frozen release's stringified-list format while the decoys stay plain strings, so both normalizer branches run through real selection, not just unit checks; smoke also asserts the exact planted drop counts (`{NumberofSlices: 1, ZSpacing: 1}`, 2 contrast drops) and the list-format tally entry, failing exit 12 on any deviation. I hand-traced this arithmetic and it is consistent.

**Two items for the reviewer's attention** (this edit returns for re-review per the round-3 approval conditions anyway):
- I added `selection_audit.json` to the contract's `required_outputs` — bookkeeping sync to your own committed revision spec (0ee49c6), same pattern as the accepted `human_approved` sync. No caps, executions, seeds, or interpretation language changed anywhere.
- `outputs_smoke/` still holds the 2026-08-11 artifacts of the previous revision; the smoke rerun will overwrite them (the known rerun-clobbering behavior, carried finding 4, unchanged per scope).

Modified files: `probes/004/run.py`, `probes/004/README.md`, `probes/004/verification.json`, `ideas/004/probe_contract.yaml`. Nothing is committed — the working tree is ready for your review, smoke run, and commit.
