**The probe_code stage for idea 004 (contract v2) is complete.** The previous build attempt had died on the old 40-turn ceiling, not on any defect: the partial commit already contained an essentially finished driver. This session verified that work end to end, fixed two stale documentation details, and wrote the fresh `verification.json` the stage requires. No expensive compute was launched and no bulk activity of any kind occurred.

**What was verified (statically — this sandbox blocks Python execution, so the review is a full line-by-line read plus mechanical text checks):**

- **run.py (3,049 lines) is complete and contract-faithful.** It implements the two-phase flow exactly: Phase M is metadata-only (three pinned tables, manifest regeneration, hard gate on 237/126/58/4, hash + unique-volume count printed for the operator, `selection_audit.json` always written), and Phase B is mechanically refused while any `TO_BE_RECORDED_AT_MANIFEST_FREEZE` placeholder remains or the approval marker's blob doesn't match the amended contract. The current marker binds to blob `a84b617c…`, which equals the contract's current blob — so phase-1 approval is valid and covers Phase M only, as intended.
- **Every provenance pin cross-checks.** Checkpoint SHA-256, code commit, HF revision, all three table hashes, both anchor-volume hashes, the v1 reference git blob (`probes/004/results/per_sample.csv` hashes to exactly the pinned `ea1cdd3f…`), and both preregistered bootstrap seeds appear identically in the contract and the driver; the gate also re-checks all of this at runtime.
- **The anchor comparison is numerically sound.** The v2 scorer, model construction, and staging layout are byte-identical to the executed v1 driver, so the cross-session 1.0e-4 tolerance check against the v1 scores compares like with like.
- **The smoke mode's planted arithmetic is right.** I re-derived by hand the stratum counts (5/2/1/1), the shared-volume unique count (17, proving unique volumes ≠ 2×pairs), the chunk plan (4+4+1), the spot-check subset, and the tier-2 eligible/excluded cell counts (1 and 71) — all agree with the assertions.
- **Stage requirements all present:** one command per phase, fixed seeds, deterministic byte-stable manifest, split guards with train/test decoys asserted refused, per-sample outputs with float64 hex for bit-identity, resolved config + per-chunk environment capture, no tunable variants, and exit codes 0/2–13 each mapped to a contract failure class.

**Small fixes made:** `requirements.txt` referenced a "PHASE 4" that only existed in the v1 driver (now points at `load_released_model`), and the README implied smoke skips "the gate" — it skips only the HuggingFace dataset gate; the hash-bound human-approval gate does run in smoke, and the wording now says so.

**One caveat, stated plainly in `verification.json`:** Python cannot execute in this sandbox, so no compile or smoke run happened here. The smoke run (`python probes/004/run.py --smoke`) is the first command to execute on the real machine, before Phase M — it exercises the approval gate, both Phase-B refusal branches, and the full analysis path in seconds. After Phase M prints `pair_manifest_sha256` and `unique_volume_count`, recording them amends the contract, stales this approval by construction, and Phase B waits on your fresh phase-2 approval.
