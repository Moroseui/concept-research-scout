Revision r5 is implemented, exactly within the decision ledger's scope. The exit-5 load failure was an environment-alignment problem — transformers 4.31.0 stopped expecting the persistent BERT `position_ids` buffer that the <=4.30.x-saved checkpoint carries — so the fix is a pin change, not a code change.

What changed:

- **`probes/004/requirements.txt`** — `transformers==4.38.2` replaced with `transformers==4.30.1` (the released repo's own commented pin in `transformer_maskgit/setup.py` line 17), plus `tokenizers==0.13.3` added since 4.30.1 requires tokenizers<0.14 and 0.13.3 is the last 0.13.x release. A comment above the pins records why, citing the exit-5 root cause. The retained `huggingface-hub==0.22.2` sits inside 4.30.1's declared `>=0.14.1,<1.0` range.
- **`probes/004/README.md`** — a short "Revision 2026-08-12 r5" paragraph documenting the root cause, the pin, and that the exit-5 classification stays provisional until a run under the released pin.
- **`probes/004/verification.json`** — rewritten for r5: scope check (run.py untouched, still at the round-4-approved revision `759b664`), pin consistency checks, and honest caveats.

What I verified without expensive compute:

- `git status`/`git log` confirm the diff footprint is exactly requirements.txt + README.md and that `run.py`'s newest commit is `759b664`, the revision the round-4 review approved and independently smoke-ran. Since `--smoke` is stdlib-only and never imports transformers, r5 cannot alter smoke behavior, so no re-run was needed (and Python execution is blocked in this sandbox anyway).
- The Colab notebook installs via `pip install -r probes/004/requirements.txt`, so the pin propagates with no notebook edit; `run.py` has no hardcoded transformers version assertion and captures the installed environment dynamically into `environment.txt`.

Two caveats recorded in verification.json: the full pip dependency solve hasn't been executed locally (the Colab install is the first real resolver run — a conflict there would be an environment finding, not a contract failure), and per the ledger's process note the prior exit-5 remains provisionally environment-class until the real probe runs under this pin. Changes are staged in the working tree, uncommitted, ready for the human's next real-probe attempt in a fresh persistent output directory.
