No bad escapes in the JSON, and the approval marker (`a84b617c...`) still matches the contract blob — the contract file was untouched, so the phase-1 approval binding is preserved.

The revision is complete. I have not committed, per standing practice of leaving stage commits to the operator.

## What was fixed

The single blocking finding from `probe_review.md`: the 30-session cap (contract R8) was enforced by counting distinct session ids in `anchor/anchor_log.csv`, which is only written after the anchor protocol succeeds — so any Phase B session that died during environment capture, metadata or checkpoint access, checkpoint loading, or anchor work never consumed the budget and could be retried indefinitely.

`probes/004/run.py` now has a durable session-attempt registry:

- **`register_session_attempt`** (new helper next to the other persistence helpers) appends a row to `<BUNDLE_DIR>/sessions/session_attempts.csv` with flush + fsync, so the record survives a crash later in the session. The cap comparison runs *before* the append: attempt 31 is refused with the existing exit 9 (budget stop) before any work begins, and the refused attempt is not itself registered.
- **`run_phase_b`** calls it as its first act — before `capture_environment`, table/checkpoint downloads, model load, and the anchor protocol. Every failure path the reviewer listed now falls after registration and consumes the cap. The only thing preceding registration is `phase0_gate`, which I verified is purely local contract/marker file reads — a gate-refused invocation performs no Phase B activity, so not counting it is consistent with the contract.
- **`summary.json`'s `sessions_used`** now derives from the same durable record, fixing the companion accounting error. `anchor_log.csv` reverts to a pure anchor-diagnostic role.

Supporting changes, all within the finding's scope: a smoke self-test that registers 30 mock attempts in an isolated subdirectory, asserts the durable count increments, and asserts attempt 31 is refused with exit 9 *without* being registered (the deliberate `FAIL` line is announced in the log so a passing smoke run reads correctly); one sentence in the Phase B section comment; and a README operator note warning that failed sessions count and the registry file must not be edited. No scientific endpoint, statistic, cap value, pin, or analysis line was touched, and the contract file was not modified.

## Verification

This sandbox blocks Python execution (confirmed again this session), so as in the prior round verification is static: a full read of every changed hunk, grep confirmation that no enforcement or reporting path still reads `anchor_log.csv`, docstring balance (104 triple-quotes, even), no TODO markers, complete file ending, and the marker-blob match above. `verification.json` is rewritten with a `revision_round` section recording the finding, the fix, the gate-boundary rationale, and the one caveat worth knowing: fsync durability on a Drive mount is best-effort — the registry also closes the file, which is the same persistence path every other bundle artifact relies on. The reviewer's environment ran `py_compile` and smoke successfully on the pre-fix code; the smoke run on the real machine remains the mandatory first command and now also covers the registry logic. No expensive compute was launched.
