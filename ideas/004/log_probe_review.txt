Round-2 review complete — verdict **REVISE** again, written to `ideas/004/probe_review.md` (left uncommitted).

**What I verified:** I re-fetched the official CT-CLIP sources and checked each round-1 blocker against the revised code. **B1 is genuinely fixed** — the call `model(False, text_tokens, video, device=device)` now matches the released call site exactly (the omitted `return_latents=True` is set unconditionally inside the released `forward`, so it's a no-op difference), and the load path (`strict=True`, `weights_only=False`, `map_location` then `.cuda()`) is equivalent to the released `.load()`. **B5 (`--output-dir`) is fixed and smoke-tested.** B3's ImportError routing is in place, and the scope discipline held — the diff touches only the blockers.

**Why it still fails review — two new blocking findings:**

1. **R1: Real mode cannot reach the released imports on a fresh session.** The B2/B4 remedy installs the two released packages with `pip install --no-deps -e` from inside the running probe. Three independent defects, all verified against the fetched sources: (a) `--no-deps` skips import-time dependencies the packages need — `transformer_maskgit`'s `__init__` chain pulls `ema_pytorch`, `accelerate`, `einops`, `beartype`, `torchvision`, and `vector-quantize-pytorch==1.1.2` (an API-sensitive pin the released `setup.py` carries and `--no-deps` discards), while `ct_lipro_inference` → `eval.py` needs `h5py`/`matplotlib`/`seaborn`/`scipy` — and the new `requirements.txt` pins none of them; (b) an editable install performed mid-process isn't importable until the next interpreter start (`.pth` files are processed by `site` at startup), so even a perfectly provisioned first run deterministically dies at exit 11; (c) `torch==2.5.1` is pinned without a matched `torchvision`, and on Colab the resulting ABI mismatch raises `RuntimeError` — which routes to a **false exit-5 contract-invalidating failure**, the one outcome this probe must never produce.

2. **R2: The B3 fix only catches ImportError.** A CPU-only Colab runtime (phase 1 logs "gpu: NONE VISIBLE" and proceeds; `model.cuda()` then raises `RuntimeError`) and a failed tokenizer/BERT-weights download inside the PHASE 4 try-block both still land in `fail(5, "released checkpoint/code failed to load unchanged")` — environment problems recorded as the contract result that feeds the ADVANCE/kill decision.

Both fixes are mechanical (pre-provision the environment before the interpreter starts, or use the round-1 `sys.path` alternative; add a phase-1 GPU check and route non-ImportError environment failures away from exits 3–9). The review also carries forward the round-1 non-blocking items the coder deliberately deferred (secondary metrics not in `summary.json`, deterministic-algorithms error routing, ungated pair-count cross-check, rerun clobbering, and the `human_approved: false` flag awaiting the human's commit).

One unrelated note: the session reports that the claude.ai Gmail, Google Calendar, and Google Drive connectors need re-authorization via your claude.ai connector settings before those tools can be used here.
