Review complete — **APPROVE**, written to `ideas/023/probe_review.md` (round 6, first review of the post-take-11 revision).

**What I verified.** The pre-revision run.py at the parent commit is byte-identical to the round-5-approved `9978bfed…`, so the diff I reviewed is the complete delta from the last approved artifacts. Both halves of the take-11 dual directive are implemented exactly:

- **(a) Source-corrupt routing:** a one-member constant names exactly the verified defective sub-stroke0043 CBF file; every required `.nii.gz` stream is now read fully before measurement; that one member excludes its case with reason `source_corrupt_member`, its exact path in both `schema_census.csv` and `exclusions.csv`, the map pass continues, and `summary.json` surfaces the excluded count. Any *other* unreadable member exits 6 loudly, and a *missing* member still exits 5 — the exclusion cannot silently widen.
- **(b) Finiteness tightening:** both (and only — grep-verified) stratum-mask sites now require finite rCBF and rCBV, identically in the label-blind quartile pass and the outcome pass, closing the take-10 infinite-ratio edge.

**Contract amendment judged necessary, and correctly minimal.** Exactly two lines changed (`required_inputs` plus the population-failure clause). I concur it was required — the old clause made a missing map invalidating and would have collided with the exclusion. Consequence: the standing approval (bound to `468974a7…`) is stale by design; the runtime gate refuses every phase until the human binds a fresh approval to the new blob `2963f66b…`, and the builder's verification attests the executed exit-2 refusal.

**No blocking findings.** Four new non-blocking ones, the most substantive being R8: voxels removed by the finiteness rule aren't counted in `exclusions.csv`, and since the paired-run comparison was superseded, that count is the only cheap way for the interpret stage to gauge the tightening's materiality (it remains recoverable from the npz checkpoints). Also noted: an unreachable-but-defensive outcome-pass handler, the OSError-attribution boundary on the one preregistered file, a cosmetic `/100` denominator, and the thrice-carried README duplicate-handling sentence (R1), which was again left stale under "change nothing else" despite the README being touched four lines below it.

The human gate is now the controlling step: review the two-line contract diff and, if acceptable, bind a fresh `HUMAN_APPROVED_PROBE` marker to blob `2963f66b…` — take 12 can then run under the launcher's existing blob-scoped output directory.
