The memory already covers WebFetch as the curl workaround (line 21), so no update is needed. The review stage is complete.

**Verdict: REVISE — three blocking findings, written to `ideas/023/probe_review.md`** (the round-1 review remains in git at f5cdd6a).

The round-2 code is a genuine improvement: I verified that all nine round-1 blockers were properly fixed — exact-suffix file discovery, the full mirror/brain-mask QC gate with `mirror_qc.csv`, the 5×5×3 neighborhood-median relative measures, census-wide identity centering in the label-blind pass, the unit gate, immutable-record provenance with Zenodo checksum comparison, all 16 required outputs, the frozen resampling rule, and Phase-S hash verification at Phase C entry. Phase S remains contract-faithful and could run today, and the contract blob binding to the human approval marker checks out.

But the Phase C path — the artifact the amendment cycle will freeze — still cannot complete a real run, so I blocked on:

1. **B1 — lesion discovery is dead on arrival, again.** I fetched the official ISLES'24 repository's data tree to verify the actual filename: the mask is `sub-strokecase0009_ses-0001_lesion-msk.nii.gz`, with a hyphen (`_lesion-msk.nii.gz`). The code's suffix list has only `_msk.nii.gz`/`_lesion.nii.gz` variants, so `find_one` finds zero files and exits 5 on the first outcome-pass case — after the entire multi-hour label-blind pass has already run. This is the second consecutive round where Phase C fails on the release's real schema.

2. **B2 — the compute plan doesn't fit Colab.** The mirrored-neighborhood median uses `scipy.ndimage.generic_filter` with a per-voxel Python callback, executed four times per case because pass two recomputes everything pass one already derived; that's roughly 20–30 CPU-hours at thick-slice dimensions and days at thin-slice, with no checkpoint/resume (a disconnect loses everything) and 100 full-volume brain masks held in memory simultaneously (1–8 GB, OOM risk).

3. **B3 — three preregistered secondary metrics are never computed:** the median-d patient-bootstrap 95% CI, the label-blind native-support distribution of z, and the identity-residual distribution. Only point summaries are emitted, which trips the contract's own Output-failure class.

Nine non-blocking findings are also recorded — notably that the whole-volume finiteness gate may refuse real vendor maps with NaN backgrounds (an intra-contract ambiguity the operator should resolve at amendment time, before spending the 99 GB download), and that the feasibility memo's claim that derivatives contain a "registered NCCT" is wrong per the official tree — the NCCT lives in rawdata only, so the extraction instructions need to say so.
