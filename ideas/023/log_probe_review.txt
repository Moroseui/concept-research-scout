**Verdict: APPROVE.** The round-2 revision fixes both round-8 blocking findings exactly as prescribed and changes nothing else; `ideas/023/probe_review.md` (round 9) is written with the full audit and an APPROVE verdict block.

What I verified:

- **The delta is fully accounted for.** The `9e843c7..0e840d0` probe diff is one 13-line hunk inside `tissue_audit`, an 18-line README touch, and the verification.json refresh. Hash lineage anchors cleanly: approved run.py `80af2c87…` at both the approval commit and the revision parent, round-8's `ecb2e0f9…`, now `ff237ea7…` — all matching verification.json's recomputed hashes.
- **B1 (empty-band crash) is correctly resolved.** The audit now branches on `z.size`: non-empty bands keep the finite-cut assert; empty bands assert NaN cuts (which is exactly what the map pass produces for them, run.py:804-810) and emit both style rows with zero members and blank statistics. I traced all three six-row invariants (run.py:599, 784, 846), confirmed empty bands genuinely occur on valid input (one- and two-voxel regions leave bands empty by the band arithmetic), and confirmed the guard restores symmetry with `patient_measure`'s existing `z.size < 4` tolerance — the frozen support gates, not a crash, now decide small-deficit patients.
- **B2 (README extraction set) is resolved.** The extraction declaration gains `{raw_data,rawdata}/**/*_ncct.nii.gz` and the "not used" sentence now names the NCCT as a label-blind audit-only input, matching run.py:454. `find_one` is spelling-agnostic and fail-loud, and the NCCT goes through the same full-stream gzip verification as every required member.
- **Carried R12 was fixed in the same touch**, and the new duplicate-lesion sentence matches the actual selection logic clause for clause (canonical ses-02 preferred, identical size+CRC required for lexicographic fallback, otherwise exit 5, excluded members surfaced in both CSVs).
- **Governance intact.** The contract is still blob `0e223c82…`, the approval marker binds that blob, the estimator/gates/claim language are byte-identical to the audited baseline, and the checkpoint identity keys on the run.py hash so neither superseded revision's cache can be consumed.

Non-blocking carries are restated for the next steps: N1 (contract-text NCCT harmonization) is queued for the operator at the mechanical Phase-S amendment, and R15 means the take-13 launcher must be regenerated at package-colab from the now-corrected README so the NCCT actually gets extracted. Per the 2026-08-28 sequence, the path forward is: mechanical amend-contract from the mirror-free Phase-S bundle → fresh approval on the new blob → package take 13.
