The probe review is complete — verdict **APPROVE, no blocking findings**. `ideas/023/probe_review.md` now carries round 7, the first review of the mirror-free redesign (rounds 1–6 remain in git).

What I verified, in brief:

**The directive is implemented exactly.** The contract amendment (blob `2963f66b…` → `0e223c82…`) does precisely what the 2026-08-27 decision ordered: matched flow becomes three within-patient CBF percentile bands, Q1-vs-Q4 becomes each patient's own label-blind CBV quartiles, and every mirror clause, output, and failure mode is stripped. In run.py, `mirror_qc`, `neighborhood_median`, the reflection machinery, rCBF/rCBV ratios, and NCCT loading are all gone; the new `flow_band_labels` uses stable mergesort ranking with integer arithmetic so band boundaries and ties are exactly the contract's rule. The region rule (Tmax>6s, erosion, p98 vessel exclusion), identity-residual gate at 0.10, support floors, source-corrupt policy, provenance chain, and resume machinery are unchanged in behavior. One operationalization change I examined and endorsed: the two-voxel midline exclusion now uses the array midline instead of the (deleted) mirror-estimated plane — the contract states this inline, and it introduces no reference anatomy.

**Discipline held.** Phase-S placeholders are restored, forcing recalibration under the new strata before Phase C is approvable; per-patient quartile cuts are computed and checkpointed in the outcome-blind pass before any lesion opens; the split is hashed before label access; and the direction-aware three-band conjunction, seed, and bootstrap are identical between the simulation and the census. A long-carried finding (F1, the quartile-mask asymmetry) is structurally resolved because both passes now consume the same cached region/bands.

**Findings:** six non-blocking. The one I escalated: the README's duplicate-lesion sentence still claims non-identical duplicates stop the run, but the code's canonical ses-2 rule resolves the payload's real 150th lesion row without stopping — the paragraph around it was rewritten this round, so the "change nothing else" defense has expired; it should be fixed before approval is bound, since the human reads that file at the gate. The others: leftover dead code from the mirror removal, uncounted positivity-excluded voxels (successor to R8), the stale take-12 launcher notebook (fail-closed, but package-colab must regenerate it including a Phase-S path), a degenerate-quartile overlap note, and two cosmetics.

**State now:** the approval marker still binds the old blob, so both phases refuse with exit 2 — stale by design. Next step is yours: review the contract amendment diff and, if acceptable, bind a fresh `HUMAN_APPROVED_PROBE` to blob `0e223c82f9eb879652a549df9bf857c155ef61db`, which authorizes the Phase-S recalibration run.
