The review is complete and written to `ideas/023/probe_review.md`. **Verdict: REVISE**, with two blocking findings.

**What I reviewed:** the round-1 probe-code commit (`4403d4c`) implementing the 2026-08-28 HU tissue-audit directive. The complete delta from the approved artifacts is 63 lines in `run.py` (plus verification.json) — I confirmed the run.py at both the approval commit and this revision's parent is the round-7-approved `80af2c87…`, the contract blob is byte-identical (`0e223c82…`, so the standing approval validly holds through verify, exactly as the directive intends), and requirements.txt/README.md are untouched.

**The audit itself is faithful to the directive:** NCCT is read label-blind in pass one only, resampled onto the common grid by the existing frozen linear rule; median/IQR plus voxel accounting per case × flow band × CBV-quartile style group; rows go into the per-case checkpoint and `bin_tissue_audit.csv`, written *before* the identity gate so the evidence survives an invalidating stop; no estimator, gate, seed, or contract change; no HU value feeds any pass/fail rule; Phase C stays locked behind the restored Phase-S placeholders.

**But two defects meet the blocking bar:**

1. **B1 — empty-band crash.** The map pass sets a band's quartile cuts to NaN when the band is empty (run.py:796-799), and `tissue_audit` asserts those cuts are finite unconditionally (run.py:567). A census patient whose eroded deficit region shrinks to ≤2 voxels — a plausible mild-stroke case never yet censused under the mirror-free region rule — leaves a band empty and kills the take as an unclassified exit-13 harness fault. The pre-existing code deliberately tolerates exactly this input (`patient_measure` skips `z.size < 4` cells *before* its own assert), so the audit silently narrows the design's tolerance — a de facto new gate, and the 13th potential instance of the wasted-take failure class.

2. **B2 — stale README extraction set.** README lines 24-26 and 37 still say "Rawdata NCCT … not used," but run.py:454 now hard-requires the NCCT per case (exit 5 if absent). The take-13 launcher is regenerated from that declaration — the exact dependency mechanism built after Phase-C attempt 1 died to an omission of this kind — so following it guarantees a dead take at the first map-pass case.

Both fixes are agent-resolvable in one probe-build touch with no contract change. I also queued **N1** for the operator: the approval-bound contract text still declares NCCT outside the probe, so the upcoming mechanical Phase-S amendment (which changes the blob anyway) is the natural place to name the NCCT as an audit-only input and decide whether `bin_tissue_audit.csv` joins `required_outputs`. Non-blocking notes cover the latent missing/corrupt-NCCT failure classes (all 149 NCCT members are known present and sweep-clean), the R16 overlap semantics inherited by the audit rows, and the R12 README carry now fixable in the same touch as B2.
