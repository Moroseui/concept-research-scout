**Verdict: APPROVE — no blocking findings.** The round-4 review is written to `ideas/023/probe_review.md`, replacing round 3 (prior rounds remain in git, per established practice).

What this round covered:

**Directive fidelity.** The revised `probes/023/run.py` (SHA-256 `d2dd41a6…`, commit `7fbef0e`) implements the 2026-08-25 exit-5 directive exactly. Case discovery now comes from the 7z archive member manifest via `archive_case_inventory` (run.py:226), with a case-insensitive regex accepting both `sub-stroke` and `sub-strokecase` prefixes and suffix-only matching that makes the `raw_data`/`rawdata` divergence moot. The full diff against the previously approved code touches only discovery, lesion-member selection, duplicate surfacing, and exact-path extraction resolution — Phase S, the approval gate, the split function, all gates, thresholds, and the analysis are byte-unchanged, and the contract blob (`349af5ad…`) still matches the approval marker, so the standing Phase-C approval remains bound.

**The decisive check: validation against the real payload.** I fetched the attempt-2 results branch and traced the new logic against the actual 2,983-member `archive_manifest.csv` the failed run persisted. The manifest shows 149 members each for CBF/CBV/MTT/Tmax, 149 rawdata NCCTs, identical case-id sets across all three families, and confirms the 150th lesion row: `sub-stroke0142` has both the canonical `…_space-ncct_lesion-msk.nii.gz` and a stray untagged, uncompressed `…_ses-02_lesion-msk.nii`. The code's canonical-selection rule retains the space-ncct member (also the only contract-valid NCCT-space input) and routes the duplicate into both `schema_census.csv` and `exclusions.csv` with an explicit reason. On this payload the census that killed attempt 2 now passes, and the split assignment is provably identical to what the approved code would have produced — no patient can cross the census/reserve boundary.

**Findings.** Round 3's F3 (the load-bearing README glob typo) is resolved; F1, F2, F4b, F5, F6, F7 and the N-series carry unchanged. Four new non-blocking items, the only user-facing one being that the README's duplicate-handling paragraph mispredicts the real run — it says non-identical duplicates stop the run, but the actual non-identical duplicate is resolved by the (undocumented) canonical-selection rule, so an operator reading it would wrongly expect attempt 3 to fail. Worth a wording fix at the next authorized touch; it does not affect execution.

One environment note: this sandbox cannot execute Python, so compile/smoke attestations rest on the builder's `verification.json` (whose artifact hashes I confirmed match the reviewed files) plus static tracing — the same basis round 3 used.
