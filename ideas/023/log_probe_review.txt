Review complete — **APPROVE, no blocking findings.** The new `ideas/023/probe_review.md` (round 5) is written, replacing round 4 with git preserving the history.

What I verified:

- **The diff base is airtight.** run.py at both `7fbef0e` (the round-4 APPROVE) and `6fde01e` (the take-8 launcher commit) hashes to the same `d2dd41a6…`, so the single `6fde01e..681b06c` diff is the complete delta from the last approved code. requirements.txt and the launcher notebook are byte-unchanged.
- **The amendment directive is implemented exactly.** The contract diff touches only clause 66 (now the unit-free per-patient 98th-percentile vessel exclusion, with the payload evidence and the conventional-scale correspondence recorded, as the directive dictated) and the invalidating-failures line (kill-code 104 marked executed and retired). Clause 72 — the median-centered identity coordinate — is untouched. In run.py, `confirm_cbv_units` is deleted with zero remaining `fail(8)` call sites, the p98 is computed label-blind over each patient's finite positive CBV with strict "above" semantics and applied at both sites the old 8 mL/100 g cap applied, and `phase_c_cache/identity.json` gains `units_documented: false` plus the evidence summary, verbatim per the directive.
- **The pipeline is now genuinely unit-free.** The percentile mask is invariant under global CBV rescaling, rCBF/rCBV are mirror ratios, and the identity residual is median-centered — no Phase-C quantity depends on the undocumented unit. The builder's verification attests a rescaling fixture confirming the mask invariance.
- **Approval staleness is the designed state.** The contract is now blob `468974a7…` while the marker still binds `349af5ad…`; the runtime gate refuses both phases with exit 2 (builder-attested) until the human reviews the contract diff and binds a fresh approval.

Three new non-blocking findings: **R5** — take 8 left a pre-amendment `phase_c_cache/identity.json` on Drive, so reusing that output dir will correctly fail closed at exit 4, but the message still doesn't name the remedy (clear the cache or use a fresh dir); **R6** — the units finding lives only in the cache dir and README, so `summary.json` won't carry it into a results bundle; **R7** — strict-above with tied CBV values can exclude under 2%, which is verbatim contract semantics but worth knowing when reading `exclusions.csv`. N4 (the unit contingency) is closed as executed-and-retired, and R4 (verification.json's lost approval attestation) is resolved.

Next step is the human gate: review the two-line contract diff and bind a fresh `HUMAN_APPROVED_PROBE` to blob `468974a7…` — before then, nothing can run, and the operator should plan a fresh output dir (or cache clear) for take 9 per R5.
