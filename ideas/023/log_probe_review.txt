**Verdict: APPROVE** — round-3 review written to `ideas/023/probe_review.md`. The rebuilt probe code faithfully implements the amended contract, and Phase C can run under the standing approval.

What I verified before ruling:

- **Chain of custody.** The on-disk `probe_contract.yaml` is byte-identical to git blob `349af5ad`, which matches both the `HUMAN_APPROVED_PROBE` marker and `verification.json`; the reviewed `run.py` SHA-256 (`8bd3156b…`) matches the verification record. No placeholder tokens remain, so the Phase-C placeholder scan clears, and every contract key the code's single-occurrence parser reads occurs exactly once.
- **All five operator-mandated resolutions are genuinely in place.** B1: the lesion suffix is now the official `_lesion-msk.nii.gz`, resolved only in the label-blind-safe second pass. B2: the mirrored-median is a vectorized, memory-bounded slab computation (I checked the window arithmetic reproduces the frozen 5×5×3 reflected neighborhood exactly), each case is computed once, and both passes checkpoint per case with identity binding to contract/archive/split/code hashes. B3: the median-d bootstrap CI, native-support distribution, and identity-residual distribution are all produced, the latter two written *before* their gates can exit. The finiteness gate is scoped to analyzed voxels with per-map counts, and the rawdata NCCT is required, resolved by exact suffix, and documented in the README.
- **Round-2 carryovers.** N1 (Phase S/C conjunction divergence) is resolved — and I confirmed the rule change cannot alter the frozen simulation output, so the amended hash `59069fa9…` stays reproducible. N9 is half-resolved (thresholds now echoed in `resolved_config.json`).

Seven non-blocking findings, none of which changes what the census measures. The one worth acting on before spending the 99 GB download: **F3 — the README's NCCT extraction glob reads `rawdata/**/_ncct.nii.gz`, missing the `*` before `_ncct`**; followed literally it extracts zero NCCT files and Phase C dies at case 1 (cheap re-extraction, but it's the exact line the amendment made load-bearing). Also notable: a subtle `mtt > 0` mismatch between the quartile-cut-estimation mask and the measurement mask (F1, likely a near-empty voxel set but a preregistration-consistency wart), a non-atomic outcome-checkpoint rewrite (F2), and the fact that every Colab resume re-pays two full 99 GB digest passes before honoring checkpoints (carried N7, now amplified by the resume design).

I did not commit; per the pipeline's pattern the driver records review rounds in git.
