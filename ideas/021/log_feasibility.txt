Feasibility stage complete. **Verdict: GO** — conditional on a zero-GPU Stage-0 probe, written to `ideas/021/feasibility.md`, with 20 new verified literature rows and one dataset row added to the evidence CSVs.

**What the verification established (all from primary sources fetched today):**

- **All three operator-forwarded conditions are answered.** (1) Normalization: nnU-Net's per-image z-score for non-CT channels is confirmed in the actual code, and — the load-bearing finding — a `noNorm` scheme exists, so the frozen-constants normalization pin is implementable *by construction*, and the affected-side bit-identity gate can be tested with preprocessing alone, no trained model. (2) Finished maps: the release contains both raw 4D CTP and NCCT-space derivative maps; consuming only finished maps is a design pin, and the challenge report confirms the top teams did the same, so the AIF/VOF-contamination pathway is excluded. (3) Receptive field: the winner's ResEnc L patch [56, 320, 256] spans both hemispheres; for a self-trained model I added a new Stage-0 gate — the planned patch must be verified to span the midline, or the question isn't well-posed.
- **Dataset is clean to use:** Zenodo open download, CC BY-NC-SA 4.0, no DUA, 149 released cases (hidden test set stays hidden — all splits come from the 149).
- **The mandatory prior-art section holds:** the edit-input/read-output mechanism class is well-precedented (occlusion, meaningful perturbation, counterfactual diffusion), and clinical practice already does arithmetic on finished perfusion maps (the AJNR mirror-normalization pipeline, clip-at-150%). The stay-in-distribution strategy has real numbers: absolute CTP values carry 16–31% measurement CoV and a ~1.5× CTP-vs-PET scale bias, so the proposed ±10–20% confirmatory doses sit inside documented variability, gated empirically against the cohort's own percentiles.
- **One material correction to the record:** the winning team *has* released inference code and trained weights (github.com/KurtLabUW/ISLES2024 + Google Drive), contradicting the 2026-08-17 critique. This creates an operator decision — audit the actual winner at zero training cost versus the card's self-trained stand-in — flagged in the memo but not decided there. Two citation corrections also recorded (winner authorship is Ren et al., not "Riepe"; arXiv:1907.08196 is Raina et al., not Clèrigues).

**The biggest remaining risk is unchanged and honest:** the up-scaling arm may fail the realism gates or the power gate, in which case the study stops and hands the weaker fallback to the operator under the successor-governance clause. The section-11 probe (one day of CPU work plus the 99 GB download) converts every remaining unverified load-bearing fact into an inspected one before any GPU training is authorized. Per the coding gate, no code was written; the next steps would be the Stage-0 probe authorization and, separately, the operator's checkpoint-vs-self-trained choice.
