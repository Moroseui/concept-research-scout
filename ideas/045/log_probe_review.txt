The round-2 review is complete: **verdict APPROVE**, written to `ideas/045/probe_review.md` (round 1 remains in git at 2a56f99).

**What I verified:**

- **B1 (one-directional join gate) — fixed exactly as round 1 prescribed.** `build_design` now derives the audit-side key set and fails `EXIT_JOIN` on any inequality with the outcome keys, printing a bounded sample of offending keys in both directions (run.py:266-274). The round-1 concrete failure path — a wrong audit file smuggling a 100th or reserved case to a completed run — is now refused before any design row exists, and the formerly vacuous check became an honest three-way assert. The harness added dedicated regressions for both properties.
- **B2 (unaccounted band-1 drops) — fixed.** Both loaders count total-versus-filtered rows with conservation asserts; the 198 audit rows and 99 per_patient keys filtered as band 1 now produce `non_primary_band` exclusion records with counts, and file totals appear in the determinism manifests, `input_manifest.csv`, and `summary.json`. I re-derived the real-data arithmetic from the actual CSVs (594 = 396+198; 297 = 198+99) — the drop is now fully reconstructible from the bundle alone.
- **No collateral damage.** The 527271d→a5ec5db diff touches only join validation and accounting: thresholds, the condition-number/leverage/LOO metric chain, the nine gates, outcome blindness, caps, and claim language are byte-untouched. Identity checks pass: the approval marker still binds the live contract blob `e7071541…`, and `verification.json`'s sha matches the reviewed `run.py`. All six standards-checklist items are now MET (item 2 was the only miss).

**Non-blocking findings carried or added (seven, none can yield a wrong positive):** the `Infinity`-in-JSON degenerate path, the empty-band exit-12 taxonomy, success-path-only `run_log.txt`, a narrow unknown-style split-manifest corner (now loudly recorded rather than silent), the unrecorded numpy version, smoke-branch dead stores, and cosmetic type mixing in the aggregate exclusion rows.

One method caveat on the record: this environment can't execute the probe, so smoke evidence rests on the sha-bound `verification.json` plus line-level reading — the same basis round 1 used.

I did not commit — the driver owns the stage commit (it will bundle `probe_review.md` with the prompt and log, as in round 1). With APPROVE and the contract untouched, the standing human approval remains valid and probe execution can proceed under existing machinery.
