Review complete — **APPROVE, round 1, no blocking findings.** `ideas/045/probe_review.md` now carries the v3 review (the v2 round is preserved in git history at f7aec67).

What I verified before approving:

- **Authority chain is intact.** The `HUMAN_APPROVED_PROBE` marker (2026-09-01T06:57:20Z) binds contract blob `b1e28361…`, and the on-disk `probe_contract.yaml` recomputes to exactly that blob. The committed verification receipt binds the reviewed `run.py` by sha256 (`69622688…`, matching the working tree) and was checked four minutes after the approval, in the same commit as the code.
- **The code is contract-exact.** One OLS fit of `d = intercept + band-3 + centered HU imbalance` on the frozen 198 rows; both input sha256 pins now enforced in-run (closing the v2 review's leading finding); split manifest written and hashed before `per_patient.csv` is ever opened; exactly 10,000 patient-cluster bootstrap replicates at seed 20260901 with both band rows traveling per patient and abort-on-any-replicate-failure; the parent-direction reconstruction gate before the bootstrap; the three frozen outcome classes implemented with correct precedence (decisive checked before association); wall-time stop is exit 8 with no summary, so it can't read as a negative. All nine required outputs are written, and the new output-collision guard closes the v2 lineage finding.
- **Independent re-derivation.** I re-hashed both inputs against the contract pins and recounted their structure from disk (594/297 rows, 99 per stratum, zero duplicate keys, exactly the two style groups) — the join gates and the exclusion-conservation assert will hold on the real data.

Eight non-blocking findings are on the record; the two worth your eye:

1. **Per-replicate re-centering** — the bootstrap recomputes the pooled centering mean inside each replicate (textbook plug-in bootstrap, and the same convention v2's leave-one-out used). The contract underdetermines this; I recorded the reasoning *now*, before any outcome is visible, so the interpret stage describes the CI functional accurately and the choice can't be relitigated after the result is seen.
2. **Status vocabulary** — the bundle will carry the contract's own three classes (`DECISIVE_MEASURED_EXPLANATION_FAILURE` / `ASSOCIATION_COMPATIBLE_WITH_CONTRIBUTION` / `SENSITIVITY_LIMITED`), not the older `POSITIVE_PATTERN`/`NEGATIVE_PATTERN` literals. Contract-faithful, but the 045 registry has no v3 node yet, so the node added at import time must declare these terminal statuses — flagged so the S2b interface hydra is anticipated rather than rediscovered.

Next act per the pipeline: the probe may be executed under the standing approval (`python probes/045/run.py --output-dir probes/045/results/results_v4`), then record-result → interpret. This run reads `d` for the first time in the 045 lineage.
