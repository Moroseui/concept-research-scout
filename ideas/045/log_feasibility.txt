Feasibility stage complete. **Verdict: GO** — written to `ideas/045/feasibility.md`, with three new rows in `evidence/literature.csv` (idea-045 L1–L3) and one in `evidence/datasets.csv`.

What was verified today by direct inspection:

- **Keystone confirmed.** Both input tables exist at the contracted path (`probes/023/results/results_v2/`, import commit `1c0acdb`) with SHA-256s recorded. The audit table has exactly 99 rows in each of the six stratum×cell groups with zero duplicate keys; `per_patient.csv` has 99 rows per stratum, zero duplicates; the case sets are identical across the two files, so the required join is bijective. All 594 `median_hu` values are finite numerics (3.0–59.0 HU) with real variation per cell (28–32 distinct values each). The excluded case `sub-stroke0043` is absent and properly named in `exclusions.csv`, and `summary.json` matches the card's keystone evidence transcription-exactly.
- **Outcome-blindness preserved.** I never read a value from the `d` column and deliberately did not compute the HU-imbalance distribution, condition number, or leverage — those are the unapproved probe's own gated deliverables, and computing them before approval would contaminate the gate. The memo states this conduct rule explicitly.
- **External sources.** Alzahrani 2023 (PMC9855746) re-verified live with the verbatim quote intact — with one correction of record: the journal is *Stroke*, not "Journal of the Belgian Society of Radiology" as the keystone screen wrote (non-load-bearing). New closest-external-work anchor verified: the net water uptake literature (Lu et al., AJNR 2023, DOI 10.3174/ajnr.A7741; Minnerup 2016 cited secondarily and marked as such) — it establishes NCCT attenuation as an outcome-relevant quantitative signal, which supports the confound premise, while leaving idea 045's specific audit-the-reversal analysis an untaken gap.
- **Costs and access.** No new data, no DUA, no GPU, no model — minutes of CPU over two small in-repo CSVs. The idea involves no data manipulation, so the intervention prior-art subsection doesn't apply (stated explicitly in the memo).

The one honest limitation, carried into the memo and the "In plain terms" close: the 99 outcomes were opened in the parent study, so this remains exploratory successor-design evidence — a positive can suggest but not prove the tissue explanation, and a null is bounded to the median-HU proxy. Next act per the pipeline: the human approval gate on `probe_contract.yaml` v1, which the memo endorses unchanged.

Sources: [Lu et al., AJNR 2023 (PMC9835911)](https://pmc.ncbi.nlm.nih.gov/articles/PMC9835911/), [Alzahrani et al., Stroke 2023 (PMC9855746)](https://pmc.ncbi.nlm.nih.gov/articles/PMC9855746/)
