Done. The interpretation and decision are written for round 1 of interpret-build; per the round rule I did not touch `evidence/decisions.md`.

**Verdict: REVISE.** The probe's result is a valid, pre-registered feasibility negative — `NEGATIVE_PATTERN`, with 4 of 9 frozen gates failing — and it mandates specification revision before any outcome value may be read. It is not evidence about idea 045's scientific question, which remains open and unread.

What the bundle shows (all verified against `probes/045/results/results_v2/` at import commit `fe7d30a`, identity chain intact: approval marker, current contract file, and `resolved_config.json` all carry blob `e7071541…`):

- **Demonstrated, deterministically:** the design is full-rank but ill-conditioned — condition number 38.89 against the frozen ≤30 bound (singular values 14.089 down to 0.362); band 2 has only 17 distinct integer-quantized imbalance values against the ≥20 floor (band 3 passes with 26); one row breaches the 0.20 leverage bound (0.2636, sub-stroke0183, band-2 imbalance +18 HU); and the leave-one-patient-out sweep never gets below 35.73 across all 99 deletions — so the failure is structural to the covariate geometry, not one outlier.
- **The sharpest diagnostic:** the patient that breaks the leverage gate is also the deletion that most *worsens* conditioning (43.82). Dropping the outlier makes things worse, so the repair must be respecification (standardized/rank-transformed exposure, pooled-slope reduction, or coarsened bins), not case exclusion.
- **Blindness held perfectly:** zero `d` values read, zero reserved cases accessed, split frozen before the outcome file was opened, determinism manifests bit-identical. That matters forward: because the probe was outcome-blind, its geometry can inform the revised specification without contaminating the future analysis.
- **No validity failures:** no invalidating-failure clause fired; this is a genuine negative, not an invalid run reinterpreted as one.

Artifacts produced:
- `ideas/045/interpretation.md` — full interpretation with demonstrates / suggests / does-not-establish / validity / variants / next-decision separations, every number carrying a bundle citation.
- `ideas/045/decision.md` — Result card (idea, probe position, dataset pins, primary metric, contract blob, families claude→codex, out-of-scope warnings), Layer A finding, Layer B CONSORT-style derivation (594→396 audit rows, 297→198 key rows, 297 band-1 exclusions in 2 records, 198 analysis rows, 99 cases), and a Layer C claims table where every quantitative claim resolves to file, selector, and column.

Both files are uncommitted in the working tree, ready for the cross-family (codex) citation review; the `evidence/decisions.md` entry waits until that review passes.
