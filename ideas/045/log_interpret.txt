Both revised documents are clean. The revision is complete — all three blocking findings from the round-1 cross-family review are fixed in `ideas/045/interpretation.md` and mirrored in `ideas/045/decision.md`, with no scope expansion and no change to any scientific claim, number, or the PAUSE recommendation.

**Finding 1 — uncited derived ratio and per-band imbalance algebra (Suggests item 1).** Removed the "less than 0.006 of its ~0.054 size" ratio and the entire uncited algebra about band-mean centered imbalances being equal and opposite. The paragraph now quotes only cited quantities — including band 3's actual adjustment-change interval [−0.0029349507985416763, +0.001154572906542486] from `bootstrap_summary.json`, replacing the uncited "exact mirror" shorthand — and the structural reading is stated as a qualitative side-by-side comparison of those cited values. Decision.md's Layer A lost its matching "bands 2 and 3 carry nearly the same average HU imbalance" clause.

**Finding 2 — cohort-wide extrema claim (Suggests item 3).** Before rewriting, I verified the extremum labels against the data: scanning the full `hu_imbalance` column confirms +18.0 (sub-stroke0183, band 2) is the maximum and −28.0 (sub-stroke0109, band 3) the minimum across all 198 rows. The rewrite grounds the ranking with a full-column citation (`[cite: per_patient_attribution.csv | all 198 rows | hu_imbalance]`), and the generalizing sentence is replaced by one explicitly scoped to those two rows, stating outright that no governed cohort-wide extremum comparison exists in the bundle. The decision.md Layer C row is relabeled accordingly and carries the same ranking citation.

**Finding 3 — uncited exact identity claims.** Each now has a resolvable tag or was removed:
- *Import identity*: manifest sha256, file count 13, `source_commit` null, and import timestamp are all cited to `probes/045/results/results_v4.import.json`. The bare import commit hash (`14b183f…`) and transaction commit (`e73fcef…`) were removed everywhere, since no governed artifact records them — the bundle is now identified by its byte-manifest hash, including in Layer C's bundle-root preamble.
- *Approval timestamp*: cited to `ideas/045/HUMAN_APPROVED_PROBE` line 1, quoted at the marker's full precision (`2026-09-01T06:57:20.966365+00:00` rather than the rounded `06:57:20Z`); the marker's `contract_blob` and `registry_sha256` pins are cited too.
- *run.py hash*: `verification.json` turned out to carry no hash field, so the claim is narrowed to what a citation can support — the code is "pinned at run.py sha256 `69622688…` in the round-1 cross-family APPROVE" — cited to `probe_review.md`'s opening paragraph, dropping the uncheckable "recomputed during this interpretation" assertion.
- *49 reserved cases*: all three occurrences now cite `reserved_case_count: 49` in `probes/023/results/results_v2/summary.json` (the bundle itself only proves zero were accessed).

Housekeeping: the round markers in decision.md now say round 2 (revision of round-1 blocking findings). Per the stage rules, no `evidence/decisions.md` entry was appended — that waits for review approval — and I left the unflagged probe-plan commit reference (`2b816b9`) untouched to stay within the findings' scope. The files are written but uncommitted, ready for the interpret-build harness to commit and route back to the codex checker.
