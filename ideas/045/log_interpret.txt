Uniqueness confirmed — the extra grep hits were HU medians in other columns, not the imbalance column; sub-stroke0183 (+18.0) and sub-stroke0109 (−28.0) are the unique extremes, matching the v3 diagnostics' pooled range. Both extreme-row claims stand as written.

## Summary

**The interpret stage (round 1) for idea 045's v3 attribution probe is complete: `ideas/045/interpretation.md` and `ideas/045/decision.md` are written, ending in PAUSE. No `evidence/decisions.md` entry was appended, per the round-1 rule.**

**The scientific result:** the pre-registered decisive negative fired. Status `DECISIVE_MEASURED_EXPLANATION_FAILURE` — after adjusting for Q1-minus-Q4 median NCCT attenuation imbalance, band 2 stays negative (−0.0313, 95% CI [−0.0559, −0.0079]) and band 3 stays positive (+0.0224, 95% CI [+0.0039, +0.0434]). Idea-023's sign reversal survives tissue-composition adjustment intact, so the card's question is answered **no** at achieved precision: measured attenuation imbalance did not create the reversal.

Key points carried into the interpretation:

- **The failure is structural, not marginal.** Bands 2 and 3 carry nearly identical average HU imbalance, so adjustment moved each band mean by only ±0.00067 and the band gap by −0.0013 (CI [−0.0059, +0.0023]) against a gap of 0.0537. The verdict does not depend on the slope's imprecision (β_HU = 0.00107, CI spans zero — which under the contract licenses no association claim in either direction).
- **Validity is clean.** Parent band means reconstructed digit-for-digit against `per_stratum_summary.csv`; split frozen before the first outcome read (hash identical to both feasibility audits); 10,000/10,000 bootstrap replicates, zero failures; zero reserved cases touched; all 13 bundle files present; determinism manifests agree; live contract blob `b1e28361…` matches the marker and `resolved_config.json`.
- **Scope is tightly held.** The common-slope model cannot see opposite-signed HU effects across bands (the v1 design that could was numerically infeasible), so only this one measured explanation is closed — not tissue composition in general, and the reversal's actual cause remains unknown.
- **Decision: PAUSE.** The revision made this analysis the whole candidate and its decisive arm executed; nothing further is authorized under the card. Successors (band-specific respec, non-median composition proxy, severity-adjustment design) must register as new candidates. One ops note flagged for the operator: the executed attribution probe still needs its node added to `ideas/045/registry.yaml` and a re-ratification, as probe_review finding 7 anticipated.

Next in the pipeline: the codex cross-family citation review of this interpretation; on APPROVE, the scoped decisions.md entry and `ratify-interpretation 45 --status PAUSED` are the operator acts.
