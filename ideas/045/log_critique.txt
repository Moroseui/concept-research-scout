Critique written to `ideas/045/critique.md`. **Decision: ADVANCE TO REVISION — no fatal objection**, with three repairable defects, one of which materially weakened the card's premise once I checked it against the bundle.

**The leading finding came from joining the card's own evidence files.** The revival basis rests on band-1 hypodense-Q1 examples, but band 1 was the parent's *null* band — the decisive failure was the band-2/band-3 sign reversal. Worse, the three cited motivating cases (sub-stroke0092, 0057, 0189) have near-zero or *negative* per-case d in `per_patient.csv`, the opposite of what an "established injury inflates Q1 membership" story predicts; the rows look more like CSF partial-volume *dilution*, with case-dependent direction. Tissue mixing corrupting the contrast remains plausible (it also fits the mean-vs-median divergence), but "tissue mixing explains the reversal" is currently pure inference — and the card prespecifies no Rung-0a→0b decision rule, so the expensive census could run even if the aggregate analysis refutes its premise. The repair: extend Rung 0a to a prespecified imbalance-versus-d attribution join (both CSVs are already imported; minutes of CPU) and write the go/no-go rule before the aggregate is seen.

The other two objections: **(2)** the confirmatory census reuses the 100-case split whose band-level outcome structure is now known, so "label-blind at design time" is diluted — the HU window must be pinned to a citable external source and frozen before the label-touching 0a join runs; **(3)** excluding frankly hypodense voxels preferentially deletes outcome-positive voxels, so the gate can manufacture a narrow-CI null — the census needs a preregistered post-gate outcome-prevalence descriptor and a frozen fork that downgrades such a null from "decisive" to sensitivity-limited (which also caps `negative_result_value` at 2 until fixed).

Lesser items: `design_template: counterfactual-synthesis` is mislabeled (nothing is synthesized — it corrupts the homogenization telemetry), the deliverable sentence should scope to equal-patient-weight means, and no plain_pitch exists so that fidelity check is N/A. I verified the card's provenance claims (confer q0002 item 4 and the CONCUR verdict are real; keystone-evidence numbers are transcription-exact against `per_stratum_summary.csv`). The constructive close recommends the attribution join as the low-hanging fruit — already paid for, decision-grade either way — with the census strictly conditional on it.
