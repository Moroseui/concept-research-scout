# Python standard library only. The real Phase-B staging event
# additionally requires the system 7-Zip binary (p7zip: `7z`), which is
# not a Python dependency; smoke runs need nothing beyond the stdlib.
