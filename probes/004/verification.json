{
  "idea_id": "idea-004",
  "stage": "probe_code_revision",
  "date": "2026-08-11",
  "review_addressed": "ideas/004/probe_review.md",
  "checks": [
    {
      "name": "syntax_compile",
      "command": "python3 -m py_compile probes/004/run.py",
      "status": "PASS"
    },
    {
      "name": "smoke_run_with_external_output_dir",
      "command": "python3 probes/004/run.py --smoke --output-dir /tmp/idea004-smoke-review",
      "status": "PASS",
      "detail": "Exit 0; all seven required artifacts were written outside the probe directory; summary.json kept contract_satisfied=false; all harness checks passed; per_sample.csv contained 54 data rows (3 executions x 18 heads)."
    },
    {
      "name": "released_call_signature",
      "status": "PASS_STATIC",
      "detail": "The scoring call is model(False, text_tokens, video, device=device), matching the released ImageLatentsClassifier call form identified by review B1. No real inference was launched."
    },
    {
      "name": "released_package_import_path",
      "status": "PASS_STATIC",
      "detail": "After freezing the clone commit, real mode installs transformer_maskgit/ and CT_CLIP/ with the released README's editable-install structure. Install failures use environment exit 11 rather than contract exit 5. Installation method and project paths are recorded in provenance.json."
    },
    {
      "name": "dependency_failure_routing",
      "status": "PASS_STATIC",
      "detail": "Phase 1 now fails fast with exit 11 for every declared real-mode dependency. Model and preprocessing ImportError/ModuleNotFoundError paths are separately routed to exit 11 and explicitly labeled environment failures, not contract results."
    },
    {
      "name": "pinned_requirements",
      "status": "PASS_STATIC",
      "detail": "probes/004/requirements.txt now pins torch, numpy, nibabel, pandas, transformers, huggingface-hub, scikit-learn, and tqdm. torch.load explicitly sets weights_only=False to preserve the released loader behavior across the PyTorch 2.6 default change. Dependency installation was not run locally because it is unnecessary for stdlib smoke mode and could be expensive."
    },
    {
      "name": "scope_and_compute",
      "status": "PASS",
      "detail": "Only review blockers B1-B5 were changed. No gated data, checkpoint, network download, CUDA inference, or expensive compute was launched."
    }
  ]
}
