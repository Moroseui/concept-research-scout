# Bulk artifacts the probe downloads or stages; never committed.
work/
vendor/
__pycache__/
# Real-run outputs may be committed after human review; smoke outputs are
# regenerable in seconds.
outputs_smoke/
# Contract-v2 artifacts: the results bundle may be committed (git add -f)
# after human review; the anchor cache holds ~0.5 GB of image volumes and is
# never committed.
outputs_smoke_v2/
results_v2/
anchor_cache_004/
