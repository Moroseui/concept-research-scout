# Frozen driver and released-code import dependencies for idea-004.
# Install this file before starting run.py; run.py never mutates its own environment.
torch==2.5.1
torchvision==0.20.1
numpy==1.26.4
nibabel==5.2.1
pandas==2.2.2
# transformers is pinned to the authors' released environment (transformer_maskgit/setup.py
# line 17, commented pin "#transformers==4.30.1"). 4.31.0 made BERT position_ids a
# non-persistent buffer, so the <=4.30.x-saved checkpoint carries a key that >=4.31 models
# reject on strict load (probe exit 5, root-caused 2026-08-12). tokenizers cascades:
# transformers 4.30.1 requires tokenizers<0.14, and 0.13.3 is the last 0.13.x release.
transformers==4.30.1
tokenizers==0.13.3
huggingface-hub==0.22.2
scikit-learn==1.4.2
tqdm==4.66.4
vector-quantize-pytorch==1.1.2
ema-pytorch==0.7.7
einops==0.8.0
beartype==0.19.0
accelerate==0.33.0
h5py==3.11.0
matplotlib==3.9.2
seaborn==0.13.2
scipy==1.14.1
pillow==10.4.0
