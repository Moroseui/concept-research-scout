# 047b: original bundle corresponds; successful console still missing

The operator's run label is not an attestation. The preserved original bundle
contains Phase B `STUDY_COMPLETE`, and both its resolved configuration and internal
run log name contract `dc586665d0bece940d1a1f4b3b0572f8c951c2ba` at historical
source `940293b6d562f2d3dd6bfd9d8d8281ccf01e4783`. Exact file hashes are in
[the correspondence receipt](047B_ORIGINAL_CORRESPONDENCE_20260907.json).
This establishes bundle correspondence, not complete scientific acceptance.

The system's bounded read-only search checked 509 existing private evidence
logs/streams/text logs and notebook outputs, without any candidate containing all
real Phase-B authority, exact contract and completion markers. No files hit the
size/time limit. [Search receipt](047B_CONSOLE_SEARCH_20260907.json).
This is not proof the console never existed. Git bundles preserve internal logs,
not an uncommitted successful-session console; full original bundle and the
separate exit-7 failure evidence were verified during cleanup. No rerun occurred.

Useful operator recovery locations, when convenient:

- The original successful Colab notebook's saved cell outputs or a downloaded copy
  retaining outputs, particularly its launcher cell through the terminal result.
- In Drive, the sibling `concept-research-scout-results/047_B_dc586665d0be.console.log`
  beside the original output directory, plus any renamed/copied successful-session
  output folder and its sibling console. This is the generated launcher's intended
  location, not a claim that a file currently exists there.
- Downloads or a session export from the original successful run, including a saved
  terminal transcript. Share its private location; do not paste case-level output
  into the conversation. An existing private evidence copy can be compared by hash.

Available disposition routes from the reviewed lifecycle are: recover genuine
original evidence and run existing gates; keep acceptance blocked if it cannot be
recovered; or propose an explicitly limited missing-evidence acceptance policy for
separate scientific review and operator decision. **The third is not an already
approved waiver or executable import path.** Existing policy still refuses import;
required audit evidence cannot simply be excluded. Internal run_log.txt is not
reconstructed or relabeled as the successful console.

Historical registry attestation, exclusions/audit semantics and explicit scientific
landing remain open. The completed first cleanup does not resolve those gates.
P001 preparation proceeds independently.

## Subsequent recovery — 2026-09-07

The operator subsequently located a saved notebook outside the earlier search
inputs. The versioned [incoming-evidence stage](047B_NOTEBOOK_RECOVERY_20260907.md)
verified saved Phase B output against both pinned bundles. The historical search
above is preserved; its missing-evidence status is superseded to the extent stated
in the new receipt. Scientific landing and original sibling-log verification are
not implied.
