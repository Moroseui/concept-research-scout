# 047b private acceptance and supervised interpretation decision

Status: prepared for operator decision; import, registry attestation and Phase-B interpretation have not run.

The shortest path uses the existing numbered-idea workflow in a private, unpublished checkout. No hosted adapter or new result representation is required. The recovered console, reviewed notebook intake, prior 17-file validation, completed cleanup and historical Phase-A approval are reused.

## One requested decision

Approve these three linked actions under decision ID `047b-private-acceptance-20260908-v1`:

1. **Private audit/metadata retention.** Retain `probe_exclusions.csv`, `per_case_staging.csv`, `staging_audit.json` and `split_manifest.csv` byte-for-byte. Preserve the two bookkeeping rows and two file-anomaly rows across three identities without describing all as analyzed-case exclusions. The system will interpret the existing evidence and limitations. This changes no scientific data or contract.
2. **Private Phase-B import and scientific landing.** Import all 17 required original files through `record-result 47` into the protected unpublished checkout. Keep the 198 staged input files separately preserved as exact private originals. No public case-level release, experiment rerun, main merge or push is included.
3. **Historical registry attestation after import.** Attest the unchanged historical Phase-A approval/import bindings in the existing two-node registry after Phase B is imported. Both nodes must satisfy the command's COMPLETE postcondition. This preserves the earlier approval and interpretation; it does not ask for Phase-A science to be approved again.

Then run the existing `interpret-build 47` and its opposing review. Any later scientific decision ratification or public output export retains its existing gate.

## Exact evidence and execution order

The [binding record](047B_PRIVATE_ACCEPTANCE_PACKET_20260908.json) binds original source `940293b6d562f2d3dd6bfd9d8d8281ccf01e4783`, Phase-B contract `dc586665d0bece940d1a1f4b3b0572f8c951c2ba`, all 17 file hashes and manifest `56380f9fae381867df3b9b574e43efe653b96becfca17c5a4c6d8576f718d361`. The source-complete subset rehearsal accounted for all 215 original files; all 198 omitted staged inputs were verified against preserved private bytes. The file allowlist/required set is unchanged. The current operative policy remains blocked pending this decision.

Execution is: record exact approval and scoped configuration; apply the approved private-import policy; import to `probes/047/results/results_v2-dc586665d0be`; verify the current-contract resolver selects that exact Phase-B path; run `ratify-registry 47`; run `interpret-build 47`. Do not ratify the registry before importing Phase B or substitute Phase A if the Phase-B binding is absent.

Astra authors and Fable 5 reviews using existing authentication, fixed roles and no model fallback. The small configuration change also supplies current operating/reviewer instructions and replaces unrelated retinal-specific rules in the generic interpretation templates with the actual governing charter/contract. The prior Phase-A interpretation, review, decision, prompts, logs and stage receipts are preserved with hashes and Git history. The three former current outputs are moved byte-for-byte into an explicitly historical subdirectory before the fresh run, so stale files cannot satisfy its output checks. The clone has no remotes and no automatic publication configuration.

## Work estimate and what changed

The earlier 3–5 engineering-hour estimate bundled acceptance, a proposed public metadata derivative, and hosted routing. A private exact-original import removes the derivative and hosted adapter from the first-interpretation path. No scientific code rewrite is required.

| Work | Engineering effort | Other elapsed time |
| --- | --- | --- |
| Prepare the exact acceptance packet | 30–60 minutes; packet now prepared | One 3–5 minute operator decision window |
| Necessary role/context configuration and source review | 20–45 minutes; four-file change prepared | Existing authenticated reviewer call |
| Formal Phase-B interpretation and opposing review | Supervision/receipt collection only; no analysis implementation | Historical Phase A: 480.451 seconds combined; allow 10–25 minutes initially, with any requested revision additional |
| Autonomous hosting only | 2.5–4.5 hours separately | Later explicit activation and 24–48 hours of disconnected observation |

No new authentication is presently required. No experiment runtime is needed for historical 047b interpretation. P001 remains independent. Autonomous hosting is separately preserved work and does not gate this supervised route.

## Why this is still a decision

The canonical [operating direction](../operations/REMOTE_OPERATING_DIRECTION.md) reserves “047 scientific landing” separately, and the [reviewed lifecycle](../isles-pilot/047_LIFECYCLE.md) requires an explicit audit/metadata disposition and registry attestation. The earlier push, cleanup and console intake had separate scopes; none performed result acceptance or Phase-B interpretation. This packet satisfies those remaining boundaries without reopening completed evidence checks.
