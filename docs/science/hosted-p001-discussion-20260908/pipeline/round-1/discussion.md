# P001 discussion — exploratory readiness proposal

**Question:** How well does unchanged admission Tmax >6 seconds predict the released follow-up infarct mask in the frozen 99 development cases?

**Evidence:** The operator ratified the prediction charter and conditional adoption of externally seeded P001 at decision packet `a94d886`. Supplied specification, executable text and historical opposing-family review support a transparent baseline. Official starter-code evidence supports a decoded-seconds convention.

**Limitations:** No validated terminal admission-input preflight receipt or patient result is supplied. Archive-specific units, decoding and integrity remain unverified here. This discussion uses supplied evidence; it reports no new inspection, search or measurement.

**Next decision:** Retrieve and validate the existing preflight's original terminal evidence through the bounded system evidence route, without redispatch. Use its findings to prepare the separate exact launch decision or identify a necessary amendment.

P001 has scientific value as a reproducible minimal reference: it measures spatial agreement and volume bias between admission hypoperfusion and the released MRI-derived infarct mask in NCCT space after observed treatment. Equal-patient mean Dice is primary; median Dice and absolute/signed volume errors provide context. Preserve the specified 2,000-resample patient bootstrap, seed 20260905, and its limited interpretation as within-cohort resampling uncertainty.

A valid poor result would show limitations of this fixed threshold on these development cases. It would not establish that admission imaging is uninformative or that fitted prediction cannot improve. Treatment-dependent tissue survival, small lesions, registration, cohort selection and prior outcome exposure limit inference. This is neither a trained model nor evidence of clinical utility, causality, novelty or external validation. Retaining the conditionally adopted baseline is reasonable; the supplied evidence establishes no contradiction requiring changed scientific bytes. No follow-up is selected before a valid baseline return and inspection; the ceiling remains baseline plus at most two comparisons.

The preflight evidence must distinguish three questions:

1. **What do decoded Tmax values mean?** The supplied subsequent check in `docs/science/PREDICTION_PRIMARY_SOURCES_20260906.json` cites official notebook `utils/isles24_evaluate.ipynb` at commit `34c66370e365bd5db4e9429154674928245e9915`: cells 3 and 5 load with nibabel `get_fdata` and apply a cutoff described as 9 seconds without additional scaling. This supports a convention, but predates the pinned v3 archive and does not verify its derivatives. It does not change P001's threshold of 6.0. The source-check amendment supports follow-up timing of 2–9 days and registration provenance, while explicitly leaving exported scalar units unresolved. Neither acquisition cadence nor NIfTI temporal-unit fields alone proves scalar-map units.
2. **Does the frozen loader implement that meaning?** Within the already approved admission-input scope, bind export metadata and actual eligible derivative identities to the stored datatype, effective NIfTI slope/intercept and decoded values. P001 uses `np.asarray(image.dataobj, dtype=np.float32)`; evidence should establish how the pinned nibabel environment applies scaling and whether the resulting values support direct comparison with 6.0 seconds. Header fields, effective proxy scaling and any absent/default scaling must be distinguished. Plausible value ranges alone cannot prove seconds. Record unresolved assumptions explicitly; contradictory scaling evidence requires a separately versioned amendment and affected scientific review, never silent conversion, threshold substitution or outcome-guided calibration.
3. **Are the authorized inputs intact and usable?** Bind the receipt to the preflight source, request/attempt, environment, frozen cohort/manifest and actual inspected files. Establish exact eligible membership from only the pinned `case_id` column, unique canonical admission Tmax selection, recorded compressed size/CRC checks before opening, finite decoded 3D arrays including background, and usable geometry with positive voxel volume. Preserve failures and private originals; no imputation, masking, resampling or extra exclusions are permitted. Report actual coverage and every unperformed check rather than treating a partial check as a cohort-wide pass.

The older `P001_METADATA_PREFLIGHT_20260906.json` proves only a supplied metadata-selection result: 99 eligible cases, 198 selected members and zero patient payloads opened. The frozen runner's `--preflight` returns before payload and review-gate checks. Neither establishes admission-image integrity or units. The full experiment's 198-member selection ceiling does not authorize opening follow-up labels during admission preflight. Keep the 49 reserved cases untouched and labels sealed; binary-label validation and matching prediction/label shape and affine (`atol=1e-5`, `rtol=0`) remain execution/evaluation checks after each prediction is written. Archive verification, if required by the approved route, retains the frozen size and MD5; historical archive evidence does not prove current access or replace required checks.

The immediate evidence gap is terminal retrieval, not a new charter decision. The current decision inbox records verified preflight dispatch and exhausted bounded client requests, with terminal retrieval pending. Reconcile the original attempt, console and output receipt through a registered, bounded retrieval interface; missing visibility must not become an automatic retry. The Drive grant is recorded as approved with Google setup pending, so do not request the same grant again or infer that access is operational. If retrieval remains unavailable, record that dependency and continue eligible source/readiness desk work.

After validation, assemble the exact launch packet with input findings, residual unit assumptions, execution snapshot, applicable opposing-family review and reconciled job/checkpoint state. Patient launch and transfer to a new backend remain separate operator decisions. Historical review cannot cover changed dependencies automatically. A passing preflight is neither launch permission nor a scientific result. P001 does not depend on 047 acceptance; pending 047 interpretations supply no conclusions. Other reserved publication, main-merge, writer/reset, limiter/unattended, credential and spending decisions remain unchanged.

This artifact is discussion-stage proposal content only. Its controller receipt and subsequent review are not supplied or claimed. Execution, result acceptance, analysis, interpretation and successor selection must later run through their versioned system interfaces with bound receipts. Preserve private consoles/checkpoints and the exact five-file aggregate publication manifest; no patient data or expanded publication authority is introduced.

Provenance: current operating direction SHA-256 `0690a6d7e8c1c8ab18236d47e9dc9fd1329c0a2ff77dda197164b90527d6239c`; selection record `campaigns/isles24-pilot/prediction_selection.json` SHA-256 `56fa21d431ad60f5c6241d059e144bf7de0c2956eda85cf871c6955498c5c643`; discussion source `251f899b29b292c7cbb79474d7b4ca6e40de2d54`; task packet SHA-256 `3048557d75fc3dc7a076ed3fa04a7959caa2e36c56ae3175ce5766fc849769e8`. Historical proposal status text remains preserved; the later selection records ratification and conditional adoption, which are not reopened.

Frozen P001 bindings, transcribed rather than rehashed: specification SHA-256 `d76cf92b35c8aabe443b91d734285af2e5653de00bbadc13360e87fa6e45f56c`; code SHA-256 `d54e3ea5c45c0d47fe8bace014058e1660d92b72abc36cc2fdc077acae3d47b0`; investigator decision SHA-256 `78da702da6a340da79a242aa049b64009b4b954a215e22fc1387212390dce68e`; historical review SHA-256 `18f6ace723871d9319a5eda995ffb99389af46bb95cd50b1e97e1a63d326d727`, reviewed commit `469d29df002ea78f64146731244769d7c82330d6`. Preserve original snapshot `0770c7dcabe781cbfb87de505755e7aafa758f2e`, scientific source `d6a1184b4378e849213fd887a6f7b103fb1a64d5`, notebook commit `1a81c037343598f4e4585153b11d761b87a9ae3a`, cohort SHA-256 `aba525122f796618761e6c4d29b664647760e8dff4987932c3ff6ab5456faae9`, manifest blob `edb9a8c2ceb90df214cdd7ec167f0b1e8c858bb2`, and archive identity 99,014,629,647 bytes / MD5 `36ae28b9a17f7340b8bbef62b595cb57`. Original authorship and approvals remain unchanged.
