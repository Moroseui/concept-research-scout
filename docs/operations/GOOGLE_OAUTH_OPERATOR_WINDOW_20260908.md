# Google OAuth — remaining operator window, 8 September 2026

Reuse [the approved access grant](DRIVE_ACCESS_APPROVED_20260908.json). This document does not request it again or change its exact source pin.

Completed: existing project, Drive and Picker APIs, drive.file scope, supplied Branding app name/support/developer contacts, operator-reported app publishing to Production, Desktop client creation/download and private transfer. The approved public informational site is live at https://moroseui.github.io/ (repository commit `fb536dff0e011d3c93306c30f9496f4e3b7dd3b0`); homepage, privacy.html and terms.html returned HTTP 200 with exact approved byte matches. No research evidence is hosted there. The user's screenshot had empty app-domain links and authorized domains; Google's generic error did not identify one individual field. Website publication had its own explicit approval.

The ready authentication action is the private terminal helper at `/home/partho/.cache/research-drive-consent-window-20260908.py`. Run it in the operator's WSL terminal, open its private URL locally and select the existing original 047 console, existing train.7z archive, and available original P001 preflight console/receipt/admission-header files. Its loopback listener waits five minutes. URL, callback and token stay out of chat/Git. The helper uses the verified existing source, existing SSH identity and protected Desktop client; no model sign-in or experiment dispatch occurs. A timeout or partial consent must be inspected before repeating it.

Original 047 filename: `047_B_dc586665d0be.console.log` under `concept-research-scout-results`. P001 preflight uses `isles-pilot/p001-preflight-.../preflight.console.log`, with `receipt.json` and `admission_headers.private.json` under `attempt/`. Register actual selected file IDs privately. An absent original is an evidence dependency, not a request to rerun a notebook. Archive registration authorizes metadata only.

After CONSENT_SAVED_PRIVATELY, the agent will inspect existing consent/registration intents, register actual IDs, verify aliases and private output folder, install the approved dedicated service, and exercise bounded collection/storage/refresh/restart. P001 preflight review and 047 evidence gates remain separate system tasks. Do not print selected contents into the public setup record.

References checked: [Google Branding](https://support.google.com/cloud/answer/15549049?hl=en), [personal-use verification exception](https://developers.google.com/identity/protocols/oauth2/production-readiness/brand-verification#exceptions), [GitHub Pages](https://docs.github.com/en/pages/getting-started-with-github-pages/what-is-github-pages). Domain verification was not inferred from the generic message; any actual Google ownership prompt remains a named external action.
