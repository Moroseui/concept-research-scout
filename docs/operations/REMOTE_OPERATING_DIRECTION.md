# Remote research-system operating direction — operator instruction, 2026-09-06

The operator's full deployment handoff is preserved privately, including connection
configuration. This sanitized record supplements the existing primary charters and
scientific contracts; it does not replace them. The purchased DigitalOcean Linux
host is the platform for research-system, with ISLES its first active campaign.
The earlier unprovisioned Google Cloud proposal is superseded as provider direction.
No second server or paid service is to be created by this implementation.

The continuing driver is Codex with exactly `gpt-6-astra`. Preserve other configured
system-agent roles. Ideas, specifications, scientific code, critique, analysis,
interpretation and repairs belong in persistent system routes. Existing campaign
proposal output is not adoption or authorization to execute. Deterministic
calculations and validation remain ordinary code. Use one mutating driver per
branch; completion and failure events must be durable, deduplicated and recoverable.
Blocked tasks yield to eligible independent work; idle polling is deterministic.

Authorized now: bounded administrator bootstrap on the purchased host, non-root
runtime roles, synthetic Linux validation, supervisor/report/review preparation,
and checked code/docs publication on the existing authorized development branch
`astra/infrastructure-milestone-record`. Preserve main, the original pilot branch,
cfadeb1/551be01 and frozen P001 checkout. Each main merge still needs independent
candidate review and explicit operator approval. New credentials, protected writer
permissions, limiter activation and new spending remain separately gated.

ISLES direction is exploratory prediction of follow-up infarct from arrival CT
inputs. P001 is a deterministic admission Tmax threshold baseline, not a trained
model or completed patient-outcome predictor. The envelope remains baseline plus
at most two justified follow-ups on the frozen development cohort. Reserved data,
outcome-selected membership as features, and unreviewed scientific changes remain
prohibited. Retinal evidence and historical interpretations stay frozen. P001
transfer/launch on Linux, 047 landing and cleanup each need their separate gates.
No patient input is transferred or archive rehashed during synthetic bootstrap.

Use a small Linux service around reusable queue/validator/publication primitives.
Pinned workers have immutable source/spec/environment bindings, bounded resources,
private original consoles and verified artifacts. Scientific workers receive no
model or GitHub credentials. Driver, reviewer, worker, controller and protected
publisher/reset ownership are separate. Bootstrap root access is not ongoing agent
sudo authority. Do not weaken SSH host verification or forward/copy private keys.

Keep human GitHub controls, readable notebooks, reports and steering. Colab stays
optional; its browser dependency cannot block compatible CPU bootstrap. Remote
Colab investigation has an approximately two-hour engineering bound. No unattended
Colab claim or limits evasion. Nightly deterministic reports queue one fresh Claude
primary-evidence review per immutable report identity; unavailable reviews are
honest and retries capped. A review or disposition must not recursively trigger
another report review. 21:00 America/New_York is a proposed report time, not chosen.

N=48 and hard 96 remain conditional and inactive. Before unattended server turns,
a reviewed design must cover starts/resumptions and subordinate activity, preserve
UTC counting and midnight-persistent halt, and enforce operator-only reset in
protected state. Writer/reset permission approval, shared initialization and hosted
activation verification are all required. This is not a dollar cap. Existing
Codex API and Claude subscription arrangements remain distinct; exact subscriptions
and provider backup state require private account verification, not assumption.

The operator reports the purchased host/backups at $115.20/month before taxes or
overages; planned uncreated storage would add $5. These are supplied budget facts,
not new procurement authority or live account verification. Do not promise measured
performance, backup recovery, phone delivery or 24–48-hour laptop independence
without actual hosted receipts and device/operator verification.

Unavailable originals in inspected project trees: ONBOARDING_astra_implementation.md,
ASTRA_REMOTE_RESEARCH_DEPLOYMENT_REVIEW_2026-09-06.md and
HANDOFF_fable_deployment_review_and_next_steps.md. Existing README, charters,
decisions, campaign and relevant ratified Next-decision sections were located.
Continue independent deployment work; recover the originals before a scientific
decision that depends on them. No replacement charter is invented.
