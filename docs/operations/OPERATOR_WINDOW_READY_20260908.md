# Remaining operator window — 8 September 2026

The protected handover design is approved in [the recorded decision](PROTECTED_HANDOVER_APPROVED_20260908.json). Main protection is installed and read back. Google Branding and the persistent connector are complete; no existing grant needs reapproval. The following concrete steps provide the missing App credential and the original files requested by the completed P001 system assessment.

1. Open [the prefilled writer App registration](https://github.com/settings/apps/new?name=moroseui-research-writer&description=Protected+publication+and+shared+admission+for+the+research+system.&url=https%3A%2F%2Fmoroseui.github.io%2F&public=false&contents=write&actions=read&webhook_active=false&request_oauth_on_install=false). Confirm Contents read/write, Actions read, automatic Metadata read; all other permissions absent, webhooks off, and only this account may install. Create it (or reuse an already created exact matching App). Generate its private key, then Install App on **only Moroseui/concept-research-scout**. Supply only the downloaded PEM file's local path. Credentials are transferred and API identity/permission/repository scope verified privately before installation; no credentials go into chat. The notification App remains separate. This is the already approved creation step.
2. In the WSL terminal run `python /home/partho/.cache/research-drive-consent-provenance-20260908.py`. Select the original preflight folder `isles-pilot/p001-preflight-0219db9669cf48fb8404b4e1c9c03540`: `attempt/binding.json`, `process.json`, `launch.console.log`, and `setup.console.log`. These supply the missing attempt/source/environment linkage; do not rerun the notebook. Select available originals and identify any absent item. This is per-file Picker access under the existing approved evidence scope, not a new Drive scope/service grant. The existing live token, five aliases and app-created output folder will be preserved; the supplemental registration helper creates only a private proposed configuration for exact reviewed installation.

P001 system assessment/review/disposition are complete and keep launch held for retained provenance, trusted termination or an explicit scoped missing-evidence disposition, current runtime/dependency/output bindings, and the separate exact launch decision. The retained protocol already verifies CPU Colab and the reviewed setup/launch wrapper execution; it contains no terminal preflight process-exit observation. The additional files are requested for correspondence, not to infer a nonexistent exit record. A current Colab connection may later be needed only after the read-only reconciliation cell is prepared.

Neither operation activates unattended research. Model-secret relocation remains staged behind separately reviewed main workflow integration and its bounded canary. Preserve current private credentials until those checks pass. Shared ledger initialization, exact writer scope checks and later activation remain finite follow-up tasks. Provider recovery confirmation and the separate historical audit disposition remain open.

GitHub documents [prefilled registration parameters](https://docs.github.com/en/apps/sharing-github-apps/registering-a-github-app-using-url-parameters). No domain purchase, new subscription, broad token grant or routine manual Colab JSON relay is needed.
