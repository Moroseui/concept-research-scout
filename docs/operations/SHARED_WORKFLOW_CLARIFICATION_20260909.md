This supplements the continuation handoff I already sent. Incorporate these clarifications into the current work without restarting completed tasks.

1. **Scientific work must use the system’s workflows.** This includes idea development, debate, experiment specification, scientific code/notebook generation, execution, analysis, review and decisions. Delegated authority lets an agent authorize eligible progress; it does not let it bypass those stages. When a capability is missing, implement a small reusable system feature, document and verify it, then use it. Avoid performing the science in an untracked side script and inserting the finished conclusion afterward.
2. **Humans and agents must be able to operate the same system.** They should share underlying operations, validation and persistent task state, with explicitly attributed identities and permissions. Human usability includes maintainability, readable code and explanations, simple controls, understandable errors, reliable recovery, and the ability to propose, inspect, steer, pause and continue work. Routine operation must not depend on knowledge hidden in an agent’s conversation or require hand-editing internal records. Difficult scientific ideas should receive clear explanations rather than rejection for being difficult to explain.
3. **Align the directives supplied to both remote models.** Reconcile Astra’s operating instructions, Claude’s reviewer directive, collaborator rules, stage prompts and authority checks against one shared, versioned policy. Preserve their complementary roles: Claude should independently challenge Astra’s reasoning, changes, usability and actual remote setup. Shared rules do not require agreement. Verify which policy and role versions both models actually receive; changing repository documents alone does not establish that the deployed agents use them.
4. **Provide an easy, recorded change-request path.** A human or agent should be able to name an idea, notebook or run and propose a fix in plain language. Reuse existing steering/intake mechanisms and have the system supply the bookkeeping. Distinguish proposal, authorization, implementation and review. Record who proposed and applied the change, why, the affected version, actual modification, checks, authority and review status. Make it visible in the affected task, subsequent driver/reviewer context and daily report.
5. **Keep the proposed notebook connected to what actually ran.** Read-only debugging does not need a scientific approval ceremony. But an applied fix affecting code, dependencies, configuration, inputs, execution, state or interpretation must be recorded before dependent work uses it. Preserve the original proposal and revised executed version, and bind results to the actual code and settings used. Do not silently attach an old approval or result to changed code. Preserve original outputs and reconcile unexpected differences.
6. **Make review proportional, with meaningful later pushback.** Authorized, low-impact, reversible repairs may proceed after appropriate checks where policy permits deferred review. Mark review as pending and queue it. Material scientific changes—such as cohorts, labels, units, thresholds, preprocessing, features or evaluation—need their applicable review before affected execution or acceptance, using agent authority where delegated. Calling something “debugging” or “easy” does not establish that it is low-impact. Later criticism must reach the driver and identify any results needing correction or revalidation. Pending review must never silently become approval.

Demonstrate these requirements through the existing acceptance task: submit a simple change request, record and apply an eligible fix, preserve the proposed and executed versions, bind the result correctly, show the change to both roles, and obtain a recorded review outcome. A human should be able to inspect and steer that same saved work.

Keep this a focused extension of the existing system. Continue scientific progress and the finite handover work; avoid creating a separate pipeline or a large new audit framework.
