# Prediction readiness and deployment — bounded path to Wednesday

Target: Wednesday, 9 September 2026. This is a dependency plan, not a promised
patient result or permission to bypass a gate.

The system authored a focused charter/rubric/prompt proposal and prospectively
assessed the externally seeded P001. Fresh Claude reviews approved both the
charter packet and a subsequent readiness packet **as proposals only**. The
recommendation is ADOPT_CONDITIONALLY as a minimal exploratory fixed-threshold
reference. The original scientific runner and historical approvals are preserved.

1. Operator reviews the exact proposed charter and prospective adoption linkage.
   The available current direction supports proposal preparation; the named original
   `ONBOARDING_astra_implementation.md` was not found in the checked locations.
   It has not been reconstructed or claimed read.
2. Resolve the inherited Tmax numeric-unit assumption from authoritative release
   or export documentation. The dataset paper now supports timing and registration
   provenance; it does not by itself prove exported value scaling. Contradiction
   requires an amendment and fresh opposing review.
3. Select the concrete launch route in a separate launch decision. The existing
   reviewed CPU Colab notebook/source is the shortest prepared route. Linux requires
   explicit backend/transport review and separate patient-transfer authorization.
   Full unattended deployment is not a prerequisite for a supervised approved run.
4. At authorized launch, reconcile live processes/checkpoints and current input
   availability/identity using the original private receipts. Reuse the preserved
   archive; do not redownload or replace it merely because a runtime is unavailable.
5. Validate all five aggregate return artifacts **and** private checkpoints and
   original console; use `scout.py validate-bundle`, then `record-result`, with
   `--campaign isles24-pilot --experiment P001 --bundle <aggregate-directory>
   --private <private-directory> --console <original-console>`. These argument names
   were checked against the current CLI/source; concrete paths come from the
   actual attempt receipt. Use the unchanged frozen execution environment for
   return validation where its review bindings require it.
6. Only after import, invoke `scout.py interpret-build --campaign isles24-pilot
   --experiment P001`; the system's opposing-family review and next-decision
   artifacts govern follow-up planning. No P002/P003 selection before that result.

If charter/input/launch gates are resolved early enough, Monday preparation and
Tuesday execution/validation leave Wednesday for review and interpretation. If a
run fails or an integrity gate refuses it, preserve the failure and report no valid
performance result. Time pressure is not a reason to change frozen thresholds,
exclude a case, expand scope, or claim clinical utility.

047b remains an adjacent evidence-reconciliation thread. Its candidate identity,
original successful console, exclusion disposition, registry and scientific landing
gates remain unresolved. Its pending context entry exposes dependencies and no
invented conclusion. It does not block P001 preparation or launch authorization.

The bounded metadata queue has executable context, baseline and Phase-B inventory
handlers, isolated immutable attempts, two threads and deduplicated completions.
Its blocked launch/import entries are decision placeholders, **not patient or import
handlers**. System author/discussion/repair routes execute separately under one
writer. Automated adoption and arbitrary tool dispatch have not been implemented.

Standing protected writer/admission/reset and unattended operation remain gated by
`PROTECTED_WRITER_ADMISSION_DECISION.md`. Phone acknowledgment is demonstrated;
operational phone decisions and recurring polling are not enabled. The next
scientific operator decision is prospective charter/adoption ratification, separate
from launch. No new model sign-in is currently needed.
