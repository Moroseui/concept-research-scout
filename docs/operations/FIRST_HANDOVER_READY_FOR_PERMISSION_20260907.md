# First remote handover: authorized preparation complete

The bounded implementation, supervised synthetic acceptance and fresh Claude
reviews are complete. **Live unattended research is not activated.** This is the
current closeout entry point; dated progress notes retain earlier failed/pending
checkpoints. Main and the preserved pilot branch are unchanged.

## What actually ran

- The immutable Linux executor `6b555075fcf553994ecac8e368f4676cbdffdc56` ran a new
  harmless predecessor and one selected successor under coordinator `a0795e6`.
  Each completion produced a real Astra-requested continuation, fresh Claude Fable
  review and Astra disposition: six new model-stage calls, exactly one successor
  submission. Original console/protocol evidence and separate retrieval are retained.
- Final installed source is `9af868d47fecaf26c82c88a4d957183ea50962f5`. The fully
  consumed three-turn fixture was updated without expanding authority. Actual
  human status/pause/repeated pause/resume/invalid-input checks passed, revision
  2→4. A non-root idle tick passed. Timers remain inactive for handover operation.
- Prior actual lost-response/restart recovery used zero new model calls. The
  application backup sample restored 57 files and two consistent SQLite databases.
  Actual phone delivery and authenticated synthetic acknowledgment passed.

Read the [execution result](HOSTED_COMPLETION_RESULT_20260907.md),
[installed evidence](hosted-final-handover-20260907/service.json),
[evidence clarification](hosted-final-handover-20260907/evidence-amendment.json), and
[actual human status example](hosted-final-handover-20260907/human-status-example.md).
The idle collector's zero-call/job fields are explicitly inferred, not independent
process counters. Astra's exact requested model was gpt-6-astra; its CLI did not
return a resolved model name. Claude's actual reviewer model was claude-fable-5.

## Verification and review

Final pytest: **489 tests and 40 subtests passed** outside the managed sandbox.
Orchestration runner: **214 tests OK**. State regeneration: **47/47 byte-identical**;
registry validation: **4/4**. The earlier sandbox-only socket denial and the failed
first human status command are preserved, not erased. The latter was a missing
`/usr/sbin` lookup in a scrubbed PATH; the reviewed fixed executable solved it.

Fresh source reviews approved `ca8f989`, `eb40b95`, and `9af868d`.
The [closing deployed-evidence review](HANDOVER_FINAL_DEPLOYED_REVIEW_20260907.json)
approved `edbadf1f45b83ba3549c023e9e50fb6474438542`. All five new review input
inventories were checked against Git blobs at their reviewed commits. Reviews are
attributed author-operated cross-family sessions, **not independent main-merge desk
approval**. Nonblocking collector/courier improvements and their dispositions are
retained in the closing review; original evidence was not silently changed.

Protected publication, shared Actions/server admission, operator-only reset,
report delivery, reporting/review/disposition and human controls are implemented
and tested. Credentialed hosted publication, live ledger initialization and the
scheduled unattended cycle remain unexercised because their grants are reserved.
No post-push CI claim substitutes for the controlled publisher's outgoing-history
and content checks. Exact publication/CI pins will be reported after those operations.

## One next decision

Review and approve or amend the
[concrete protected writer/admission/reset packet](PROTECTED_WRITER_ADMISSION_DECISION.md).
Its SHA-256 is `4a50253be6b24255b807f3ae4e5052a9a4075fde48a9e57b96ad438f67c48835`.
It specifies App scopes, two allowed refs, root-owned code/state boundaries,
operator-only reset, shared 48/96 accounting, existing credential bypasses and
proposed GitHub restrictions. Approval of that design is **not activation**.
After approval, account/credential setup will be guided one action at a time.
Reporting time, provider backup/account checks and remaining notification limits
are grouped there and in the decision inbox. No model sign-in is currently needed.

Then allow roughly 2–4 engineering hours for approved configuration and hosted
activation checks, plus separately approved 24–48 hours of laptop-disconnected
observation. This is an estimate, not a spending or scientific grant. During that
observation the supported intervention is authenticated SSH:

```sh
research-system-control status
research-system-control pause
research-system-control resume
```

Status leaves work running; pause preserves admitted work and blocks new admission;
resume does not reset quotas or activate timers. GitHub reports are phone-readable.
Phone comments currently acknowledge information only; they cannot launch, pause,
reset, ratify science or grant authority. Provider-side backup recovery and full
laptop-independent operation remain unproven.

P001 remains unexecuted. Prediction-charter/adoption, 047b processing and permitted
cross-charter evidence propagation remain queued with their dependencies; wider
context/intake design is not a handover prerequisite. Patient launch, reserved-49
access, 047 scientific landing/cleanup, main merges and new spending remain separate.
