# Resume verification

Resumed clean checkpoint fe6b7714911a1625ca8a6d8cb62105468c0a6489 on
astra/infrastructure-milestone-record. Remote main, pilot and milestone refs were
unchanged. SSH authentication succeeded; no key unlock was needed.

Existing source 6b555075fcf553994ecac8e368f4676cbdffdc56 and its three attempts
were reconciled before collection. Two successes, one intentional failure and
one blocked Colab job remain. No duplicate job was submitted. Hosted collection
passed restart/event identity checks, actual role boundaries, and a 19-file
application backup/restore sample. This does not establish provider recovery.
The synthetic 48/96 warning/halt/duplicate/midnight/reset fixture passed; live
activation and authenticated operator-reset enforcement remain unproven.

The approved b670743 Codex sandbox script was sent as hash-checked code over SSH
and executed without editing the installed release. It confirmed permitted
workspace writes, denied outside writes, and denied worker execution of the
model CLI while preserving the global userns restriction. No model was invoked.
The script's cli_version field is expected from its pinned binary identity, not
an independently measured version in that invocation. Original stdout/stderr
remain private; JSON files alongside this note are permitted execution receipts.

The prediction charter and 047b tasks were registered with their original 6b
bindings and included in a hosted report. The evidence-propagation task remains
versioned in QUEUED_SCIENTIFIC_TASKS_20260906.json; its hosted migration still
requires a registrar extension preserving existing bindings. No charter adoption,
scientific interpretation or patient execution occurred.

First human dependency: remote research-driver Codex device authentication using
the existing subscription. Exact instructions are kept privately and supplied to
the operator. Next is separate reviewer authentication. No activation, API-key
fallback, new credential authority or model substitution is implied by sign-in.
Astra continuation, fresh hosted Claude report review, actual phone decision,
live limiter permissions and long unattended observation remain outstanding.
