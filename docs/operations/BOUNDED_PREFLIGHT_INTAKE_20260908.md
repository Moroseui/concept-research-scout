# Bounded preflight assessment preparation

The completed first campaign discussion is retained. The setup helper now accepts a root-only explicit readiness/discuss task after exactly one fully consumed supervised campaign turn. Source, mode and request hash determine the next trigger and private turn root. Repeated or uncertain preparations, existing jobs, active controllers, incomplete stages/bookkeeping and changed configuration are refused. Old original evidence and admission ledger stay intact. One new turn allows exactly three system model stages; no patient execution, writer, schedule, reset or unattended activation is installed. Existing exact diagnostic drop-in is reused and preserved on rollback.

Drive evidence intake checks original collection hashes and receipt/console/header correspondence. It exports no case identifier or member hash, no voxel data, and no scientific acceptance. P001 receipt statements remain reported preflight evidence; original attempt binding and independent process exit are absent. The 047 sibling is compared byte-for-byte to the stream in the original notebook, rechecking both historical Git summary copies and the existing notebook receipt. This supplies original-file correspondence, not retrospective exit attestation or scientific acceptance.

Validation: 26 focused tests cover changed evidence, unsafe files, private identifier suppression, nonfinite/extra receipt fields, bounded task identity, no allowance changes and configuration rollback. Actual intake results and the source review must be preserved before hosted use. The scientific preflight assessment, its opposing review and disposition run through the existing campaign pipeline after setup. No frozen scientific file changes.
