# Implementation review dispositions

All original Fable responses remain versioned. REQUEST_CHANGES is not approval.
The persistent coordinator received completed Fable APPROVE at 9a4e0bf; its source
and original execution snapshot checks pass. Publication and future profiles
remain under follow-up review until completed responses are adopted.

Implemented: path-component and non-regular-blob publication refusals; atomic
expected-old ref comparison with mandatory fast-forward ancestry; direct script
import path; read-only CI permissions and step-scoped read credential helper;
legacy workflows stripped to explicit binding plus unconditional quarantine;
transactional registration; stale-lease/event identity checks; exact execution
snapshot pin; immediate failure for unexpected dispatch state; current and prior
proposal/import/spec grounding checks; symlink pipeline ancestor rejection;
scoped Claude file rules and positive tool lists; malformed efficiency receipts
counted and worker stage strings no longer copied; per-stage profile hash receipt.

Evidence-backed clarifications, not blanket rejection of review findings:

- The original scientific receipt already hashes validate_return.py,
  publication.json, requirements, tests and dependencies. verify_decision calls
  campaign_review.verify_receipt, which re-proves that full binding. The first
  pipeline review did not receive those dependency files. No scientific receipt
  was changed to answer this finding.
- run_isolated_stage uses a NEW disposable Git directory for EACH author/reviewer
  leg and copies ONLY declared outputs. Author outputs cannot seed the reviewer's
  fresh directory. An additional prepopulation refusal was added anyway.
- /usr/sbin/python exists in this Arch installation. The configured executable
  ran the hook test successfully; malformed JSON exited 2 and forbidden reads
  produced deny. The future command builder checks both executable files and runs
  the denial test before emitting a command. Portability must be revalidated on a
  different host. No Linux browser or global MCP change is proposed.
- The future worker profile is preparation only, not a patient execution adapter.
  Its builder restricts CLI tools to ToolSearch plus four exact MCP tool names;
  Grep/Glob/Read are not exposed. The get_cells hook is a supplementary rejection
  rule. It does not make arbitrary run_code_cell outputs safe: only the existing
  reviewed exact-cell/capture adapters may supply execution tasks. The full future
  MCP integration is not tested or enabled on the active connection.
- Real campaign discussion and independent review ran through run_agent with
  persistent prompts, outputs, execution receipts, and review. The second live
  discussion exercised disabled Codex sandbox tool networking. Tightened scoped
  Claude permissions receive a separate synthetic/live verification; no claim of
  unattended browser consent follows from CLI profile tests.

The plan is not deployment approval. Main governance defects and pending public
results cleanup retain their separate operator gates. Remote model workflows
remain blocked; local system stages are the authorized execution route.
