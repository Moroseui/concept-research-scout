## Review verdict: APPROVE — all three r1 blockers are resolved at this revision; remaining findings are non-blocking

This is the r2 follow-up over base 4f5b6b1d..4339928a6fe8. I re-checked the scientific core and it is unchanged and sound: admission-only input timing is enforced by construction (the prediction array is computed and persisted to disk before the label file is ever opened, `run.py:run`); the frozen 99-case boundary is enforced by the SHA-256–pinned cohort table plus the git-blob–pinned member manifest and anchored per-case path regexes, with only `case_id` read and all other columns discarded, so neither reserved cases nor outcome-derived rankings can enter; metrics, Dice edge cases, seed 20260905, 2,000 patient bootstrap resamples and the lexically frozen row order match SPEC in both `run.py` and `validate_return.py`; the 60-minute analysis cap is enforced in the runner and re-checked by the return validator; completion requires exactly 99 with no additional exclusion path.

### Assessment of the r1 fixes

**B1 (finite-Tmax ambiguity) — resolved by SPEC amendment, correctly.** SPEC.md v1.1 ("Baseline and evaluation" plus the "Pre-execution clarification v1.1" section) now states explicitly that any non-finite voxel, including background, is an input-integrity stop, matching `run.py:predict` exactly. This was one of the two fixes r1 offered, it was made before any patient execution, it is labeled as a clarification rather than a result-informed change, and the investigator decision's rationale records it. If real ISLES24 Tmax maps carry NaN backgrounds the first case will abort the run — but that is now an explicit, reviewed stopping rule with a defined amendment path, staging is retained, and a failed run is declared not a negative result. That is the correct posture: input uncertainty stays a stopping rule rather than becoming silently imputed data.

**B2 (notebook cannot pass its own authority gate) — resolved procedurally, and the bypass is closed.** The committed notebooks are now explicitly non-executable review scaffolds (their pin `3f0337b9` fails closed at cell 03 because `review.json` is absent — safe). The executable handoff is produced by the new campaign `package-colab` stage, which requires a clean tree, a build receipt bound to the committed `review.json`, and current verification, then regenerates both notebooks pinned to the commit that contains the review receipt; only the two notebook paths may change. Critically, the clean-tree assert in the setup cell now uses `--untracked-files=all`, so the r1 untracked-`review.json` drop-in bypass no longer exists. The procedure is documented in CAMPAIGN_COMMANDS.md and PROGRESS.md. The receipt chain is non-circular: `make_receipt`/`verify_receipt` bind the original CLI response bytes plus the SHA-256 of every executable dependency (`required_files`), and committing the receipt, build receipt, verification, and notebooks touches none of those files, so the reviewed source hashes remain fixed while only the literal PIN string changes. At run time `verify_decision → verify_receipt` re-hashes all of it inside the pinned checkout and fails closed on any drift.

**B3 (broad clone staging raw patient blobs) — resolved with test coverage.** `colab_P001.ipynb` cell 01, `scripts/package_pilot.py`, and the synthetic notebook all use `git init` + a branch-scoped `remote.origin.fetch` refspec + `git fetch --no-tags --depth=1 origin <PIN>` + detached checkout with pin verification. `tests/test_pilot_notebook_acquisition.py` builds a synthetic remote with a contaminated results branch and asserts the unwanted commit's objects are absent from the acquired repository, that a repeat is harmless, and that a dirty/untracked rerun refuses nondestructively.

**Operator-flagged missing campaign route — implemented.** `scout.py:main` now wires `--campaign`/`--experiment` onto the six commands and dispatches to `orchestrator/campaign_lifecycle.py`, with `--experiment requires --campaign`, idea-number exclusion (`context()` refuses `idea is not None`), scope-guarded per-stage commits, receipted disposable-repo interpretation legs with opposing families, and follow-up gating on unchanged predecessor evidence. The r1 LOW findings are also fixed: index-first checkpoint ordering (`run.py:run`, crash between index and checkpoint now recomputes rather than poisoning the path — tested), failure cards written under `.private/failed_attempts/`, `gpu_minutes` now null with an honest `compute_method` note, `-bb0` on the 7z call, and the return validator now enforces the pinned numpy/nibabel environment instead of merely assuming it.

### Non-blocking findings

1. **MEDIUM — `orchestrator/campaign_review.py:verdict`.** The scanner takes the *last* JSON object in the response body containing `verdict` and `reviewed_commit`. It fails closed when the last object is non-APPROVE, but a response that quotes an example approval object *after* its real verdict would be mis-ranked. Bounded fix: refuse when more than one verdict-bearing object exists, or parse only the final fenced block; add a test with two conflicting verdict objects.
2. **LOW — `orchestrator/campaign_review.py:evidence`** hard-codes `scope != 'p001'` regardless of the `experiment` parameter, so P002/P003 receipts can never be minted (fail-closed, and the generator is P001-only anyway). Fix: compare against the experiment name.
3. **LOW — `orchestrator/campaign_lifecycle.py:run_isolated_stage` + rotation.** `run_agent` applies `effective_agent` rotation even to explicitly requested families; with rotation enabled on an odd `active_cycle` (read from the real repo's state file, which is not swapped), the author leg would execute as the other family and the `family_effective` check would spuriously refuse interpret-build. Fail-closed but operationally fragile; bypass rotation for campaign stages or assert rotation state first, with a synthetic test.
4. **LOW — `campaign_lifecycle.commit` / `package-colab`** use `git commit` with `check=True`; an idempotent rerun with nothing to commit raises `CalledProcessError`, which `dispatch()` does not convert into a clean refusal. Skip the commit when nothing is staged.
5. **LOW — `campaign_lifecycle.dispatch`** appends the `INTERPRETATION_FAILED` event to `lifecycle.jsonl` *after* the partial-evidence commit, leaving a dirty tree that blocks the next campaign command until manually committed.
6. **LOW — `scout.py:_mount_cell`** still catches only `SecretNotFoundError`; a defined-but-ungranted `HF_TOKEN` raises `NotebookAccessError` and crashes the legacy launcher's mount cell (r1 note; not on the P001 path, which uses no secrets).
7. **LOW — campaign `verify-probe`** passes even if numpy/nibabel are absent, because the prediction tests skip themselves; assert the pinned dependencies importable before declaring `SYNTHETIC_VERIFIED`.
8. **Doc/cosmetic:** `docs/isles-pilot/CHECKPOINT.json` remains stale (tip `68298a8`; the reviewed revision `4339928a` is absent from its commit list, and its `single_next_operator_action` is already satisfied); the dead `if policy_path.exists():` after the mandatory refusal in `scout.py:record_result` is unchanged.

### Unverified matters

No execution was possible, so: the claimed test passes (227-test suite, six-command synthetic lane, acquisition and runner/validator tests) are reported, not reproduced. Several inventoried files were not supplied and remain unreviewed: `scripts/check_pilot_publication.py` (so `orchestrator/publication.py:check_git_paths` wiring is still unverified), `orchestrator/publication_subset.py`, `scripts/efficiency_review.py`, `scripts/rehearse_047_cleanup.py`, `colab/smoke.py`, `colab/probe_mcp.py`, and most of the new test files outside the four shown. I could not hash-verify that `investigator_decision.json`'s `campaign_sha256`/`spec_sha256` match the supplied CAMPAIGN.md/SPEC.md v1.1 bytes — a mismatch would make every campaign command refuse (fail-closed), not corrupt anything. The real ISLES24 Tmax background convention (NaN vs 0) remains unknown; under v1.1 that is now an explicit stopping rule with a reviewed amendment path, which is the intended posture. GitHub fetch-by-SHA requires the pin to be pushed and branch-reachable. Finally, an inherent limit worth restating: the review execution/response evidence is written by the local harness, so the receipt chain protects against drift and accident, not against deliberate local fabrication of the entire evidence set — the operator's inspection of the committed `docs/isles-pilot/reviews/*` bytes against the original CLI output remains the backstop. None of these blocks approval of this revision.

```json
{"verdict":"APPROVE","scope":"p001","reviewed_commit":"4339928a6fe8fe4539a7adc44c501f95ba5a9351","blocking_findings":[]}
```
