## Cleanup review r2 — verdict: APPROVE (cleanup scope, revision 3f0337b986dc)

The leased rewrite remains technically suitable for operator approval at this revision, and the two Medium findings from the r1 review that mattered to the operation itself are now genuinely fixed: `scripts/rehearse_047_cleanup.py` uses explicit `require(...)` raising `ValueError` (so integrity checks survive `python -O`), it asserts the exact OLD→NEW change set via `git diff-tree --name-status --no-renames` (`changes==expected`: 198 `staged/` deletions + `probe_exclusions.csv` deletion + one failure-console addition, nothing else), and `tests/test_cleanup_rehearsal.py` exercises both the happy path and a `-O` refusal on a synthetic repo. The projection arithmetic is internally consistent (215 = 17 + 198; 16 retained = 17 − 1 quarantined; retained blobs compared by git blob identity; parent pinned to `b652005f`; failure console byte-compared). The push command is a single-ref, expected-value leased force push with the mandatory lease; no code path in the revision can execute it. Determinism of `c8124212…` follows from the fixed tree, parent, author/committer identity and dates, so the optimized re-rehearsal reproducing the same hash is expected, not merely claimed. Downstream remains fail-closed: the 047 policy is `blocked`, `record_result` refuses on it before the subset path is reachable, and `publication_subset.verify` refuses non-`staged/` exclusions — so approving this cleanup provably does not unlock a 047 import.

**On the explicitly requested point — retained identity-linked metadata:** the audit (`docs/isles-pilot/reviews/047-retained-metadata-audit.json`) establishes that this cleanup is a raw-payload removal, not anonymization. `per_case_staging.csv` (99 rows, 495 identifier occurrences, per-case `*_data_row_usable` and `*_anomalies` columns), `staging_audit.json` (594 occurrences), and `split_manifest.csv` (case_id + stratum) remain on the public ref after the rewrite. That means the privacy rationale for quarantining `probe_exclusions.csv` — identity-linked file-anomaly disclosure — plausibly applies to information still present in retained files, including for the same malformed-file case. I still judge this non-blocking, for three reasons: the rewrite is strictly content-reducing (all 16 retained files are public today in the contaminated commit, so executing it adds no exposure and removes the worst); the retained columns carry usability/anomaly bookkeeping, not clinical values, per the audit; and removing more files without the operator's exclusions-semantics ruling would exceed delegated authority and is exactly what the safeguards forbid. Blocking would only prolong exposure of 198 raw phenotype files while the identifier-bearing files stayed public regardless.

### Findings

1. **Medium — quarantine asymmetry / second-rewrite risk. `docs/isles-pilot/047_LIFECYCLE.md` (proposed decision 2) vs the retained tree.** Decision 2 as written adjudicates only the exclusions audit; an adverse ruling on identity-linked anomaly metadata would logically extend to `per_case_staging.csv`/`staging_audit.json`/`split_manifest.csv` and force a *second* force push of the same ref (new lease, second clone invalidation). Failure mechanism: operator approves the cleanup, later rules against identity-linked anomaly metadata, and the ref must be rewritten again. Bounded fix: amend decision 2 to name the three identifier-bearing retained artifacts explicitly and let the operator choose knowingly between (a) approving now and accepting a possible second rewrite, or (b) ruling on all identity-linked metadata first and folding any further removals into one re-rehearsed projection. Documentation change only; CLEANUP_REVIEW.md's continuation section already states the substance, but the formal decision list is where the ruling will be made.

2. **Medium — stale audit receipt (persisting r1 finding 3). `docs/isles-pilot/CHECKPOINT.json`.** The recorded tip `68298a87…` and 56-artifact manifest do not cover the reviewed revision `3f0337b9…`; every commit since — including the hardened rehearsal script, its test, and the committed review artifacts — is outside the recorded contaminated-ancestry and per-artifact attestation. This does not touch the cleanup pins (they are independent of the branch tip). Fix: re-run `scripts/check_pilot_publication.py --tip 3f0337b986dc…` and refresh the checkpoint before any further pilot-branch push.

3. **Low — `scripts/rehearse_047_cleanup.py:rehearse`: two residual nits.** The `git init`/`fetch` calls use `subprocess.run` without the 120 s timeout applied to every other git call; and the receipt's `proposed_command` hardcodes the production remote URL even for synthetic runs, so a fixture receipt names the real repository. Fail-safe in practice (a synthetic OLD sha can never satisfy the lease against the real remote), but parameterize the remote or stamp synthetic receipts as such. Test: extend the existing synthetic test to assert the receipt's remote matches a passed-in parameter.

4. **Low — r1 advisory findings unaddressed at this revision (all fail-closed, listed so they are not silently dropped):** `orchestrator/campaign.py:verify_decision` still has an open key schema for approval forgery (`operator_approved: true` passes) and does not bind the review to the decision document; `orchestrator/publication.py` and `publication_subset.py` git calls still lack timeouts and a policy missing `allowed`/`required` still surfaces as a `KeyError` traceback through `record_result`; `publication.check_git_paths` remains unreferenced by any caller or test; `check_pilot_publication.py` raw-artifact detection still keys on path/extension heuristics only; one `publication.json` per probe still cannot serve 047's two contract pins. None affects the cleanup ref operation.

5. **Info —** the replacement commit will carry the fixed public identity "scout cleanup rehearsal / 2026-09-05T12:00:00Z" (the price of hash determinism), and re-validating the remote ref state immediately before executing the push remains mandatory; the lease enforces it mechanically.

### Unverified matters (outside supplied material; execution disabled)

- The deterministic hash `c8124212…`, both rehearsal receipts, `git bundle verify` results, the 11-ref/173-blob reachability audit, and the claim `replacement_reaches_raw_phenotype_blobs: 0` **including the parent history** `b652005f` (if the failure-state parent tree carried any staged file, the cleaned branch would still reach it — the checkpoint asserts it does not, but I cannot reproduce that).
- The retained-metadata audit's counts and identifier-detection method: the JSON reports occurrences and columns but not how an "identifier occurrence" was matched; free-text anomaly columns could carry indirect identifiers beyond the counted pattern. The ruling in finding 1 should be made on the actual bytes, which are correctly not supplied here.
- Whether commit `3f0337b986dc…` contains exactly the supplied bytes; test counts (220/226/227); the checkpoint manifest SHA.
- Roughly half the change inventory was not supplied (P001 runner/validator/notebooks, `probes/047/run.py`, the generated 047 notebook, `ideas/047` state/card, `efficiency_review.py`, `package_pilot.py`, README, the `*.execution.json`/`*.response.json` receipts, several tests) and is not covered by this verdict; the P001 REVISE findings from r1 are a separate track.
- Colab execution/retrieval remains unverified, as the checkpoint itself records.

Approval here is of the supplied revision's cleanup operation only: it is a publication disposition, not scientific acceptance, and the three open operator decisions (Phase-A registry attestation, exclusions/identity-metadata disposition, successful-console recovery) remain open and are not resolved by this review. Do not execute the push without the operator's explicit go and a fresh remote-pin revalidation.

```json
{"verdict": "APPROVE", "scope": "cleanup", "reviewed_commit": "3f0337b986dc52594f4347fdc7d99bb3e7c4a0a2", "blocking_findings": []}
```
