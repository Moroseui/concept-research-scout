## Cleanup review r3 — verdict: APPROVE (cleanup scope, revision 4339928a6fe8)

The leased rewrite is unchanged from the r2-approved revision and remains technically suitable for operator approval: same single-ref, expected-value leased force push (`--force-with-lease=refs/heads/results/probe-047-dc586665d0be:940293b6…`, replacement `c8124212…`, parent `b652005f…` unchanged), same hardened `scripts/rehearse_047_cleanup.py` with optimization-safe `require(...)` checks, exact OLD→NEW path-set verification (198 `staged/` deletions + `probe_exclusions.csv` deletion + one failure-console addition, nothing else), byte-identical retained blobs, and synthetic test coverage including the `-O` refusal (`tests/test_cleanup_rehearsal.py`, supplied and sound). The projection arithmetic stays internally consistent (215 = 17 + 198; 16 retained = 17 − 1 quarantined). Determinism of `c8124212…` follows from the fixed tree, parent, identity, and dates, so the reported unchanged reproduction is expected. No code path in the revision can execute the push. Cleanup remains a publication disposition, not scientific acceptance, and not anonymization.

Two prior findings are genuinely resolved at this revision. First, r2 finding 1: `docs/isles-pilot/047_LIFECYCLE.md` now carries the "Cleanup review r2: scope of the metadata disposition" section naming `per_case_staging.csv`, `staging_audit.json`, and `split_manifest.csv` in the formal decision scope and presenting the operator's explicit choice (approve now and accept a possible second rewrite, or rule on all identity-linked metadata first and re-rehearse once). Second, r1 finding 7: `orchestrator/campaign.py:verify_decision` now enforces a closed key schema (any unlisted key, including `operator_approved`, refuses), and `orchestrator/campaign_review.py` binds `decision_sha256` plus per-file hashes of every executable dependency into the review receipt, verified against the actual completed Fable response (`claude-fable-5` required in `modelUsage`, response integrity hashed).

**On retained identity-linked metadata (restated for the record):** executing this rewrite removes raw phenotype payloads and the exclusions audit only. Case identifiers with per-case file usability/anomaly bookkeeping remain public in the three named files (495/594/99 occurrences per the committed audit). This is NOT anonymization. I continue to judge that non-blocking: the rewrite is strictly content-reducing, the retained columns are bookkeeping rather than clinical values per the audit, and removing more without the operator's ruling would exceed delegated authority. The decision is now correctly in front of the operator.

### Findings

1. **Medium — generated 047 launcher: the staging sweep cell can never pass. `probes/047/colab_probe_047.ipynb`, cell beginning `SUFFIXES = ['.nomatch']`.** The selective extraction `-ir!*.nomatch` matches nothing in the archive, so either the `7z` return-code assert or, on any path, `assert _n >= 800` fires — deterministically, on fresh and resumed runtimes alike — halting Run All *after* the ~99 GB fetch cell and before the runner cell. This misuses the generator's own archive-only lane: `scout.py:_staging_cells` explicitly omits the extract cell when suffixes are empty ("the take-13 >=800-file floor must never gate a deliberately empty sweep"), and 047's `run.py` performs its own selective extraction from `--archive-file`. `TestLauncherRunnerPassthrough` enshrines the `.nomatch` pattern without ever executing the cell. Fail-closed (no data or safeguard risk; 047 execution is policy-blocked anyway), but the shipped launcher is non-functional by construction and burns a full archive download per attempt. Fix: regenerate via `package-colab` with empty `--staging-suffixes` (pin+fetch only) and add a test asserting the 047 notebook contains no extraction-floor cell. While regenerating, revalidate the pin: the notebook clones `194cdcd…` and its export cell needs `probes/047/publication.json` and `orchestrator/publication.py` present in that tree — if the policy landed in a later commit, the export cell refuses even after a successful run (also fail-closed, but confirms the pin needs refreshing).

2. **Low — CHECKPOINT.json remains stale (persisting from r1/r2), now explicitly designated historical. `docs/isles-pilot/CHECKPOINT.json`.** The committed receipt still attests tip `68298a87…` (10 commits, 227 tests). The reported fresh read-only audit at `4339928a…` (13 commits, 232 tests) has no committed receipt in the supplied revision, so it stays unverified here. Acceptable given the stated plan (refresh follows final packaging), and irrelevant to the cleanup pins — but commit the refreshed audit output with or before the packaging push so the in-repo attestation matches the pushed tip.

3. **Low — rehearsal script residual nits persist unchanged (r2 finding 3). `scripts/rehearse_047_cleanup.py:rehearse`.** The `git init`/`fetch` calls still lack the 120 s timeout applied to every other git call, and the receipt's `proposed_command` still hardcodes the production remote URL for synthetic runs. Fail-safe in practice (a synthetic OLD sha cannot satisfy the lease against the real remote).

4. **Low — the new sibling-staging naming evades the outgoing audit's substring heuristics. `probes/047/run.py:make_smoke_inputs`/`run` vs `scripts/check_pilot_publication.py:audit`.** Staged inputs now land in `<OUTPUT_DIR>.private-staged` (the correct root-cause fix — staged raw files can no longer ride inside an exported bundle), but a path like `probes/047/results_v2.private-staged/x.csv` matches neither the `/staged/` nor the `.private/` raw-artifact checks. The path allowlist is the effective guard (such a path refuses as "unpermitted changed path"), so this is defense-in-depth only: add a `.private-staged`/`-staged/` pattern to the heuristics.

5. **Low — r1/r2 advisory items still open (all fail-closed, listed so they are not silently dropped):** `orchestrator/publication.py` and `publication_subset.py` git/subprocess calls lack timeouts; a policy missing `allowed`/`required` surfaces as a `KeyError` traceback through the non-campaign `record_result` (which catches `ValueError` only); `publication.check_git_paths` remains unreferenced by any caller or test; `check_pilot_publication.py` raw-artifact detection remains path/extension-keyed; one `publication.json` per probe still cannot serve 047's two contract pins; `export_session`'s console credential scan omits the `sk-` pattern that the outgoing audit scans for.

### Newly supplied surfaces, assessed

- **Campaign import lane** (`orchestrator/campaign_lifecycle.py`, `campaign.py`, `campaign_review.py`, `scout.py` dispatch): no publication bypass found. Campaign mode requires explicit `--campaign`/`--experiment` and refuses numbered ideas; every stage requires the verified delegated decision and the opposing-family receipt; imports pass through `copy_verified` under the experiment's own policy; `commit()` refuses any changed path outside the experiment's allowed artifact set before staging; isolated agent stages strip `ANTHROPIC_API_KEY`/`OPENAI_API_KEY` from the environment; failed interpretation legs preserve evidence and refuse. Approving the cleanup provably does not unlock a 047 import: the blocked policy refuses in `record_result` before the subset path is reachable, and `publication_subset.verify` refuses non-`staged/` exclusions, so the cleaned 16-file tree has no import lane without a separate operator ruling plus a policy/contract amendment.
- **Efficiency/MCP material**: no accidental execution or data export. `scripts/efficiency_review.py` reads receipts and writes a local proposals file, applying nothing (test confirms input bytes unchanged). `campaigns/isles24-pilot/colab/probe_mcp.py` only handshakes a locally spawned server and explicitly reports execution/retrieval unverified; `smoke.py` writes synthetic bytes under `/tmp`.
- **Changed orchestration tests**: the launcher assertions correctly flipped (no `SCOUT_RESULTS_PAT`, no `git('push'`, no `-delete` scrub, `run_logged`/`export_session` present), and the now-mandatory per-probe publication policy has regression coverage through the transport, M4, and R3b historical-lane import tests.
- **047 derived views** (`ideas/047/state.json`, `CARD.md`, `registry.yaml`): faithfully show the registry as UNRATIFIED with Phase A STALE (historical pin unattested) and Phase B BLOCKED, with the DRIFT note preserved; no authority is manufactured and the historical Phase-A ratification hashes are unchanged.

### Unverified matters (outside supplied material; execution disabled)

- The deterministic hash `c8124212…`, both rehearsal receipts, `git bundle verify` results, the 11-ref/173-blob reachability audit, and `replacement_reaches_raw_phenotype_blobs: 0` including the parent tree `b652005f` (if the failure-state parent carried any staged file, the cleaned branch would still reach it; asserted, not reproducible here).
- The reported read-only outgoing audit at `4339928a…` (13 commits, 232 tests) — no committed receipt supplied; and whether commit `4339928a…` contains exactly the supplied bytes.
- Not supplied and therefore outside this verdict: `scripts/package_pilot.py`, `campaigns/isles24-pilot/CAMPAIGN.md`, all P001 experiment artifacts and both pilot notebooks (P001 is under its separate simultaneous review), the `*.execution.json`/`*.response.json` receipt contents, `tests/test_campaign*.py`, `test_prediction_p001.py`, `test_pilot_notebook_acquisition.py`, `CAMPAIGN_COMMANDS.md`, `COLAB_MCP.md`, `EFFICIENCY_PROPOSALS.json`, `REVIEW_REQUEST.md`.
- Presence of `orchestrator/publication.py` and `probes/047/publication.json` in the tree at launcher pin `194cdcd…`; the durable private evidence at the stated owner-only path; Colab execution/retrieval.
- The retained-metadata audit's identifier-matching method (counts and columns are reported, not the detection rule); the operator's ruling should be made on the actual bytes, which are correctly not supplied here.

Approval covers the supplied revision's cleanup operation only. The three operator decisions (Phase-A registry attestation, exclusions/identity-metadata disposition, successful-console recovery) remain open and are not resolved by this review. Do not execute the push without the operator's explicit go and a fresh remote-pin revalidation; the lease enforces the pin mechanically.

```json
{"verdict": "APPROVE", "scope": "cleanup", "reviewed_commit": "4339928a6fe8fe4539a7adc44c501f95ba5a9351", "blocking_findings": []}
```
