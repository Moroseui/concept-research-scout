name: results-validate
# Historical main implementation is unchanged remotely. This pilot copy cannot
# read a raw results branch or open a main PR. Use campaign validate/import.
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  quarantined:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'BLOCKED: raw results branch lifecycle is outside pilot authority.'
