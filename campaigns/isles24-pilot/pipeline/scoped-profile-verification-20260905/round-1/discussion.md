# P001 bounded verification discussion

P001 results are still pending. The supplied P001 v1.1 specification records opposing-family review as pending; no original run return or completed review evidence is supplied here. This document records the existing evidence gates for the scoped unattended profile. It is a bounded discussion only, not approval, execution authority, an executable amendment, or a new scientific proposal. No human ratification is requested or recorded. All scientific work remains exploratory.

The supplied campaign retains the initial operator-connected Colab notebook and manual Run All requirement. Describing this discussion as unattended does not alter that execution boundary. The existing requirement for an agent-attributed decision binding exact specification bytes and opposing-family review of specification and executable implementation remains in force before a real run.

## Archive integrity gates

- Preserve the cohort table SHA-256 `aba525122f796618761e6c4d29b664647760e8dff4987932c3ff6ab5456faae9` and archive member manifest Git blob `edb9a8c2ceb90df214cdd7ec167f0b1e8c858bb2`. Read only `case_id` from the pinned contribution table, discard other columns, sort lexically, and require exactly 99 unique eligible IDs.
- Select exactly one admission NCCT-space Tmax derivative and one canonical ses-02 NCCT-space lesion derivative per eligible ID by full manifest path. Missing or ambiguous selection fails; outcome content cannot resolve ambiguity. Noncanonical duplicate lesions are not staged.
- Extract only the selected 198 members. No whole-archive extraction; the 49 reserved cases must not be selected, extracted, opened, trained on, or evaluated.
- If using `train.7z`, first verify exactly 99,014,629,647 bytes and MD5 `36ae28b9a17f7340b8bbef62b595cb57`.
- Verify every selected compressed file against its recorded size and CRC32 before image opening. The supplied implementation also records input SHA-256 values and rejects staged paths escaping the private root or selected-file symlinks. Reused staging must pass member verification again.
- Receipt archive verification and extraction separately; allow at most one staging invocation per new output path. Integrity or scope failure stops the run and preserves original console output and completed patient checkpoints.

## Original-return evidence gates

- Retain the original console for audit, including failure output and partial checkpoints. Reviewed console is supplied separately from the publication bundle. A failed or blocked attempt is not a negative scientific result; scientific claims stop until invalid runs are resolved.
- Require evidence binding the returned artifacts to the exact specification, code, cohort, member manifest, and review. Reruns may reuse only byte-verified checkpoints with identical specification/code/input bindings; the supplied implementation checks checkpoint bytes, prediction hashes, and input hashes.
- Completion requires exactly 99 evaluated cases, without additional exclusions. Preserve the fixed admission Tmax > 6.0 seconds baseline, prediction persistence before label image opening, finite 3D inputs, binary labels, positive voxel volume, and matching shape/affine with absolute tolerance `1e-5` and `rtol=0`. Nonfinite Tmax anywhere, geometry disagreement, unit implementation failure, or output identity failure stops the run. No silent imputation, lesion-dependent masking, or post hoc resampling is authorized.
- Preserve arithmetic mean per-patient volumetric Dice as primary metric, equal patient weighting, the specified empty-mask conventions, and the secondary descriptive volume-error and median-Dice metrics. Preserve 2,000 patient bootstrap resamples, seed `20260905`, and the central percentile 95% interval. Report inherited Tmax units as an assumption and the reused development cohort as exploratory, without external-validation or clinical claims.
- Require the complete permitted aggregate return: `summary.json`, `resolved_config.json`, `environment.json`, `execution_receipt.json`, and `RESULT_CARD.md`. These are both the allowed and required publication files in the supplied publication manifest. Check aggregate outputs and the result card against the original evidence before treating them as a valid completed return.
- The receipt must account for execution duration and separately recorded staging time under the existing one-CPU-Colab-run, 60-analysis-minute cap. Record failures, machine/agent identity, and available usage as required by the campaign; unavailable cost, token usage, and human intervention measurements remain null, never estimated as observed facts.
- Keep selected-input manifests, patient IDs/paths, per-case metrics, predictions, masks, staged inputs, and private checkpoints out of the Git publication bundle. Any later permitted publication remains subject to outgoing-history inspection and existing campaign publication restrictions; this discussion authorizes no publication or remote write.
- Baseline evidence must be returned and inspected before selecting any follow-up. The campaign envelope remains one baseline and at most two separately specified and reviewed comparisons. No follow-up, scientific change, or amendment is proposed here.

These gates are listed from the supplied specification, campaign, publication manifest, and implementation; they have not been verified against patient inputs or an original execution return. No patient data or original repository was sought, and no experiment was executed. Original experiment pins and scientific choices remain unchanged.
