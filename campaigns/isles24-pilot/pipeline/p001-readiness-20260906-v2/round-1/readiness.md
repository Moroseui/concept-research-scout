**Question:** Is externally seeded P001 ready to become the minimal admission-imaging prediction reference under the selected proposed prediction charter?

**Evidence:** The supplied fixed P001 v1.1 specification, runner and source-bound Claude APPROVE support a coherent exploratory benchmark. The charter packet is `REVIEWED_PROPOSAL_NOT_ADOPTED`. P001 has no real return; current input access is unverified.

**Limitations:** Tmax seconds are inherited, performance is unknown, and development outcomes were previously used. Treatment-dependent labels, registration and cohort selection limit inference. Historical input verification and synthetic operation do not establish current patient-run readiness.

**Next decision:** Recommend **ADOPT_CONDITIONALLY**, preserving P001 unchanged as a minimal reference. Resolve the evidence and route gates below before any separate adoption or launch decision. This packet supplies neither ratification nor launch authority.

The companion `launch_decision.proposed.json` records exact transcribed source pins, the full supplied historical P001 review inventory, selected proposal identities and open gates. This assessment uses only supplied excerpts and the disposable directory's prompt/state; original source bytes were not rehashed. No patient access, scientific execution, remote write, new source search or human ratification occurred.

Scientific adequacy and prospective linkage

P001 predicts the released binary MRI-derived follow-up infarct mask in NCCT space after observed treatment, using only the admission Tmax map available by completion of admission CT. The question is spatial agreement and volume error over the full released grid. A transparent threshold reference is useful before investing in a fitted predictor. A valid poor result would establish limitations of this fixed rule on these 99 cases; it would not establish that admission imaging is uninformative or learned prediction cannot work. No competitive-performance, novelty, mechanism, causal, untreated-infarct or clinical-use claim is supported.

Conditional adoption is justified because the design can answer this narrow question and no verified contradiction presently requires changing its scientific bytes. Rejecting it solely because it is not trained would miss its reference value. Contradictory unit evidence or a requirement to repair nonfinite inputs/geometry would instead require a separately identified amendment and fresh review. No amendment is implemented here.

The explicitly selected assessment frame is `isles24-prediction/proposed-v1`, with `isles24-prediction-rubric/proposed-v1` and `isles24-prediction-prompts/proposed-v1`, under `campaigns/isles24-pilot/pipeline/prediction-charter-20260906-v1/round-1/`. Their exact SHA-256 identities and the adoption/review identities are in the companion packet. Selection for this preview does not install them. A later prospective linkage must bind final charter, rubric, prompt guidance, adoption and adoption-review bytes to the unchanged P001 spec/code/decision/review. Preserve the external seed and operator delegation; system authorship applies to this assessment, not P001 code.

For this prediction assessment, the legacy mandatory “model is using X” claim and mechanism-only fields are inapplicable. Use target, admission cutoff, prediction rule, leakage controls and patient evaluation instead. The scoped prompt proposal does not globally modify legacy prompts, schemas, charters or historical scores. No numeric scores were requested. If scored later, use the proposed canonical `{value, why}` shape, prediction-specific identifiability, its weights and caps; report evaluation readiness and regret separately. Current compatible patient access/unit readiness is NOT_INSPECTED, while the supplied historical review is attributed evidence. Sealed outcomes are not a design-readiness defect, and specification quality cannot substitute for result-phase evidence.

Frozen contract and input packet

- Use only the pinned cohort table's `case_id` column, sorted lexically, exactly 99 unique IDs; never use contribution values/ranks. The 49 reserved cases remain untouched. These constraints do not account for all 149 released cases or authorize any remainder.
- Select exactly 198 compressed members using the pinned archive manifest: one `train/derivatives/{case}/ses-01/perfusion-maps/{case}_ses-01_space-ncct_tmax.nii.gz` and one `train/derivatives/{case}/ses-02/{case}_ses-02_space-ncct_lesion-msk.nii.gz` per eligible ID. Missing/ambiguous matches fail. No duplicate lesion staging or whole-archive extraction.
- Verify selected compressed member size/CRC before image opening; retain SHA-256 input bindings. When using the archive, require 99,014,629,647 bytes and MD5 `36ae28b9a17f7340b8bbef62b595cb57`. A staged-root route still requires every selected member's verification.
- Predict finite 3D Tmax strictly `> 6.0`, with no fitting, masking, tuning or filtering. Any nonfinite voxel, including background, stops the run. Write each prediction before opening that case's label for evaluation. Integrity hashing of compressed label bytes is distinct from opening label arrays.
- Require finite binary 3D labels, positive voxel volume, matching shape/affine with atol `1e-5`, rtol `0`; no resampling or extra exclusions. Exactly 99 evaluations constitute completion.
- Primary: equal-patient arithmetic mean volumetric Dice, both empty = 1, exactly one empty = 0. Secondary: median Dice, mean absolute volume error in mL and mean signed prediction-volume error. Use 2,000 patient bootstrap resamples, seed `20260905`, central percentile 95% interval. This interval reflects within-cohort resampling, not external or causal validation.
- Preserve one CPU Colab run, at most 60 analysis minutes, separately receipted staging and at most one staging invocation per new output path. Reuse only byte-verified checkpoints with identical spec/code/input/review bindings. Keep every failure and original console; an invalid run is not a negative result.

Historical Drive verification was a mounted-destination read-back on 6 September 2026 at 00:34:31 UTC, at `/content/drive/MyDrive/isles-pilot/archive-preservation-ec3e76e9b91f40ca847b490f9b5f7bfa/train.7z`, matching the archive size/MD5 above. It was not an independent provider durability API check. Availability, authentication, runtime state and current integrity have not been checked here. Old runtime-local `/content/train.7z` and `/content/isles-p001-input-16813698/train.7z` are not durable-input evidence. A new download is not the default next step.

Tmax-unit evidence and minimal checks

`SPEC.md` explicitly inherits the prior program's Tmax convention; `run.py` loads the NIfTI data through nibabel into float32 and compares to 6.0. The supplied runner does not independently establish that the resulting numeric values are seconds. A filename, plausible value range or prior threshold implementation cannot settle that provenance question. Header time units alone need not describe the scalar Tmax voxel values.

First, in separately permitted desk work, seek a claim-specific primary dataset/derivative or generating-pipeline statement about numeric Tmax units and scaling. Record exact source/version/excerpt and distinguish raw storage from decoded values. No such check or literature search is claimed here. The supplied metadata receipt confirms source identities but leaves full-text timing and units unresolved; the spec's MRI-at-2–9-days claim remains attributed. Annotation, license/access and official evaluation details also need claim-specific support before stronger assertions; do not substitute a newer release.

After separate patient-access authorization, the minimal practical input checks would be current archive identity, exact metadata selection, compressed size/CRC, and eligible admission Tmax header/scaling consistency against that documentation. Any limited eligible-header check would diagnose provenance only; it would not certify all 99 inputs. The unchanged runner must still enforce all-case finite/geometry checks, with label geometry/content inspected only in evaluation after prediction persistence. Do not inspect reserved cases, labels or outcome distributions to infer units; do not optimize a conversion or threshold for Dice. Unresolved units remain an explicit evidence gate for the next disposition. A contradiction stops unchanged launch and requires a separately reviewed amendment, not silent conversion.

Backend comparison and dispatch gate

| Route | Existing evidence | Remaining conditions |
|---|---|---|
| Manual CPU Colab, preferred conditional route | Reviewed scientific source/notebook; synthetic remote execution and retrieval reported via Claude Code; existing Colab entitlement | Authorized CPU reconnection and consented Drive mount; explicit ARCHIVE parameter; current input verification; exact source/dependency checks; dispatch reconciliation; separate launch disposition |
| Linux patient transport/execution | Purchased host, authenticated model accounts, protected synthetic jobs and bounded model discussion reported | Concrete private transfer/storage/return design and capacity evidence; backend review/amendment for replacing frozen Colab contract; affected-surface opposing review; separate transfer/launch authority; unattended activation evidence if that mode is used |

Preserve original snapshot `0770c7dcabe781cbfb87de505755e7aafa758f2e`, scientific source `d6a1184b4378e849213fd887a6f7b103fb1a64d5` and notebook `1a81c037343598f4e4585153b11d761b87a9ae3a`. Historical P001 APPROVE binds reviewed commit `469d29df002ea78f64146731244769d7c82330d6` and its file inventory. Preserve both the earlier pending-review decision bytes and subsequent approval. Changed scout dependencies at HEAD require fresh review; changed publisher dependencies also require affected hosted review. No old approval automatically covers a new tree.

A Linux proposal must identify source/destination, authorized private access, immutable source snapshot, hash verification, selective staging, disk headroom for archive/staged files/checkpoints, CPU/time limits, original console capture, failure retention, private return, deduplication and responsible single writer. These are unverified design requirements, not new transport code or an executable amendment. Existing authentication does not authorize patient transfer. No new spending, credentials or provisioning follows from this proposal.

The coordinator is reported BLOCKED with zero retries and an open ambiguity inbox. Before either patient route, reconcile the job store, remote processes, output paths and checkpoint bindings, then deliberately resolve the hold under appropriate authority. NOT_VISIBLE is not proof of absence; do not blindly Run All or automatically retry. No reconciliation was performed here.

Existing return, validation, recording and interpretation routes

The supplied context supports the following exact file/entry-point identities, but does **not** include validator/import/interpretation CLI argument definitions. This packet therefore does not pretend to supply verified executable commands for those stages. Obtaining their approved source/help in a future authorized environment is a concrete handoff gap; no original repository is sought here.

1. Metadata selection: `campaigns/isles24-pilot/experiments/P001/run.py --preflight` checks pinned cohort/manifest selection and reports 99/198 with zero payloads opened. It exits before `verify_decision`; it is not a launch authorization or runtime/unit check. Nothing was run here.
2. Execution binding: the real runner calls `orchestrator.campaign.verify_decision` on campaign/spec/decision/review/code before real payload access. Exact dependency review and route authorization must also hold; preflight success cannot replace them.
3. Private return and package: preserve original console, failed-attempt history, private binding, selected input hashes, checkpoint index, predictions and per-case metrics outside Git. Use the existing reviewed `scripts/package_pilot.py` / `orchestrator/publication.py` route subject to its source binding and publication checks. P001's required/allowed aggregate bundle is exactly `summary.json`, `resolved_config.json`, `environment.json`, `execution_receipt.json`, `RESULT_CARD.md`. Review even aggregate files for IDs/paths before publication; console is separately supplied privately for audit.
4. Validation: the named P001-specific validator is `campaigns/isles24-pilot/experiments/P001/validate_return.py`, pinned SHA-256 `c021d2c8cce8f2e1c92df88108eb93ba473d05b0990233532060aeab82a0d9db`. Required acceptance evidence includes exact bindings, five-file completeness, exactly 99 valid evaluations, metric/uncertainty consistency and original private evidence/failure audit. Its implementation is not supplied, so do not assume every requirement is automatically enforced by it. A synthetic lifecycle demonstration is not validation of a real P001 return.
5. Recording: use the existing `scout.py` `record-result` lifecycle route only after validation and publication-policy passage, with the actual source identity. Do not invent a P001 destination; supplied documentation describes a bundle-name plus blob-prefix rule. The supplied optional `record-result --publication-subset DECLARATION.json --expected-blob BLOB --source-commit SHA` route verifies full source/file inventory and explicit hash-bound dispositions to available private originals; its initial exclusion support is only `staged/`. It is not a general permission to remove required science/audit evidence or expose P001 private files. Ordinary verbatim import semantics remain intact.
6. Interpretation: after valid recording, use the existing campaign interpretation/review lifecycle in `scout.py`, `orchestrator/campaign_lifecycle.py` and `orchestrator/campaign_review.py`. The supplied excerpts do not establish its exact subcommand syntax. Bind interpretation and opposing-family review to the real result/source artifacts. Discuss Dice uncertainty, volume bias, failures and residual unit/treatment/selection limits before any follow-up decision. There is no current P001 import or interpretation to launch or claim.

Core validity alone is insufficient: the supplied 047 example passed its historical core validator but was refused at publication/import policy. That illustrates gate separation, not a P001 dependency. Preserve 047 Phase A, original approvals, Phase-B evidence gaps and pending related findings unchanged; no 047 ratification, acceptance, missing console or cleanup decision is a prerequisite for P001. No pending 047b finding is used as a conclusion here.

Supervised work and Wednesday critical path

Desk assessment, proposal review, source-gap inventory, prediction-scoped schema/prompt compatibility discussion, and a concrete backend/input checklist can proceed before unattended activation. Existing authorized supervised non-patient validation/review work can also proceed in its appropriate environment; this disposable task performs only documentation. A separately authorized manual patient route can proceed after its own scientific/input/dispatch gates, without inventing a dependency on unattended hosted operation.

Protected shared 48/96 admission is conditionally selected, not active. Writer/reset design approval, initialization, hosted verification, recurring reporting and 24–48-hour laptop-disconnected observation remain operational work for unattended activation; reset remains operator-only. These gates neither authorize spending nor prevent desk science. Single-writer and immutable-snapshot discipline remain applicable.

The target is Wednesday, **9 September 2026**. The dependent path is: resolve prospective charter/adoption disposition and unit/source evidence; establish exact execution snapshot and affected review; establish authorized current input/runtime access; reconcile held dispatch/process/checkpoints; obtain separate launch disposition; execute only through that authorized route; privately return originals plus permitted bundle; validate and record; then produce reviewed exploratory interpretation. Desk review and source-gap work can progress together. No durations or completion guarantee are available. If launch or return gates remain closed, Wednesday's honest deliverable is the reviewed readiness packet with named open gates, not an invented baseline metric. Follow-ups remain unselected within the existing maximum of two.

Evidence attribution and remaining gaps

All historical assertions above are attributed to supplied `P001/{SPEC.md,run.py,build_receipt.json,investigator_decision.json,review.json,publication.json}`, `CAMPAIGN.md`, `CURRENT_STATUS.md`, `047_LIFECYCLE.md`, the prediction direction/primary-source receipts and the selected charter packet. Later status receipts supersede older pending prose without rewriting it. Named onboarding and deployment-review originals are unavailable; their absence remains disclosed. The public-source receipt reports previous checks of Zenodo record 16813698 and arXiv identities 2408.11142 / 2408.10966; this work does not repeat or expand those checks. Runtime, patient metrics, intervention minutes, token usage and cost are unavailable, recorded as null in the packet. Approval of this proposal would still be neither adoption nor patient launch.
