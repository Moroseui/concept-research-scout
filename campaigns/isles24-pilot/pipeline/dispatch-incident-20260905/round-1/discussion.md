# P001 bounded execution-incident discussion

This document records an execution incident and a bounded proposal for discussing transport repair. It is not an approval, an executable amendment, a dispatch instruction, or a request for human ratification. It introduces no new science and changes no experiment or approval bytes. The account uses only the supplied context; no patient data, original repository, private protocol, or remote state was accessed. All scientific work remains exploratory.

## Incident and evidence boundary

Acquisition now reports `VALIDATED`. The persistent coordinator attempted dispatch from unchanged snapshot `0770c7dcabe781cbfb87de505755e7aafa758f2e`. The worker returned `FAILED/OTHER` before any Colab tool calls; its only tool was `StructuredOutput`. Available usage identifies `claude-haiku-4-5-20251001` and `claude-opus-5`, despite the requested `claude-fable-5`. Record this as an execution-provenance discrepancy, without inventing a routing cause or treating the requested model as the actual worker identity. Exact tokens, cost, duration, and human intervention time are unavailable here and remain null.

The worker objected to unreadable nested escaped exec wrappers, detached private console capture that prevented its observation, and an archive parameter differing from the notebook default. These are the worker's reported objections, not independently established defects in the scientific implementation. Existing adapter approval and scientific approval still verify according to the supplied incident account. The refusal does not establish that approval was absent or invalid; the historical pending-review wording in the supplied specification does not override that reported verification.

This was a worker refusal, not an automatic permission-review rejection. There is no supplied evidence of such an automatic rejection, and none is asserted. The original protocol remains private and is not reconstructed here.

The coordinator opened `AMBIGUOUS_DISPATCH_CHECK_REMOTE_BEFORE_RETRY` and did not retry. The worker trace supports that this worker made no Colab calls; it does not by itself reconcile all remote state surrounding the dispatch. Preserve the ambiguity hold. This discussion performs no remote check and authorizes no retry. Acquisition validation is not analysis completion. There is no evidence here that P001 ran, produced metrics, or yielded a scientific result; this incident is not a negative scientific finding.

## Why private console capture must remain

Original console output can contain patient identifiers or paths, archive member names, and diagnostic details. P001 requires the actual console and any partial checkpoints to be retained for audit, while patient-level artifacts remain private. Removing capture, exposing the raw console to a general transcript, or deleting evidence would undermine those requirements. The worker's observation concern therefore calls for a reviewable observation mechanism that preserves private capture.

A future transport design could retain complete stdout/stderr privately while exposing only a reviewed, patient-free status channel: launch acknowledgement, execution state, exit status, elapsed time, and non-patient artifact integrity references. That channel must distinguish running, failed, completed, and unknown states without claiming that a detached launch has completed. Raw private evidence must remain available through the existing restricted audit process; a filtered status view cannot replace it. Do not print patient data or raw private console content to solve observability.

## Bounded transport-repair proposal

A separately reviewed repair would require a readable transport artifact with explicit arguments and ordinary control flow, replacing the nested escaped wrappers without changing the pinned scientific payload. Review would need to cover the exact transport bytes, argument resolution, private console destination and retention, observation behavior, exit-status propagation, and safeguards against duplicate launch. Existing approval must not be silently extended to changed transport bytes. Record any new transport identity separately from the preserved original snapshot and scientific identities.

The archive argument should be explicit and reconciled with the notebook default: `ARCHIVE=/content/isles-p001-input-16813698/train.7z`. A path difference alone neither proves a different archive nor excuses verification. The original archive size and digest checks remain mandatory. Review should establish that parameter resolution selects the intended archive without weakening checks or changing selection, extraction scope, output bindings, or resume rules.

Before any future dispatch could be considered, an appropriately scoped remote-state reconciliation would need to resolve the coordinator's ambiguity hold, including whether a process, receipt, console, or partial output already exists. Unknown state must remain a hold, not trigger a blind retry. Any transport verification should use synthetic, non-patient fixtures to demonstrate readable arguments, private capture, safe status reporting, failure propagation, and duplicate-launch prevention. No repair, verification execution, remote inspection, or dispatch is performed or authorized by this document. A scientific change discovered to be necessary would fall outside this discussion and require its own specification and review.

## Existing manual fallback

The existing human Run All fallback remains available under the campaign's previously recorded workflow: use the pinned notebook belonging to the unchanged snapshot, with `ARCHIVE=/content/isles-p001-input-16813698/train.7z`, the existing exact-byte approval checks, and private console/evidence retention. No notebook filename or additional digest is supplied here, so none is invented. Manual Run All is an execution fallback, not fresh scientific ratification, and it must not bypass unresolved dispatch ambiguity or create a duplicate attempt. This document neither asks anyone to run it nor initiates it.

## Preserved experiment pins and limits

- P001 v1.1 and snapshot `0770c7dcabe781cbfb87de505755e7aafa758f2e` remain unchanged. Existing specification, code, review, decision, adapter, and notebook bindings remain in force; no missing hashes are invented and no approval is relabeled.
- The frozen cohort is exactly 99 eligible development cases, using only `case_id` from the contribution table with SHA-256 `aba525122f796618761e6c4d29b664647760e8dff4987932c3ff6ab5456faae9`. The archive member manifest remains git blob `edb9a8c2ceb90df214cdd7ec167f0b1e8c858bb2`. Exactly 198 selected members are eligible for extraction; the 49 reserved cases remain excluded and whole-archive extraction remains prohibited.
- `train.7z` must remain exactly 99,014,629,647 bytes with MD5 `36ae28b9a17f7340b8bbef62b595cb57`; selected compressed files retain their size/CRC32 checks. A different transport path cannot substitute for these identity checks.
- Prediction remains admission NCCT-space Tmax strictly greater than 6.0 seconds, without fitting or postprocessing; all Tmax voxels must be finite. Each prediction is written before its evaluation label is opened. The target remains the released follow-up infarct mask after observed treatment, on the full released Tmax grid. Geometry, binary-label, voxel-volume, and input-integrity stopping rules remain unchanged, including affine tolerance `atol=1e-5`, `rtol=0`, and no resampling or extra exclusions.
- Primary evaluation remains equally weighted mean per-patient volumetric Dice across exactly 99 cases, with both-empty Dice 1 and exactly-one-empty Dice 0. Secondary metrics remain median Dice and mean absolute and signed volume error. Uncertainty remains 2,000 patient bootstrap resamples, seed `20260905`, central percentile 95% interval. No metrics or interval are available from this incident.
- Budget remains one CPU Colab run with at most 60 analysis minutes, separately receipted archive verification/extraction, and at most one staging invocation per new output path. No GPU, training, paid provisioning, or search is introduced. Resume remains limited to byte-verified checkpoints with identical bindings; failures, original console, and partial checkpoints must be retained.
- Publication remains limited to checked permitted artifacts: `summary.json`, `resolved_config.json`, `environment.json`, `execution_receipt.json`, and `RESULT_CARD.md`, without patient IDs or paths. Private console is separately reviewed for audit; private manifests, per-case metrics, masks, and inputs remain private. This discussion publishes nothing remotely.
- The baseline-plus-at-most-two-follow-ups envelope is unchanged. No follow-up is selected or run. Prior development outcome use, inherited Tmax units, treatment dependence, registration, and cohort selection continue to limit any eventual findings; no clinical or external-validation claim is licensed.

Disposition: retain the worker refusal and model provenance as incident evidence, retain the coordinator's ambiguity hold, and keep any readable transport repair a separate review item. No new approval, amendment, human ratification, execution, or scientific result is recorded.
