# Result card — P001 readiness proposal, 7 September 2026

**Question:** Does the new official starter evidence support unchanged P001 as a minimal admission-imaging prediction baseline?
**Evidence:** Yes, at the general decoded-unit convention level: the bound starter uses nibabel `get_fdata()` followed directly by a cutoff described as 9 seconds. P001 retains **Tmax > 6.0**. Historical source-bound Claude approval exists; P001 has no patient return.
**Limitations:** The starter predates the frozen v3 archive. Its derivative identity, decoding/scaling and present input availability remain unchecked. Completed hosted synthetic jobs and a still-present Colab worker do not establish patient readiness. No measurements, literature search or patient inspection were performed here.
**Next decision:** Recommend **ADOPT_CONDITIONALLY** under the explicitly selected proposed prediction charter. Prefer the preserved reviewed Colab CPU route if its access and dispatch gates can be resolved; otherwise consider a separately reviewed Linux backend amendment. Current launch remains **RESERVED / HOLD**. This packet grants no authority and requests no human ratification.

## Scope and scientific assessment

This is a prospective design assessment under `isles24-prediction/proposed-v1`, `isles24-prediction-rubric/proposed-v1` and `isles24-prediction-prompts/proposed-v1`, selected explicitly for this proposal preview. None is adopted or installed. The existing broad ISLES charter and historical scores remain unchanged. P001 is externally seeded and operator-delegated; the system authors this readiness assessment, not its frozen scientific implementation.

P001 is adequate as a minimal reference: using admission NCCT-space Tmax alone, a fixed mask predicts the released MRI-derived follow-up infarct mask after observed treatment. Equal-patient overlap and volume bias provide a useful reference before investing in fitting. It is a deterministic predictor, not a trained model. Its adequacy as a competitive predictor is unknown. Treatment-dependent tissue survival, registration, small lesions, site/scanner variation, selected membership and prior outcome exposure limit inference. A valid poor result would show limitations of this fixed threshold on these development cases; it would not show that admission imaging or learned prediction is uninformative. No novelty, causal, mechanism, untreated-infarct, clinical-use or external-validation claim is supported.

Conditional adoption is preferable to amendment because the new evidence supports the existing unit convention rather than contradicting it; it gives no reason to change frozen scientific bytes. Rejection would discard an interpretable reference without evidence of a fatal design defect. A verified unit contradiction or integrity failure must instead stop the affected work and prompt a separately versioned, reviewed amendment. There is no result-phase assessment or selected follow-up.

The legacy mandatory “model is using X” framing is inapplicable to this predictive estimand. Apply the proposed prediction guidance only in this explicitly selected preview: assess target, admission cutoff, permitted inputs, fixed rule, patient evaluation, leakage and uncertainty. Do not fabricate mechanism fields, demand model-beats-human evidence, penalize sealed labels or apply legacy generation quotas. A future schema/prompt adapter needs separate installation and affected-surface review; this document establishes no backend compatibility. No historical scoring is recalculated. No numeric priority is needed for this single-candidate adoption decision. Under the proposed rubric, present data readiness and feasibility are conditional; the archive-specific/current-input keystone is `NOT_INSPECTED`. The supplied starter excerpt supports only the general convention. No novelty search supports a novelty upgrade.

## What the new evidence resolves

Source: bound `docs/science/PREDICTION_PRIMARY_SOURCES_20260906.json`, `subsequent_checks`, checked 2026-09-07. It records [official starter `utils/isles24_evaluate.ipynb`](https://github.com/ezequieldlrosa/isles24/blob/34c66370e365bd5db4e9429154674928245e9915/utils/isles24_evaluate.ipynb), commit `34c66370e365bd5db4e9429154674928245e9915`, Git blob `f5f4ba2713389637f77419bea1fd5a0b506a3478`, SHA-256 `e5ada81c5cf0cd134205886b452c6f1890d551887a17120287daa0ea9b2aa61e`: cell 3 loads with `get_fdata`; cell 5 directly thresholds decoded values at a cutoff described as 9 seconds, without intervening scale conversion. This is supplied source evidence, not a fresh retrieval or notebook execution here.

Update the previous broad “Tmax units unresolved” finding to **general decoded scalar seconds convention supported; pinned-v3 applicability remains unverified**. Nine seconds is evidence about units in that starter, not a recommendation for P001's threshold. P001 uses `np.asarray(image.dataobj, dtype=np.float32)` and compares to 6.0. Assess the decoder's scaling behavior and float32 conversion against the starter's decoded values, rather than confusing stored integer values with decoded scalars. Neither a NIfTI header's time-unit field nor a plausible-looking value range alone proves perfusion scalar units. The notebook predates v3 and cannot certify its headers, maps or archive bytes.

The earlier charter receipt binds an older primary-source document digest (`5257b6ed150db6d4e5815167171a6596a387b7b33dc102cbadac7e2ebe8bf070`). Preserve that historical receipt. Do not assign that digest to the augmented source document or claim its prior review covers this new packet. A future prospective review must bind the augmented evidence bytes and this assessment separately.

Minimal remaining checks, proposed only:

1. Desk review release-specific derivative provenance and nibabel decoding documentation against the exact execution environment; record what supports v3 seconds and what remains an assumption. Existing synthetic jobs are complete; do not rerun them merely because this evidence changed. A focused synthetic scaling/threshold-boundary check is warranted only if their retained evidence does not cover the decoder question. No new code is authored here.
2. Only after the separate patient-access gate, use the pinned eligible selection and verify compressed size/CRC before opening any selected admission Tmax. Record relevant stored dtype, proxy slope/intercept, decoding behavior and derivative provenance privately. Compare decoded representations only as needed to establish implementation fidelity around the unchanged 6.0 rule, without outcome inspection or threshold selection. A small check can diagnose a convention discrepancy, but cannot certify every member; the existing full-cohort integrity checks still apply.
3. Do not inspect reserved cases or labels to resolve units. Do not infer units from lesion agreement. Any required conversion, changed precision policy, resampling, nonfinite repair or exclusion would need an explicit amendment and fresh review; none is approved here. Evidence that merely corroborates the existing convention can be attached prospectively without editing the frozen spec's inherited-assumption wording.

The original gap came from the prior program's inspected `probes/023/run.py` convention, inherited by P001 without archive-specific unit proof. The bound [v3 hosting record](https://zenodo.org/records/16813698) supports release metadata and the listed archive MD5. The bound [dataset paper identity](https://arxiv.org/abs/2408.11142) and [challenge paper identity](https://arxiv.org/abs/2408.10966) supply no newly verified full-text timing, annotation, evaluation or performance facts here. The spec's 2–9-day timing remains attributed to the spec. Claim-specific source gaps remain before stronger claims; no newer release is substituted.

## Concrete input and backend choice

| Item | Available evidence | Smallest remaining action / gate |
|---|---|---|
| Input identity | Historical mounted-Drive destination read-back at 2026-09-06 00:34:31 UTC: 99,014,629,647 bytes; MD5 `36ae28b9a17f7340b8bbef62b595cb57` | Reuse this historical receipt. Current access/integrity must be established in a separately authorized input step; no live rehash here. It is not provider durability proof. |
| Input location | `/content/drive/MyDrive/isles-pilot/archive-preservation-ec3e76e9b91f40ca847b490f9b5f7bfa/train.7z` | For the preserved notebook, set `ARCHIVE` explicitly to this historical destination and record the parameter. Do not assume old `/content/train.7z` or `/content/isles-p001-input-16813698/train.7z` survives. No new download unless integrity requires it and the action is authorized. |
| Colab CPU — preferred conditional candidate | Frozen scientific source/notebook and historical opposing review; synthetic remote execution/retrieval demonstrated | Prior worker process remains present, but Drive availability is unverified. Reconcile job store, remote process state, output paths and private checkpoint bindings; establish authorized CPU connection and Drive mount/browser consent; then deliberately resolve dispatch hold. Do not blindly Run All, kill/restart a worker, or retry ambiguous dispatch. |
| Linux CPU — fallback proposal | Purchased host, authenticated model accounts and completed hosted synthetic jobs | These are not patient transport evidence. Define private input/return transport, capacity, permissions, credential-free scientific worker, environment, resource cap, immutable snapshot and console/checkpoint handling; propose an explicit backend amendment to the Colab-only compute contract and obtain affected-surface opposing review plus separate transfer/launch disposition. Do not move data or run here. |
| Current HEAD | Supplied status identifies changed `scout.py` dependency and later publisher changes | Old review is not valid for arbitrary HEAD. Verify every applicable reviewed file binding; refresh review for changed scout/publisher/transport surfaces used by the chosen route. Preserve historical approval rather than overwriting it. |

Recommend keeping the existing scientific source and Colab CPU notebook for the earliest bounded supervised baseline, conditional on those gates. Linux provider direction makes it a sensible alternative if Colab cannot be made available, but does not amend P001's one-CPU-Colab-run contract. Do not create a second implementation or make a transport change look like unchanged authorization. The approximately two-hour Colab engineering bound is a ceiling, not a promise or measured effort.

Historical CPU/notebook approval is available evidence, not current reserved patient-launch authority. The coordinator remains blocked with zero automatic retries and an ambiguity inbox. A present worker is not proof of a running patient job; `NOT_VISIBLE` is not proof of absence. Reconciliation must precede a deliberate hold resolution, preserving any original logs/checkpoints and preventing duplicate execution.

## Preserved experiment and prospective linkage

The companion JSON transcribes exact identities; none was rehashed from the unavailable original repository. Preserve the entire historical `review.json` inventory, its execution/response bindings, build receipt, investigator decision and publication manifest. The spec and decision's pending-review wording precedes the later Claude `APPROVE`; preserve both. The reviewer was `claude-fable-5`, while investigator/build attribution is Codex / GPT-6 Astra. No human approval is manufactured or relabeled.

Future adoption linkage must bind the exact proposed charter, rubric, prompt guidance, original adoption proposal and its proposal review, this updated readiness packet and new opposing review, unchanged experiment spec/code and historical review, and the selected execution snapshot/environment. The proposal review's `APPROVE` means the proposal met its bounded task; it does not mean charter adoption, runtime readiness or launch. This packet is itself unreviewed and non-executable.

Retain the fixed contract: sort exactly 99 unique IDs from only `case_id` in the pinned contribution table; discard ranks and other columns. Select precisely one canonical admission Tmax and one canonical ses-02 NCCT-space lesion per ID by full manifest paths, exactly 198 members. Ambiguity fails. The reserved 49 stay untouched; 99 plus 49 is not a release-accounting rule granting access to any remainder. Never extract the whole archive. Verify the archive size/MD5 if using it, then every selected compressed member's size/CRC before opening; retain private input hashes.

Predict finite 3D Tmax >6.0 on the full grid, without fitting/filtering. Any nonfinite voxel, including background, stops. Persist prediction before opening its label for evaluation. Require finite binary 3D labels, matching shape/affine (atol 1e-5, rtol 0), and positive voxel volume; no implicit resampling. Primary: mean per-patient volumetric Dice with equal patient weight, both-empty=1 and exactly-one-empty=0. Secondary: median Dice, mean absolute and signed prediction-volume errors in mL. Use 2,000 patient bootstrap resamples, seed 20260905, central percentile 95% interval; it describes resampling variability on reused development cases.

One CPU Colab run, at most 60 analysis minutes, separately receipted archive verification/staging and at most one staging invocation per new output path remain pinned. Exactly 99 completions are required; no extra exclusions. Resume only byte-verified checkpoints with identical spec/code/input/review bindings and prediction hashes. Preserve failures and partial work; an invalid run is not a negative result. No GPU, fitting, tuning, new spending, credentials, provisioning or preselected follow-up.

## Existing return, validation, recording and interpretation routes

These are descriptions for a later authorized run, not commands to execute now. The supplied context includes the runner and route references, but not validator/lifecycle CLI implementations. Exact argument syntax or validator coverage beyond that evidence is unavailable; do not invent a runnable command or claim those routes were exercised here.

1. **Source/selection gates:** `campaigns/isles24-pilot/experiments/P001/run.py --preflight` reads pinned cohort/manifest metadata and reports counts without opening payloads. It returns before `verify_decision`, so a successful preflight does **not** certify authorization. The real run calls `orchestrator.campaign.verify_decision` on campaign/spec/decision/review/code before payload access. The runner requires exactly one of `--archive` or `--data-root`, with `--output-dir`; these interfaces do not grant permission to use them. Use the preserved notebook/manual Run All route only after reconciliation and named launch gates.
2. **Private execution evidence and return:** retain the original sibling `<OUTPUT_DIR>.console.log`, failure history and `<OUTPUT_DIR>.private` binding, selected-input manifest/hashes, checkpoint index, per-case checkpoints and predictions outside Git. The runner does not itself create the sibling console capture; the execution/return route must preserve it. Retrieve originals privately. Missing/partial/invalid evidence holds scientific claims; do not reconstruct a successful console or substitute a failed log.
3. **Validation:** use the existing experiment-specific `campaigns/isles24-pilot/experiments/P001/validate_return.py` and campaign lifecycle validation/verification route (`orchestrator/campaign_lifecycle.py`, `orchestrator/campaign.py`) on the exact return and source bindings. Acceptance must establish exactly 99 completions, matching spec/code/cohort/manifest/review, required artifacts, coherent finite metrics/uncertainty, private console/checkpoint evidence and all recorded failures. The supplied runner shows those creation checks; validator implementation was not supplied, so verify its actual coverage and invocation from the selected reviewed snapshot before use. A generic or historical core-validator pass alone is insufficient.
4. **Packaging and recording:** `scripts/package_pilot.py`, `orchestrator/publication.py` and the shared `scout.py` `record-result` / `orchestrator/campaign_lifecycle.py` route are the existing interfaces. Preserve the source commit and expected result blob binding and use the campaign-specific validated route, not an improvised copy. The evidenced `record-result` options `--expected-blob` and `--source-commit` identify provenance; their complete P001 invocation is not supplied. The separate `--publication-subset DECLARATION.json` mechanism described for 047 is not a P001 permission to drop required evidence. Verify publication policy and outgoing history before any permitted publication. A valid local return is not remote-write authority in this task.
5. **Interpretation:** after valid source-bound recording, use the existing persistent campaign interpretation and opposing-review routes through `scout.py`, `orchestrator/campaign_lifecycle.py` and `orchestrator/campaign_review.py`. Bind interpretation to the exact recorded result and source; inspect overlap, uncertainty, volume bias, failures and limitations. No interpretation of nonexistent P001 measurements, automatic follow-up selection or human ratification marker. Only a valid baseline and reviewed interpretation can inform a later one-at-a-time comparison proposal within the two-follow-up ceiling.

The exact five required/allowed public files are `summary.json`, `resolved_config.json`, `environment.json`, `execution_receipt.json`, `RESULT_CARD.md`. Private IDs/paths, inputs, manifests, per-case metrics, predictions and original console stay outside Git; console is supplied separately for audit. Missing usage, cost and intervention measurements remain null. Historical synthetic complete build/verify/package/validate/import/interpret evidence demonstrates that route only; it is not a real P001 return or proof of every current source binding.

## Supervised progress and Wednesday critical path

Now-eligible supervised desk work: review this exact evidence update and external-seed adoption recommendation, resolve public-source questions, prepare prospective linkage and a readable backend decision, and inspect already permitted synthetic receipts for remaining operational gaps. Supervised synthetic setup may continue within existing authority where a specific unresolved issue requires it; completed jobs need no replay. No patient data is accessed in this task. Do not make desk science or a separately gated supervised manual route wait for full unattended acceptance.

For Wednesday, **9 September 2026**, the smallest dependent path is: source-bound readiness/adoption review and prospective charter disposition → choose and bind the execution backend (amend/review if Linux; refresh changed dependencies on either applicable route) → separately permitted current-input/unit checks and job/process/checkpoint reconciliation → separate reserved launch disposition → one bounded baseline → private original evidence and aggregate return → validation and source-bound recording → system interpretation and opposing review. These are conditional gates, not approvals requested or performed here. If unavailable, deliver this reviewed readiness packet with explicit blockers; no Wednesday completion or performance guarantee exists.

Unattended operation has its own remaining gates: concrete protected writer/reset permission approval, shared initialization, hosted 48/96 activation verification, persistent recovery/reporting and exercised status/pause/resume controls, then actual laptop-disconnected observation (24–48 hours if claimed). R1/R2/R4 historical ratification and R3's conditional N=48/hard-96 selection remain valid; selection is not activation or a spending cap. Notification/synthetic device ACK is available evidence, not operational phone-decision authority. Do not request new sign-in merely because historical queue text is stale.

047b evidence reconciliation and cross-charter propagation can remain adjacent desk threads. Pending 047b candidate conclusions supply dependency notices only. **P001 does not depend on 047 acceptance, cleanup, registry attestation or its missing successful console.** Preserve Phase A, historical approvals and the intended Phase-B destination. Broader intake/context redesign is not a prerequisite either.

Missing `ONBOARDING_astra_implementation.md` and named deployment-review originals remain disclosed gaps, not reconstructed authority. The available campaign, canonical `REMOTE_OPERATING_DIRECTION.md`, reviewer directive, readiness direction and explicit task bounds support this proposal. Any later decision that depends on missing originals must address that dependency. No human ratification, launch, patient inspection, experiment execution, remote writes or original-repository search occurred here.
